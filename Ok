-- This file was generated at discord.gg/syncrypt

LoadStart = tick()
if not string.split then
    function string.split(p1, p2)
        local t1 = {}
        local n1 = 1

        while true do
            local start, stop = string.find(p1, p2, n1, true)

            if not start then
                break
            end

            table.insert(t1, string.sub(p1, n1, start - 1))
            n1 = stop + 1
        end

        table.insert(t1, string.sub(p1, n1))

        return t1
    end
end
if not table.find then
    function table.find(p3, p4)
        for i = 1, #p3 do
            if p4 == p3[i] then
                return i
            end
        end

        return nil
    end
end
local u1 = (function()
    local ok, result = pcall(function()
        if isfile and isfile("cheesy/data/userdata.json") then
            return game:GetService("HttpService"):JSONDecode(readfile("cheesy/data/userdata.json")).UILibrary
        end
    end)

    return ok and result or nil
end)()
local u2
local u3
local u4;
(function()
    local v174 = game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/refs/heads/main/Library.lua"):gsub("Data.Description = tostring(Info.Description)", "Data.Description = Info.Description and tostring(Info.Description) or \"\""):gsub("local LoaderIcon = Library:GetCustomIcon(LoadingInfo.LoadingIcon)", "local LoaderIcon = Library:GetCustomIcon(LoadingInfo.LoadingIcon) or Library:GetCustomIcon(LoadingInfo.Icon) or {Url=\"\",ImageRectOffset=Vector2.zero,ImageRectSize=Vector2.zero}"):gsub("local Icon = Library:GetCustomIcon(LoadingInfo.Icon)", "local Icon = Library:GetCustomIcon(LoadingInfo.Icon) or {Url=\"\",ImageRectOffset=Vector2.zero,ImageRectSize=Vector2.zero}"):gsub("Image = LoaderIcon%.Url,", "Image = LoaderIcon and LoaderIcon.Url or \"\","):gsub("local IconData = Library:GetCustomIcon(Icon)", "local IconData = Library:GetCustomIcon(Icon) or {Url=\"\",ImageRectOffset=Vector2.zero,ImageRectSize=Vector2.zero}"):gsub("ImageRectOffset = LoaderIcon%.ImageRectOffset,", "ImageRectOffset = LoaderIcon and LoaderIcon.ImageRectOffset or Vector2.zero,"):gsub("ImageRectSize = LoaderIcon%.ImageRectSize,", "ImageRectSize = LoaderIcon and LoaderIcon.ImageRectSize or Vector2.zero,")

    u2 = loadstring(v174)()
    u3 = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/refs/heads/main/addons/SaveManager.lua"))()
    u4 = loadstring(game:HttpGet("https://raw.githubusercontent.com/deividcomsono/Obsidian/refs/heads/main/addons/ThemeManager.lua"))()
end)()
local function v5(p5)
    if typeof(p5) == "string" then
        local ok, result = pcall(Color3.fromHex, p5)

        if ok then
            return result
        end
    end

    local function v179(p6)
        local ok, result = pcall(string.sub, p5, p6, p6 + 1)

        if not ok then
            return 0
        end

        local ok2, result2 = pcall(tonumber, result, 16)

        return ok2 and result2 or 0
    end

    return Color3.fromRGB(v179(1), v179(3), v179(5))
end
local HttpService = game:GetService("HttpService")
local function v7(p7)
    if typeof(p7) == "string" then
        return p7
    end

    local ok, result = pcall(function()
        return HttpService:JSONDecode("\"" .. p7 .. "\"")
    end)

    if ok and typeof(result) == "string" then
        return result
    end

    local ok3, result3 = pcall(string.len, p7)

    if ok3 and result3 > 0 then
        local t2 = {}

        for i = 1, result3 do
            local v187
            local ok4, result4 = pcall(string.byte, p7, i)
            if ok4 then
                v187 = result4
            end
            if v187 then
                t2[#t2 + 1] = string.char(v187)
            end
        end

        if #t2 > 0 then
            local ok5, result5 = pcall(table.concat, t2)

            if ok5 then
                return result5
            end
        end
    end

    return ""
end
local function v8()
    u2.TabButtons = {}

    local Create = u2.Create

    function u2.Create(p8, p9, p10)
        if p10 then
            for k, v in pairs(p10) do
                if typeof(v) == "table" and type(v) == "table" then
                    p10[k] = v7(v)
                end
            end
        end

        return Create(p8, p9, p10)
    end

    local CreateWindow = u2.CreateWindow

    function u2.CreateWindow(p11, ...)
        local v984 = CreateWindow(p11, ...)
        local v985 = v984.Holder and v984.Holder:FindFirstChildOfClass("Frame")

        if not v985 then
            return v984
        end

        local Frame = v985:FindFirstChildOfClass("Frame")

        if not Frame then
            return v984
        end

        local Frame2 = Frame:FindFirstChildOfClass("Frame")

        if not Frame2 then
            return v984
        end

        local ScrollingFrame = Frame2:FindFirstChildOfClass("ScrollingFrame")

        if not ScrollingFrame then
            return v984
        end

        local Frame3 = Frame2:FindFirstChildOfClass("Frame")

        if not Frame3 then
            return v984
        end

        local AddTab = v984.AddTab
        local n2 = 0

        function v984.AddTab(p12, p13, p14)
            local v1326 = AddTab(p12, p13, p14)
            local v1327
            local v1328
            for _, child in ipairs(ScrollingFrame:GetChildren()) do
                if not child:IsA("Frame") then
                    continue
                end

                local TextLabel = child:FindFirstChildOfClass("TextLabel")

                if TextLabel and p13 == TextLabel.Text then
                    v1327 = child
                    v1328 = TextLabel

                    break
                end
            end
            if v1327 then
                table.insert(u2.TabButtons, {
					Frame = v1327,
					Label = v1328
				})
            end
            n2 += 1
            local n3 = 0
            local v1333
            for _, child in ipairs(Frame3:GetChildren()) do
                if child.Name ~= "TabFrame" then
                    continue
                end

                n3 += 1

                if n3 == n2 then
                    v1333 = child

                    break
                end
            end
            v1326.SetVisible = (function(p15, p16)
                return function(_, p18)
                    if p16 then
                        p16.Visible = p18
                    end

                    if p15 then
                        p15.Visible = p18
                    end
                end
            end)(v1333, v1327)

            return v1326
        end

        return v984
    end

    local _ = u4.ApplyTheme

    function u4.ApplyTheme(p19, p20)
        local CustomTheme = p19:GetCustomTheme(p20)
        local v995 = CustomTheme or p19.BuiltInThemes[p20]

        if not v995 then
            return
        end

        if p19.Library.InnerVideoBackground ~= nil then
            p19.Library.InnerVideoBackground.Visible = false
        end

        local v996 = v995[2]

        for k, v in next, CustomTheme or v996 do
            if k == "VideoLink" then
                p19.Library[k] = v

                if p19.Library.Options[k] then
                    p19.Library.Options[k]:SetValue(v)
                end
            else
                local v999 = v5(v)

                p19.Library[k] = v999

                if p19.Library.Options[k] then
                    local ok, _ = pcall(p19.Library.Options[k].SetValueRGB, p19.Library.Options[k], v999)

                    if not ok then
                        pcall(p19.Library.Options[k].SetValue, p19.Library.Options[k], v999)
                    end
                end
            end
        end

        p19:ThemeUpdate()
    end
end
v8()
local n4 = 1
if game.PlaceId ~= 6516141723 and game.PlaceId ~= 6839171747 and game.PlaceId ~= 110258689672367 and game.PlaceId ~= 10549820578 then
    return
end
function TableClear(p21)
    for k in pairs(p21) do
        p21[k] = nil
    end
end
function TableFind(p22, p23)
    for i = 1, #p22 do
        if p23 == p22[i] then
            return i
        end
    end

    return nil
end
local FinishedLoadingRoom = game:GetService("ReplicatedStorage").GameData:FindFirstChild("FinishedLoadingRoom")
if FinishedLoadingRoom then
    FinishedLoadingRoom:Destroy()
end
CHEESE_KEY = "joincheesydsc"
DISCORD_INVITE = "https://discord.gg/CuZJQaCquK"
task.spawn(function()
    local v200 = syn and syn.request or (request or http_request)

    if not v200 then
        return
    end

    local LocalPlayer = game:GetService("Players").LocalPlayer

    if not LocalPlayer then
        return
    end

    local ok, result = pcall(function()
        return game:GetService("MarketplaceService"):GetProductInfo(game.PlaceId)
    end)
    local v204 = (function()
        local ok6, result6 = pcall(identifyexecutor)

        if ok6 then
            return result6
        end

        local ok7, result7 = pcall(function()
            return _G.identifyexecutor()
        end)

        if ok7 then
            return result7
        end

        return "Unknown"
    end)()

    pcall(function()
        v200({
			Url = "https://cheesy.wispbyte.org/api/send" .. "?userid=" .. LocalPlayer.UserId .. "&placeid=" .. game.PlaceId .. "&placename=" .. game:GetService("HttpService"):UrlEncode(ok and result.Name or "Unknown") .. "&executor=" .. game:GetService("HttpService"):UrlEncode(v204) .. "&executions=" .. n4,
			Method = "GET"
		})
    end)
end)
local function v11()
    pcall(function()
        if not writefile then
            return
        end

        pcall(function()
            makefolder("cheesy")
        end)
        pcall(function()
            makefolder("cheesy/data")
        end)

        local t3 = {
			key = CHEESE_KEY,
			executions = n4,
			UILibrary = u1
		}

        if isfile("cheesy/data/userdata.json") then
            local data = game:GetService("HttpService"):JSONDecode(readfile("cheesy/data/userdata.json"))

            if type(data) == "table" then
                t3.UILibrary = u1 or data.UILibrary
            end
        end

        writefile("cheesy/data/userdata.json", game:GetService("HttpService"):JSONEncode(t3))
    end)
end
if not (function()
    local ok, result = pcall(function()
        if isfile and isfile("cheesy/data/userdata.json") then
            local v1008 = readfile("cheesy/data/userdata.json")
            local data = game:GetService("HttpService"):JSONDecode(v1008)

            if data then
                n4 = (data.executions or 0) + 1
                u1 = data.UILibrary

                return data.key == CHEESE_KEY
            end
        end

        return false
    end)

    if ok and result then
        v11()

        return true
    end

    return false
end)() then
    local u12 = false
    local v13 = u2:CreateLoading({
		Title = "Discord 'key'",
		TotalSteps = 1,
		ShowSidebar = true,
		WindowWidth = 520,
		Icon = 106990021902590,
		LoadingIcon = 106990021902590
	})

    v13:SetDescription("We don't have any key system at all. \nThe entire key is in the discord and its lifetime,\nthere is no ads or anything. It isn't a 'key system' at all")
    v13.Sidebar:AddInput("KeyInput", {
		Text = "Key",
		Placeholder = "",
		ClearTextOnFocus = false
	})
    v13.Sidebar:AddButton({
		Text = "Copy/Open Discord Invite",
		Func = function()
        pcall(function()
            setclipboard(DISCORD_INVITE)
        end)
        pcall(function()
            _G.Clipboard.set(DISCORD_INVITE)
        end)
    end
	})
    v13.Sidebar:AddButton({
		Text = "Continue",
		Func = function()
        if u2.Options.KeyInput.Value == CHEESE_KEY then
            n4 = 1
            v11()
            u12 = true
            v13:Continue()
        end
    end
	})

    while not u12 do
        task.wait()
    end
end
pcall(function()
    u2.Options.KeyInput:Destroy()
end)
u2.Options.KeyInput = nil
if u1 == "Linoria" then
    (function()
        local function v175(p24)
            return (("_istable=function(v)if typeof(v)~='table'then return false end return not pcall(string.sub,v,1,1)end;" .. p24):gsub("typeof%s*%((%w+)%)%s*==%s*\"table\"", "_istable(%1)"):gsub("typeof%s*%((%w+)%)%s*~=%s*\"table\"", "not _istable(%1)"))
        end

        u2 = loadstring(v175(game:HttpGet("https://raw.githubusercontent.com/mstudio45/LinoriaLib/refs/heads/main/Library.lua")))()
        u3 = loadstring(v175(game:HttpGet("https://raw.githubusercontent.com/mstudio45/LinoriaLib/refs/heads/main/addons/SaveManager.lua")))()
        u4 = loadstring(v175(game:HttpGet("https://raw.githubusercontent.com/mstudio45/LinoriaLib/refs/heads/main/addons/ThemeManager.lua")))()
    end)()
    v8()
end
local v14 = game:HttpGet("https://raw.githubusercontent.com/bocaj111004/ESPLibrary/refs/heads/main/Library.lua"):gsub("']</font>'", "'M]</font>'"):gsub("Frame%.Visible = OnScreen", "Frame.Visible = OnScreen and Library.ShowESPText ~= false"):gsub("local ScreenPoint, OnScreen = Camera:WorldToViewportPoint%(ObjectPos%)", "local ScreenPoint, OnScreen = Camera:WorldToViewportPoint(ObjectPos); if OnScreen then local Dist = (Camera.CFrame.Position - ObjectPos).Magnitude; local ObjMaxDist = Library.ObjectMaxDistance and Library.ObjectMaxDistance[Object]; if Dist > (ObjMaxDist or Library.MaxDistance) then OnScreen = false end end"):gsub("Tracers = false,", "Tracers = false, TracersEnabled = {},"):gsub("LineData%[1%].Visible = OnScreen", "LineData[1].Visible = OnScreen and (Library.Tracers == true or Library.TracersEnabled[Object])"):gsub("if LineData and CachedHighlight and Library.Tracers == true and OnScreen then", "if LineData and CachedHighlight and (Library.Tracers == true or Library.TracersEnabled[Object]) and OnScreen then"):gsub("TracersFrame.Parent = ScreenGui", [[TracersFrame.Parent = ScreenGui

function Library:SetTracersEnabled(Object, Value)
	if Value then
		Library.TracersEnabled[Object] = true
		TracersFrame.Visible = true
	else
		Library.TracersEnabled[Object] = nil
		local any = false
		for _ in pairs(Library.TracersEnabled) do any = true break end
		TracersFrame.Visible = any or Library.Tracers
	end
end]])
local v15 = loadstring(v14)()
v15.ObjectMaxDistance = {}
local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local UserInputService = game:GetService("UserInputService")
SoundService = game:GetService("SoundService")
Lighting = game:GetService("Lighting")
TextChatService = game:GetService("TextChatService")
CoreGui = game:GetService("CoreGui")
VirtualUser = game:GetService("VirtualUser")
ProximityPromptService = game:GetService("ProximityPromptService")
TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local PlayerGui = LocalPlayer:FindFirstChildOfClass("PlayerGui")
local CurrentCamera = workspace.CurrentCamera
local CurrentRooms = workspace:FindFirstChild("CurrentRooms")
_CameraChangedConn = workspace:GetPropertyChangedSignal("CurrentCamera"):Connect(function()
    CurrentCamera = workspace.CurrentCamera
end)
local Character
local Humanoid
local HumanoidRootPart
local u27
local u28
PartProperties = {}
local u29
local function v30()
    local v207 = ReplicatedStorage:FindFirstChild("GameData") and ReplicatedStorage.GameData:FindFirstChild("LatestRoom") or ReplicatedStorage:FindFirstChild("FloorReplicated") and ReplicatedStorage.FloorReplicated:FindFirstChild("LatestRoom")

    if v207 and v207 ~= u29 then
        u29 = v207

        if u29.Value then
            LocalPlayer:SetAttribute("CurrentRoom", u29.Value)
        end

        u29.Changed:Connect(function(property)
            LocalPlayer:SetAttribute("CurrentRoom", property)

            if Toggles and (Toggles.AmbientToggle and Toggles.AmbientToggle.Value) then
                TweenService:Create(Lighting, TweenInfo.new(0.2, Enum.EasingStyle.Exponential), {
					Ambient = Options.AmbientColor.Value
				}):Play()
            end
        end)
    end

    return v207
end
v30()
local function v31(p25)
    p25.ChildAdded:Connect(function(child)
        if child.Name == "LatestRoom" then
            v30()
        end
    end)

    if p25:FindFirstChild("LatestRoom") then
        v30()
    end
end
local GameData = ReplicatedStorage:FindFirstChild("GameData")
if GameData then
    v31(GameData)
else
    ReplicatedStorage.ChildAdded:Connect(function(child)
        if child.Name == "GameData" then
            v31(child)
        end
    end)
end
local FloorReplicated = ReplicatedStorage:FindFirstChild("FloorReplicated")
if FloorReplicated then
    FloorReplicated.ChildAdded:Connect(function(child)
        if child.Name == "LatestRoom" then
            v30()
        end
    end)
end
local Toggles2 = u2.Toggles
local Options2 = u2.Options
local u36
local t12
local function v38()
    if not Character or not HumanoidRootPart then
        return
    end

    TableClear(PartProperties)
    CustomPhysics = PhysicalProperties.new(100, HumanoidRootPart.CustomPhysicalProperties.Friction, HumanoidRootPart.CustomPhysicalProperties.Elasticity, HumanoidRootPart.CustomPhysicalProperties.FrictionWeight, HumanoidRootPart.CustomPhysicalProperties.ElasticityWeight)

    for _, descendant in Character:GetDescendants() do
        if descendant:IsA("BasePart") then
            PartProperties[descendant] = descendant.CustomPhysicalProperties

            if Toggles2.RemoveAcceleration and Toggles2.RemoveAcceleration.Value then
                descendant.CustomPhysicalProperties = CustomPhysics
            end
        end
    end
end
local function v39()
    if workspace:FindFirstChild("Lobby") then
        return "Lobby"
    end

    local GameData2 = ReplicatedStorage:FindFirstChild("GameData")

    if not GameData2 then
        return "Lobby"
    end

    local Floor = GameData2:FindFirstChild("Floor")

    if not Floor then
        return "Lobby"
    end

    local str = tostring(Floor.Value)

    if str == "" then
        return "Lobby"
    end

    if str == "Hotel" then
        local v216 = ReplicatedStorage:FindFirstChild("RemotesFolder") or ReplicatedStorage:FindFirstChild("Bricks")

        if v216 and v216.Name == "Bricks" then
            return "OldHotel"
        end
    end

    return str
end
CurrentFloor = nil
OldJump = false
OldSlide = false
local function v40()
    if JumpAttributeConnection then
        JumpAttributeConnection:Disconnect()
    end

    if SlideAttributeConnection then
        SlideAttributeConnection:Disconnect()
    end

    if not Character then
        return
    end

    OldJump = Character:GetAttribute("CanJump") or false
    OldSlide = Character:GetAttribute("CanSlide") or false

    if Toggles2.EnableCharacterJump and Toggles2.EnableCharacterJump.Value then
        Character:SetAttribute("CanJump", true)
    end

    if Toggles2.EnableCharacterSlide and Toggles2.EnableCharacterSlide.Value then
        Character:SetAttribute("CanSlide", true)
    end

    JumpAttributeConnection = Character:GetAttributeChangedSignal("CanJump"):Connect(function()
        local CanJump = Character:GetAttribute("CanJump")

        if Toggles2.EnableCharacterJump and (Toggles2.EnableCharacterJump.Value and CanJump ~= true) or (not Toggles2.EnableCharacterJump or not Toggles2.EnableCharacterJump.Value) then
            OldJump = CanJump
        end

        if Toggles2.EnableCharacterJump and Toggles2.EnableCharacterJump.Value then
            Character:SetAttribute("CanJump", true)
        end
    end)
    SlideAttributeConnection = Character:GetAttributeChangedSignal("CanSlide"):Connect(function()
        local CanSlide = Character:GetAttribute("CanSlide")

        if Toggles2.EnableCharacterSlide and (Toggles2.EnableCharacterSlide.Value and CanSlide ~= true) or (not Toggles2.EnableCharacterSlide or not Toggles2.EnableCharacterSlide.Value) then
            OldSlide = CanSlide
        end

        if Toggles2.EnableCharacterSlide and Toggles2.EnableCharacterSlide.Value then
            Character:SetAttribute("CanSlide", true)
        end
    end)
end
local function v41()
    Character = LocalPlayer.Character

    if Character then
        Humanoid = Character:FindFirstChildOfClass("Humanoid")
        HumanoidRootPart = Character:FindFirstChild("HumanoidRootPart")
        u27 = Character:FindFirstChild("Collision") or Character:FindFirstChild("CollisionPart")
        u28 = Character:FindFirstChild("CollisionPart") or Character:FindFirstChild("Collision")
        v38()
        v40()
    end
end
local u42
local u43
local u44
local u45
local u46
local u47
local u48
FakePrompts = {}
local u49
Connections = {}
local u50
local u51
local u52
function SetupCharacterAnticheat()
    if not Character then
        return
    end

    if u50 then
        u50:Disconnect()
        u50 = nil
    end

    if u51 then
        u51:Disconnect()
        u51 = nil
    end

    if u52 then
        u52:Disconnect()
        u52 = nil
    end

    u50 = Character:GetAttributeChangedSignal("Climbing"):Connect(function()
        if Character:GetAttribute("Climbing") == true and (Toggles2.DisableAnticheat.Value and not t12.AnticheatDisabled) then
            task.wait(0.25)
            Character:SetAttribute("Climbing", false)
            t12.AnticheatDisabled = true
            u2:Notify({
				Title = "Disabled anticheat",
				Description = "Re-enables after cutscenes or halt",
				Time = 5
			})
        end
    end)

    if u36 then
        u51 = u36:WaitForChild("Cutscene").OnClientEvent:Connect(function(p26)
            if t12.AnticheatDisabled and not p26:find("SewerSeek") then
                t12.AnticheatDisabled = false
                u2:Notify({
					Title = "Anticheat re-enabled",
					Description = "Interact with a ladder for re-disabling",
					Time = 5
				})
            end
        end)
        u52 = u36:WaitForChild("UseEnemyModule").OnClientEvent:Connect(function(p27)
            if p27 == "Void" or p27 == "Glitch" then
                if t12.AnticheatDisabled then
                    t12.AnticheatDisabled = false
                    u2:Notify({
						Title = "Anticheat re-enabled",
						Description = "Interact with a ladder for re-disabling",
						Time = 5
					})
                end

                if u29 then
                    LocalPlayer:SetAttribute("CurrentRoom", u29.Value)
                end
            end
        end)
    end

    Character:SetAttribute("Climbing", nil)
end
v41()
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(0.5)
    v41()
    u42()

    if Toggles2.FlyToggle.Value then
        u43()
    end

    u44()
    u45()
    u46()
    u47()
    u48()
    SetupCharacterAnticheat()
end)
u36 = ReplicatedStorage:FindFirstChild("RemotesFolder") or if not ReplicatedStorage:FindFirstChild("EntityInfo") then not ReplicatedStorage:FindFirstChild("Bricks") or ReplicatedStorage:FindFirstChild("Bricks") else ReplicatedStorage:FindFirstChild("EntityInfo")
FakeEvents = {
	Screech = Instance.new("RemoteEvent"),
	Shade = Instance.new("RemoteEvent"),
	A90 = Instance.new("RemoteEvent"),
	Surge = Instance.new("RemoteEvent")
}
FakeEvents.Screech.Name = "Screech"
FakeEvents.Shade.Name = "ShadeResult"
FakeEvents.A90.Name = "A90"
FakeEvents.Surge.Name = "SurgeRemote"
if u36 then
    FakeEvents.Screech_Real = u36:FindFirstChild("Screech")
    FakeEvents.Shade_Real = u36:FindFirstChild("ShadeResult")
    FakeEvents.A90_Real = u36:FindFirstChild("A90")
    FakeEvents.Surge_Real = u36:FindFirstChild("SurgeRemote")
end
local function v53()
    local PlayerGui2 = LocalPlayer:FindFirstChildOfClass("PlayerGui")
    local v218 = PlayerGui2 and PlayerGui2:FindFirstChild("MainUI")
    local v219 = v218 and v218:FindFirstChild("Initiator")
    local v220 = v219 and v219:FindFirstChild("Main_Game")
    local v221 = v220 and v220:FindFirstChild("RemoteListener")

    return v221 and v221:FindFirstChild("Modules")
end
local v54 = u2:CreateWindow({
	Title = "cheesy",
	Footer = "Revamped :O dsc.gg/cheesyscript",
	Icon = 106990021902590,
	IconSize = UDim2.fromOffset(42, 42),
	ToggleKeybind = Enum.KeyCode.RightShift,
	NotifySide = "Right",
	ShowCustomCursor = true,
	AutoShow = false,
	Center = true,
	TabPadding = 3,
	CornerRadius = 2
})
local t4 = {
	Home = v54:AddTab("Home", "door-closed"),
	Main = v54:AddTab("Main", "house"),
	Visuals = v54:AddTab("Visuals", "scan-eye"),
	Exploits = v54:AddTab("Exploits", "sparkle"),
	Miscellaneous = v54:AddTab("Miscellaneous", "settings"),
	Mines = v54:AddTab("Mines", "gem"),
	FoolsHotel = v54:AddTab("Hotel-/Fools", "party-popper"),
	Rooms = v54:AddTab("Rooms", "bed-double"),
	Garden = v54:AddTab("Outdoors", "tree-pine")
}
for _, v in ipairs(u2.TabButtons) do
    if typeof(v) == "table" and v.Label then
        local LabelText = v.Label.Text

        if LabelText == "Hotel-" or LabelText == "Fools" or LabelText == "Hotel-/Fools" then
            t4.FoolsHotel.TabLabelRef = v.Label
        end
    end
end
Groupboxes = {}
Groupboxes.SubfloorsFools = t4.FoolsHotel:AddLeftGroupbox("Fools")
Groupboxes.SubfloorsFools:AddToggle("BypassKillbricks", {
	Text = "Anti Killbricks",
	Default = false,
	Tooltip = "Disables Lava from hurting you"
})
Groupboxes.SubfloorsFools:AddToggle("BypassSeekingWall", {
	Text = "Anti Seeking Wall",
	Default = false,
	Tooltip = "Disables ScaryWall from hurting you"
})
Groupboxes.SubfloorsFools:AddToggle("BypassBanana", {
	Text = "Anti Banana",
	Default = false,
	Tooltip = "Disables Banana Peel from making you slip"
})
Groupboxes.SubfloorsFools:AddToggle("BypassJeff", {
	Text = "Anti Jeff",
	Default = false,
	Tooltip = "Disables Jeff the Killer from stabbing you"
})
Groupboxes.SubfloorsFools:AddDivider()
Groupboxes.SubfloorsFools:AddToggle("RemoveSeekTrigger", {
	Text = "Delete Seek Trigger",
	Default = false,
	Tooltip = "Deletes/disables the Seek cutscene trigger",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Groupboxes.SubfloorsFools:AddToggle("RemoveFigure", {
	Text = "Delete Figure",
	Default = false,
	Tooltip = "Removes Figure, however this is inconsistent",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Groupboxes.SubfloorsFools:AddToggle("AutoRevive", {
	Text = "Infinite Revives",
	Default = false,
	Tooltip = "Automatically revives after dying without using a revive and having infinite usage"
})
Groupboxes.SubfloorsFools:AddToggle("FigureGodmode", {
	Text = "Figure Godmode",
	Default = false,
	Tooltip = "Prevents Figure from hurting you"
})
Groupboxes.SubfloorsFools:AddDivider()
Groupboxes.SubfloorsFools:AddToggle("RemoveBasementGate", {
	Text = "Remove Basement Gate",
	Default = false,
	Tooltip = "Removes the gate from basement rooms"
})
Groupboxes.SubfloorsFools:AddToggle("RemovePaintingsDoor", {
	Text = "Remove Paintings Door",
	Default = false,
	Tooltip = "Removes the fireplace doors from painting rooms"
})
Groupboxes.SubfloorsFools:AddToggle("RemoveSkeletonDoor", {
	Text = "Remove Skeleton Door",
	Default = false,
	Tooltip = "Removes the skeleton door from the infirmary"
})
Groupboxes.SubfloorsFools:AddToggle("AutoRoomSkip", {
	Text = "Auto Room-Skip",
	Default = false,
	Tooltip = "Automatically teleports to the next room"
})
Groupboxes.SubfloorsFools:AddButton({
	Text = "Skip Current Room",
	Tooltip = "Teleports you to the current room's door",
	Func = function()
    task.spawn(function()
        local CurrentRooms2 = workspace:FindFirstChild("CurrentRooms")

        if not CurrentRooms2 then
            return
        end

        local v1017 = u29 and u29.Value or LocalPlayer:GetAttribute("CurrentRoom")

        if not v1017 then
            return
        end

        local v1018 = CurrentRooms2:FindFirstChild((tostring(v1017)))

        if not v1018 then
            return
        end

        local Door = v1018:FindFirstChild("Door", true)

        if not Door or not Door:IsA("MeshPart") then
            local t5 = {}

            for _, descendant in v1018:GetDescendants() do
                if descendant:IsA("MeshPart") and descendant.Name == "Door" then
                    table.insert(t5, descendant)
                end
            end

            Door = t5[#t5]

            if not Door then
                return
            end
        end

        local Character2 = LocalPlayer.Character

        if Character2 then
            Character2:PivotTo(CFrame.new(Door.Position))
        end
    end)
end
})
Groupboxes.SubfloorsOutdoors = false
Hour = tonumber(os.date("%H"))
if Hour >= 6 and Hour < 12 then
    Greeting = "Good morning"
elseif Hour >= 12 and Hour < 18 then
    Greeting = "Good afternoon"
else
    Greeting = "Good evening"
end
Groupboxes.HomeWelcome = t4.Home:AddLeftGroupbox("Welcome")
Groupboxes.HomeWelcome:AddLabel({
	Text = "<font size=\"28\">" .. Greeting .. ", " .. LocalPlayer.Name .. "</font>",
	DoesWrap = true
})
avatarImage = Groupboxes.HomeWelcome:AddImage("AvatarPreview", {
	Image = "rbxasset://textures/ui/GuiImagePlaceholder.png",
	Height = 100
})
avatarImage:SetVisible(false)
local ok, result = pcall(function()
    return Players:GetUserThumbnailAsync(LocalPlayer.UserId, Enum.ThumbnailType.HeadShot, Enum.ThumbnailSize.Size420x420)
end)
if ok and result then
    avatarImage:SetImage(result)
    avatarImage:SetVisible(true)
else
    local ok8, result8 = pcall(function()
        local v222 = game:HttpGet("https://thumbnails.roblox.com/v1/users/avatar-headshot?userIds=" .. LocalPlayer.UserId .. "&size=420x420&format=Png&isCircular=false")
        local data = game:GetService("HttpService"):JSONDecode(v222)

        if data and (data.data and data.data[1]) then
            return data.data[1].imageUrl
        end

        return nil
    end)

    if ok8 and result8 then
        avatarImage:SetImage(result8)
        avatarImage:SetVisible(true)
    end
end
RootEnv = nil
pcall(function()
    RootEnv = getfenv(0)
end)
if type(RootEnv) ~= "table" then
    RootEnv = _G
end
Executor = {
	isnetworkowner = RootEnv.isnetworkowner,
	firetouchinterest = RootEnv.firetouchinterest,
	replicatesignal = RootEnv.replicatesignal,
	fireproximityprompt = RootEnv.fireproximityprompt,
	hookmetamethod = RootEnv.hookmetamethod,
	newcclosure = RootEnv.newcclosure,
	getnamecallmethod = RootEnv.getnamecallmethod,
	require = RootEnv.require
}
Groupboxes.HomeFunctions = t4.Home:AddLeftGroupbox("Functions & Features")
local function v63(p28)
    if p28 then
        return "âœ…"
    end

    return "âŒ"
end
hasFirePrompt = Executor.fireproximityprompt ~= nil
hasFireTouch = Executor.firetouchinterest ~= nil
hasReplicateSignal = Executor.replicatesignal ~= nil
hasNetworkOwner = Executor.isnetworkowner ~= nil
hasHookMeta = Executor.hookmetamethod ~= nil and (Executor.newcclosure ~= nil and Executor.getnamecallmethod ~= nil)
hasRequire = Executor.require ~= nil
Groupboxes.HomeFunctions:AddLabel({
	Text = "Auto Interact " .. v63(hasFirePrompt) .. " (fireproximityprompt)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Infinite Items " .. v63(hasFirePrompt) .. " (fireproximityprompt)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Infinite Crucifix " .. v63(hasFirePrompt) .. " (fireproximityprompt)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Auto Closet " .. v63(hasFirePrompt) .. " (fireproximityprompt)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Remove Seek Trigger " .. v63(hasFireTouch) .. " (firetouchinterest)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Fake Revive " .. v63(hasReplicateSignal) .. " (replicatesignal)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Remove Figure " .. v63(hasNetworkOwner) .. " (isnetworkowner)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Delete Figure (Mines) " .. v63(hasNetworkOwner) .. " (isnetworkowner)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Win Heartbeat Minigame " .. v63(hasHookMeta) .. " (hookmetamethod)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Bypass Eyes " .. v63(hasHookMeta) .. " (hookmetamethod)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Bypass Lookman " .. v63(hasHookMeta) .. " (hookmetamethod)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Spoof Footsteps " .. v63(hasHookMeta) .. " (hookmetamethod)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Knob Farm " .. v63(hasReplicateSignal) .. " (replicatesignal)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Auto Minecart " .. v63(hasRequire) .. " (require)",
	DoesWrap = true
})
Groupboxes.HomeFunctions:AddLabel({
	Text = "Viewmodel Offset " .. v63(hasRequire) .. " (require)",
	DoesWrap = true
})
Groupboxes.HomeInfo = t4.Home:AddRightGroupbox("Info")
Groupboxes.HomeInfo:AddLabel({
	Text = "<font size=\"18\">Cheese Hub revamped :)<br/>'cheesy' is still actively getting updated, so i'm sorry for any bugs that might occur!<br/>also, cheesy/cheese hub will eventually support more games :D</font>",
	DoesWrap = true
})
Groupboxes.HomeInfo:AddDivider()
Groupboxes.HomeInfo:AddLabel({
	Text = "<font size=\"18\">Join our discord for help, updates, and for chatting!<br/>discord.gg/CuZJQaCquK</font>",
	DoesWrap = true
})
Groupboxes.Misc = t4.Miscellaneous:AddLeftGroupbox("Miscellaneous")
Groupboxes.Misc:AddButton({
	Text = "Play Again",
	Callback = function()
    pcall(function()
        local v1024 = u36 and u36:FindFirstChild("PlayAgain")

        if v1024 then
            v1024:FireServer()
        end
    end)
end
})
Groupboxes.Misc:AddButton({
	Text = "Return to Lobby",
	Callback = function()
    pcall(function()
        local v1025 = u36 and u36:FindFirstChild("Lobby")

        if v1025 then
            v1025:FireServer()
        end
    end)
end
})
Groupboxes.Misc:AddButton({
	Text = "Reset Character",
	Callback = function()
    pcall(function()
        if Humanoid then
            Humanoid.Health = 0
        end

        local v1026 = u36 and u36:FindFirstChild("Underwater")

        if v1026 then
            v1026:FireServer(true)
        end
    end)
end
})
Groupboxes.Misc:AddButton({
	Text = "Give Star Jug",
	Tooltip = "Gives a star jug",
	Callback = function()
    task.spawn(function()
        local v1027 = Character or LocalPlayer.CharacterAdded:Wait()
        local v1028 = Humanoid or v1027:WaitForChild("Humanoid")
        local v1029 = game:GetObjects("rbxassetid://119885581324516")[1]

        if not v1029 then
            return
        end

        local n5 = 4
        local u1031 = false

        v1029:SetAttribute("Durability", n5)

        local NumberValue = Instance.new("NumberValue", v1029)

        v1029.Parent = LocalPlayer.Backpack

        local t6 = {}

        for _, child in v1029:WaitForChild("Animations"):GetChildren() do
            t6[child.Name] = v1028:LoadAnimation(child)

            if child.Name == "idle" then
                t6[child.Name].Priority = Enum.AnimationPriority.Idle
                t6[child.Name].Looped = true
            end
        end

        v1029.Equipped:Connect(function()
            t6.equip:Play()
            task.wait(t6.equip.Length)

            if v1029:IsDescendantOf(v1027) then
                t6.idle:Play()
            end
        end)
        v1029.Unequipped:Connect(function()
            if t6.idle.IsPlaying then
                t6.idle:Stop()
            end
        end)
        v1029.Activated:Connect(function()
            if u1031 then
                return
            end

            u1031 = true
            t6.open:Play()

            if n5 - 1 ~= 0 then
                n5 -= 1
                v1029:SetAttribute("Durability", n5)
            else
                v1029:Destroy()
            end

            local v1336 = Character

            if not v1336 or not v1336.Parent then
                u1031 = false

                return
            end

            v1336:SetAttribute("Starlight", true)
            v1336:SetAttribute("StarlightHuge", true)

            local u1337 = false
            local v1338 = v1336:FindFirstChild("CollisionPart") or v1336:FindFirstChild("Collision")
            local v1339 = u36 and u36:FindFirstChild("Crouch")

            if v1338 and not v1336:FindFirstChild("CollisionClone") then
                local clone = v1338:Clone()

                clone.CanCollide = false
                clone.Massless = true
                clone.RootPriority = 127
                clone.Anchored = false
                clone.Name = "CollisionClone"

                local CollisionCrouch = clone:FindFirstChild("CollisionCrouch")

                if CollisionCrouch then
                    CollisionCrouch:Destroy()
                end

                clone.Parent = v1336
                task.spawn(function()
                    while not u1337 do
                        clone.Massless = not clone.Massless

                        if v1339 then
                            v1339:FireServer(true, true)
                        end

                        task.wait(0.21)
                    end

                    clone.Massless = true
                    clone:Destroy()
                end)
            end

            NumberValue.Value = 35
            TweenService:Create(NumberValue, TweenInfo.new(70, Enum.EasingStyle.Linear), {
					Value = 0
				}):Play()

            local connection = NumberValue:GetPropertyChangedSignal("Value"):Connect(function()
                if v1336 and v1336.Parent then
                    v1336:SetAttribute("SpeedBoost", NumberValue.Value)
                end
            end)

            task.wait(35)
            u1337 = true
            connection:Disconnect()
            NumberValue:Destroy()

            if v1336 and v1336.Parent then
                v1336:SetAttribute("Starlight", false)
                v1336:SetAttribute("StarlightHuge", false)
                v1336:SetAttribute("SpeedBoost", 0)
            end

            u1031 = false
        end)
    end)
end
})
Groupboxes.Misc:AddDivider()
Groupboxes.Misc:AddButton({
	Text = "Copy/Open Discord Invite",
	Tooltip = "Copies the Discord invite to your clipboard and opens it in Discord",
	Callback = function()
    pcall(function()
        setclipboard(DISCORD_INVITE)
    end)
    pcall(function()
        _G.Clipboard.set(DISCORD_INVITE)
    end)
    task.spawn(function()
        local v1036 = syn and syn.request or (request or http_request)

        if v1036 then
            pcall(function()
                local HttpService2 = game:GetService("HttpService")

                v1036({
						Url = "http://127.0.0.1:6463/rpc?v=1",
						Method = "POST",
						Headers = {
							["Content-Type"] = "application/json",
							Origin = "https://discord.com"
						},
						Body = HttpService2:JSONEncode({
							cmd = "INVITE_BROWSER",
							nonce = HttpService2:GenerateGUID(false),
							args = {
								code = "CuZJQaCquK"
							}
						})
					})
            end)
        end

        pcall(function()
            local v1344 = getgenv()
            local v1345 = v1344.syn and v1344.syn.openurl or (v1344.openurl or v1344.open_url)

            if v1345 then
                v1345(DISCORD_INVITE)
            end
        end)
    end)
end
})
Groupboxes.Misc:AddToggle("ShowKeybinds", {
	Text = "Show Keybinds",
	Default = false,
	Tooltip = "Shows all registered keybinds"
})
Toggles2.ShowKeybinds:OnChanged(function(p29)
    u2.KeybindFrame.Visible = p29
end)
Groupboxes.Misc:AddToggle("DiscordRichPresence", {
	Text = "Discord Rich Presence",
	Default = false,
	Tooltip = "Shows your current room in your Discord status via Bloxstrap RPC"
})
BLOXSTRAP_CHEESE_ICON = 106990021902590
Discord = {
	PresenceConn = nil,
	PresenceActive = false,
	LastUpdate = 0
}
local function v64(p30, p31)
    local json = game:GetService("HttpService"):JSONEncode({
		command = p30,
		data = p31
	})

    pcall(function()
        game:GetService("HttpService"):PostAsync("http://127.0.0.1:43594/rpc", json, Enum.HttpContentType.ApplicationJson)
    end)
end
local function v65()
    local s1 = "Unknown"

    pcall(function()
        GameData = ReplicatedStorage:FindFirstChild("GameData")

        local v1037 = GameData and GameData:FindFirstChild("LatestRoom")

        if v1037 then
            s1 = tostring(v1037.Value)

            return
        end

        if LocalPlayer and LocalPlayer:GetAttribute("CurrentRoom") then
            s1 = tostring(LocalPlayer:GetAttribute("CurrentRoom"))
        end
    end)

    return "Room " .. s1
end
local function v66()
    if not Toggles2.DiscordRichPresence.Value then
        return
    end

    v64("SetRichPresence", {
		details = "Using cheesy",
		state = v65(),
		largeImage = {
			assetId = BLOXSTRAP_CHEESE_ICON,
			hoverText = "cheesy"
		},
		smallImage = {
			assetId = BLOXSTRAP_CHEESE_ICON,
			hoverText = "discord.gg/CuZJQaCquK"
		}
	})
end
Toggles2.DiscordRichPresence:OnChanged(function(p32)
    if Discord.PresenceConn then
        Discord.PresenceConn:Disconnect()
        Discord.PresenceConn = nil
    end

    if p32 then
        Discord.PresenceActive = true
        Discord.LastUpdate = 0
        v66()
        task.spawn(function()
            while Discord.PresenceActive do
                task.wait(15)

                if not Discord.PresenceActive then
                    return
                end

                Discord.LastUpdate = os.clock()
                v66()
            end
        end)

        return
    end

    Discord.PresenceActive = false

    if Discord.PresenceConn then
        Discord.PresenceConn:Disconnect()
        Discord.PresenceConn = nil
    end

    v64("SetRichPresence", {
		details = "<reset>",
		state = "<reset>",
		largeImage = {
			reset = true
		},
		smallImage = {
			reset = true
		}
	})
end)
Groupboxes.Misc:AddToggle("AntiLag", {
	Text = "Anti-Lag",
	Default = false,
	Tooltip = "Reduces lag by setting all parts in the current rooms to Plastic material"
})
Toggles2.AntiLag:OnChanged(function(p33)
    local CurrentRooms3 = workspace.CurrentRooms

    if not CurrentRooms3 then
        return
    end

    if p33 then
        for _, descendant in CurrentRooms3:GetDescendants() do
            if descendant:IsA("BasePart") then
                descendant:SetAttribute("Mat", descendant.Material)
                descendant.Material = "Plastic"
            end
        end

        return
    end

    for _, descendant in CurrentRooms3:GetDescendants() do
        if descendant:IsA("BasePart") and descendant:GetAttribute("Mat") then
            descendant.Material = descendant:GetAttribute("Mat") or "Plastic"
        end
    end
end)
Groupboxes.Misc:AddDivider()
Groupboxes.Misc:AddToggle("DisableIdleKick", {
	Text = "Disable Idle Kick",
	Default = false,
	Tooltip = "Disables the kick from being afk for 20 minutes"
})
DisableIdleKickConn = nil
Toggles2.DisableIdleKick:OnChanged(function(p34)
    if DisableIdleKickConn then
        DisableIdleKickConn:Disconnect()
        DisableIdleKickConn = nil
    end

    if p34 then
        DisableIdleKickConn = LocalPlayer.Idled:Connect(function()
            VirtualUser:CaptureController()
            VirtualUser:ClickButton2(Vector2.new())
        end)
    end
end)
Groupboxes.Misc:AddDivider()
Groupboxes.Misc:AddToggle("EnableKnobFarm", {
	Text = "Enable Knob Farm",
	Default = false,
	Tooltip = "Enables the knob farm feature."
})
Toggles2.EnableKnobFarm:OnChanged(function(p35)
    if p35 and not t12.KnobFarmStarted then
        u2:Notify({
			Title = "Knob Farm",
			Description = "Click start knob farm to start grinding",
			Time = 4
		})
    end
end)
Groupboxes.Misc:AddButton({
	Text = "Start Knob Farm",
	Tooltip = "Starts farming knobs based on your gold.",
	Callback = function()
    if not Toggles2.EnableKnobFarm.Value then
        u2:Notify({
				Title = "Knob Farm",
				Description = "Enable the knob farm toggle first!",
				Time = 4
			})

        return
    end

    if CurrentFloor ~= "Mines" then
        u2:Notify({
				Title = "Knob Farm",
				Description = "Use in the mines floor for best results!",
				Time = 4
			})
    end

    local TopbarUI = LocalPlayer.PlayerGui:FindFirstChild("TopbarUI")

    if TopbarUI then
        local GoldVal = TopbarUI.Topbar.StatsTopbarHandler.StatModules.Gold.GoldVal

        if GoldVal and GoldVal.Value <= 0 then
            u2:Notify({
					Title = "Knob Farm",
					Description = "You need gold to start the knob farm :)",
					Time = 4
				})

            return
        end
    end

    t12.KnobFarmStarted = true

    if not KnobFarmConn then
        KnobFarmConn = RunService.Heartbeat:Connect(function()
            if _Unloading then
                return
            end

            if not t12.KnobFarmStarted or t12.KnobFarmActive then
                return
            end

            t12.KnobFarmActive = true

            if Executor.replicatesignal then
                Executor.replicatesignal(LocalPlayer.kill)
            elseif u36 and u36:FindFirstChild("Underwater") then
                u36.Underwater:FireServer(true)
            end

            while task.wait() and not LocalPlayer:GetAttribute("Alive") do
            end

            local v1038 = u36 and u36:FindFirstChild("Statistics")

            if v1038 then
                v1038:FireServer()
            end

            task.wait(0.25)
            t12.KnobFarmActive = false
        end)
    end
end
})
Groupboxes.Misc:AddLabel({
	Text = "This gives you knobs based on however much gold you have, however, you have to start this in the first room as otherwise it will not work. Use in the mines floor for best results. Use modifiers for best results, atleast 150%.",
	DoesWrap = true
})
Groupboxes.Misc:AddDivider()
Groupboxes.Misc:AddToggle("DisplayInfo", {
	Text = "Display Info",
	Default = false,
	Tooltip = "Shows an overlay with FPS, ping, executor, and username"
})
DisplayInfoFPS = 60
DisplayInfoFC = 0
DisplayInfoFT = tick()
Toggles2.DisplayInfo:OnChanged(function(p36)
    if DisplayInfoLabel then
        DisplayInfoLabel:Destroy()
        DisplayInfoLabel = nil
    end

    if DisplayInfoConn then
        DisplayInfoConn:Disconnect()
        DisplayInfoConn = nil
    end

    if p36 then
        DisplayInfoFC = 0
        DisplayInfoFT = tick()
        DisplayInfoLabel = u2:AddDraggableLabel("cheesy | loading...")

        if t12.UILibrary == "Obsidian" then
            local v242 = DisplayInfoLabel.Label or DisplayInfoLabel

            pcall(function()
                v242.TextColor3 = Color3.fromHex("ebebeb")
            end)
        end

        DisplayInfoConn = RunService.RenderStepped:Connect(function()
            if _Unloading then
                return
            end

            DisplayInfoFC += 1

            local timestamp = tick()

            if timestamp - DisplayInfoFT >= 1 then
                DisplayInfoFPS = DisplayInfoFC
                DisplayInfoFT = timestamp
                DisplayInfoFC = 0
            end

            local v1040 = pcall(function()
                return math.floor(game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue())
            end) and math.floor(game:GetService("Stats").Network.ServerStatsItem["Data Ping"]:GetValue()) or 0
            local v1041 = pcall(identifyexecutor) and identifyexecutor() or "Unknown"

            DisplayInfoLabel:SetText(("cheesy | FPS: %d | Ping: %dms | Executor: %s | User: %s"):format(math.floor(DisplayInfoFPS), v1040, v1041, LocalPlayer.Name))
        end)
    end
end)
Groupboxes.MiscFun = t4.Miscellaneous:AddRightGroupbox("Fun")
Groupboxes.MiscFun:AddToggle("Uhhhh", {
	Text = "uhhhh",
	Default = false,
	Tooltip = "content (this makes a lot of lag btw)"
})
UhhhhDecals = {}
UhhhhConn = nil
Toggles2.Uhhhh:OnChanged(function(p37)
    if p37 then
        for _, descendant in pairs(workspace:GetDescendants()) do
            if descendant:IsA("BasePart") then
                local Model = descendant:FindFirstAncestorWhichIsA("Model")

                if not Model or not Model:FindFirstChild("Humanoid") then
                    for _, v in ipairs(Enum.NormalId:GetEnumItems()) do
                        local Decal = Instance.new("Decal")

                        Decal.Texture = "http://www.roblox.com/asset/?id=135666356081915"
                        Decal.Face = v
                        Decal.Parent = descendant
                        table.insert(UhhhhDecals, Decal)
                    end
                end
            end
        end

        UhhhhConn = workspace.DescendantAdded:Connect(function(descendant)
            if descendant:IsA("BasePart") then
                local Model = descendant:FindFirstAncestorWhichIsA("Model")

                if not Model or not Model:WaitForChild("Humanoid") then
                    for _, v in ipairs(Enum.NormalId:GetEnumItems()) do
                        local Decal = Instance.new("Decal")

                        Decal.Texture = "http://www.roblox.com/asset/?id=135666356081915"
                        Decal.Face = v
                        Decal.Parent = descendant
                        table.insert(UhhhhDecals, Decal)
                    end
                end
            end
        end)

        return
    end

    if UhhhhConn then
        UhhhhConn:Disconnect()
        UhhhhConn = nil
    end

    for _, v in ipairs(UhhhhDecals) do
        if v and v.Parent then
            v:Destroy()
        end
    end

    TableClear(UhhhhDecals)
end)
Groupboxes.MiscFun:AddButton({
	Text = "uhhhh v2",
	Tooltip = "content",
	Callback = function()
    local ScreenGui = Instance.new("ScreenGui")

    ScreenGui.Name = "content"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.IgnoreGuiInset = true
    ScreenGui.Parent = CoreGui

    local Frame = Instance.new("Frame")

    Frame.Size = UDim2.new(1, 0, 1, 0)
    Frame.BackgroundColor3 = Color3.new(0, 0, 0)
    Frame.BackgroundTransparency = 1
    Frame.Parent = ScreenGui

    local TextLabel = Instance.new("TextLabel")

    TextLabel.Size = UDim2.new(1, -40, 1, -40)
    TextLabel.Position = UDim2.new(0, 20, 0, 20)
    TextLabel.BackgroundTransparency = 1
    TextLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
    TextLabel.TextTransparency = 1
    TextLabel.TextScaled = true
    TextLabel.Font = Enum.Font.Code
    TextLabel.TextXAlignment = Enum.TextXAlignment.Center
    TextLabel.TextYAlignment = Enum.TextYAlignment.Center
    TextLabel.Text = [[.............                                            
                                           ................                                     ..................                                     
                                        .......................                               ......................                                   
                                     .............................                          ..........................                                 
                                   .................................                      .............................                                
                                  .....................      .......                     ................................                              
                                 ............                     ..                    :..                   ............                             
                                ..........                                                                       ..........                            
                               ........                                                                            .........                           
                              .......                                                                               .........                          
                             .......                                                                                  .......                          
                            .......                     +%##%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@@                         ........                         
                           ......                +*#%%#%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@@                    ......                         
                           ....             *###%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@                 .....                        
                          ....          *##%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@@            :.....                       
                         ....        ##%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@@          -.....                      
                        ....:     #%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@        -....                      
                        -.-    %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@@     =:..:                     
                            #%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@@                             
                          #%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@@                         
                        %%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%@@@                       
                      ###%%%%%####%%%%%%%%%%%%#####%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%#%%%%@@@@@@@@@%%%#######%%%%%%@@                     
                    %#%%####%@@@@@@@@@@@@@@@@@@@@@@@%###%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%#%%@@@@@@@@@@@@@@@@@@@@@@@%#####%%%@%%%                   
                  #######%@@@@@@@@@@@@@@@@@@@@@@@@@@@@%#*#%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%##%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%###%%%%%%%                 
                 %%####%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%*#%%%%%%%%%%%%%%%%%%%%%%%%%%%%#%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%###%%%%%%                
               #%%**#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%*#%%%%%%%%%%%%%%%%%%%%%%%%##@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%###%%%%%%              
              %%#**%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#*#%%%%%%%%%%%%%%%%%%%%##%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@###%%%%%%             
            ##%#*#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#*#%%%%%%%%%%%%%%%%%%##@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@###%%%%%%            
           *#%#*#@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%*#%%%%%%%%%%%%%%%%#%@@@@@@@@@@@@@@@%#*+=-----+*#@@@@@@@@@@@@@@@@@@@#*#%%%%%%%          
          *#%#*#@@@@@@@@@@@@@@@@@@@@@@@@@%#+=:.....:=*%@@@@@@@@@%+#%%%%%%%%%%%%%%#%@@@@@@@@@@@@@#=...............=%@@@@@@@@@@@@@@@@@#*#%%%%%%%         
         *###*#@@@@@@@@@@@@@@@@@@@@@@@%=...............:+@@@@@@@@%+#%%%%%%%%%%%%#%@@@@@@@@@@@%=...............:---:-%@@@@@@@@@@@@@@@@*##%%%%%%%        
        *####*@@@@@@@@@@@@@@@@@@@@@@%=...    .........---:*@@@@@@@*+%%%%%%%%%%%##@@@@@@@@@@@+................:==-=-::*@@@@@@@@@@@@@@@%*#%%%%%%%        
       *####*%@@@@@@@@@@@@@@@@@@@@@*...   ..........:===--:-%@@@@@@+#%%%%%%%%%%#%@@@@@@@@@%-.................-==----:.#@@@@@@@@@@@@@@@+##%%%%%%%       
      *#####*%@@@@@@@@@@@@@@@@@@@@=.................===----:-%@@@@@**%%%%%%%%%##@@@@@@@@@%-..................-==----:.:@@@@@@@@@@@@@@@*###%%%%%%%      
     **####*#@@@@@@@@@@@@@@@@@@@@=..................-==----:.+@@@@@#+%%%%%%%%%#%@@@@@@@@@*....................:-=-:::.:@@@@@@@@@@@@@@@#*##%%%%%%%%     
    **#####*#@@@@@@@@@@@@@@@@@@@#....................==----:.-%@@@@%+#%%%%%%%%#%@@@@@@@@@+............................:@@@@@@@@@@@@@@@#*###%%%%%%#     
    *######*#@@@@@@@@@@@@@@@@@@@+.....................:--::..-%@@@@%+#%%%%%%%##%@@@@@@@@@*............................-@@@@@@@@@@@@@@@#*###%%%%%%##    
  ***######*#@@@@@@@@@@@@@@@@@@@=............................=@@@@@%+##%%%%%%#*%@@@@@@@@@#:...........................%@@@@@@@@@@@@@@@**###%%%%%##%    
  *+#######**@@@@@@@@@@@@@@@@@@@*............................*@@@@@#+##%%%%%%#*#@@@@@@@@@@+.........................:%@@@@@@@@@@@@@@@@*###%%%%%%%%##   
# +*#######**%@@@@@@@@@@@@@@@@@@%:..........................=%@@@@@+*##%%%%%%%##@@@@@@@@@@%+.......................+@@@@@@@@@@@@@@@@@%*####%%%%%%%%#   
##+*#######**#@@@@@@@%%%%%@@@@@@@*.........................-%@@@@@%=###%%%%%%%#*%@@@@@@@@@@%#-..................:+%@@@@@@@@@@@@@@@@@@+*####%%%%%#####  
#**#########*+%@@@@@%%%%%%%%@@@@@@*.......................+@@@@@@@++####%%%%%%#**@@@@@@@@@@%%%#+:............:=#@@@@@@@@@@@@@@@@@@@@*+#####%%%%######  
***#########**+%@@@@@%%%%%%%%%@@@@@%=...................=%@@@@@@@*+####%%%%%%%##*#@@@@@@@@@%%%%@@%#*+++++*#%@@@@@@@@@@@@@@@@@@@@@@@#+#####%%%%%%#####  
***##########*++@@@@@@%%%%%%%%%%@@@@@%+:.............-+%@@@@@@@@#+*###%%%#%%%%%##+#@@@@@@@@@%%%%%%%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#+*#######%%%######  
***##########**++%@@@@@@%%%%%%%%%%%@@@@@%#=-::::-=*%@@@@@@@@@@@*+*####%#%%%%%%%###**@@@@@@@@@@%%%%%%%%%%%%%@@@@@@@@@@@@@@@@@@@@@@*+*#########%%######  
#**##########***+=%@@@@@@@%%%%%%%%%%%@@@@@@@@@@@@@@@@@@@@@@@@@*+*#######%%%#%######*+%@@@@@@@@@@%%%%%%%%%%%%@@@@@@@@@@@@@@@@@@@#+*###########%######## 
#**###########****+*@@@@@@@@@%%%%%%%%%@@@@@@@@@@@@@@@@@@@@@@#++*#####################+*@@@@@@@@@@@@@@%%%%%@@@@@@@@@@@@@@@@@@@#++*##################### 
#**###########*****++*@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@%++*########################*+*%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#++*####################### 
#**###########*******++*%@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@@#==*############################*++#@@@@@@@@@@@@@@@@@@@@@@@@@@@@%+=+**#######################  
#**######*#************+++*%@@@@@@@@@@@@@@@@@@@@@@@@@#+=+*################################**++*#%@@@@@@@@@@@@@@@@@@@%*==+**##########################  
#***#####*****************++++#%@@@@@@@@@@@@@@@@@#*==+**######################################**++++**#########**++++***#############################  
##**#####*********************+++++++++++++++++==++**##############################################*******++++******##=::...::+######################  
##**#####*****************************************##################################################################+..........=#####################  
##**#####************************************#######################################################################+-=*+......:*###################   
###**#####*****************************###############################################################################*-...:*#=:+###################   
####**####******************************#############################################################################=....-*#######################    
#####*####********************************########################################################################*=....-*#########################    
 ####**####***************************************#*############################################################+:....=*##########################     
  ####*#####*******************************************######################################################*-....:+############################      
  #####*#####***************************************************#########################################*+-....:+###############################      
   ###########***************+::-+***********************************#################################*=.....-*##############################*##       
    #####*#####****************+:...:=+***********************************#*####################**=:.....:=*###################################        
     ####**#####******************+-....:-=*********************************#########*#####**=:......:+***##################################*#         
      ####**#####********************+=-:.....:-=+************************************+=-:......:-+**#####################################***          
      ######*######*********************+++=:........::--==++***************++==-::........:-=+*****######################################**           
       ######*#######************************++==-:..................................:-=++**********#####################################**            
         #####*#######******************************++==----:::::::::::::::---===++**********************############################***#              
          ######*########**********************************************************************************##########################*##               
           ###############*************************************************************************************#################*###*#*                
            #######*########**********************************************************************************################***#***                  
              #######*#########********************************************************************************#############**####**                   
                ######**##########****************************************************************************###########*******#*                     
                  ######**##########*************************************************************************##########*********                       
                   #########*#########**********************************************************************########********##                         
                      ########*#########*****************************************************************########***********                           
                        ##########*#########*********************************************************#########************                             
                          ######################*************************************************########***************                               
                             ##########################*************************************#########****************                                  
                               ##################################******************#############******************                                     
                                   *######################################################*********************                                        
                                      ***####***###*******#############*##########*************************                                            
                                          *####*##*##*#************************************************]]
    TextLabel.Parent = Frame
    TweenService:Create(Frame, TweenInfo.new(5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			BackgroundTransparency = 0.1
		}):Play()
    TweenService:Create(TextLabel, TweenInfo.new(5, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			TextTransparency = 0
		}):Play()
    u2:Toggle(false)
    task.wait(6)
    TweenService:Create(Frame, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
			BackgroundTransparency = 1
		}):Play()
    TweenService:Create(TextLabel, TweenInfo.new(1, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
			TextTransparency = 1
		}):Play()
    task.wait(1)
    ScreenGui:Destroy()
end
})
Groupboxes.MiscFun:AddButton({
	Text = "Death Farm",
	Tooltip = "Execute this in the hotel at a start of run, either use it from here or execute the actual script",
	Callback = function()
    loadstring(game:HttpGet("https://raw.githubusercontent.com/doram44/cheesy/refs/heads/main/stuff/deathfarm.lua"))()
end
})
Functions = {
	FirePrompt = nil,
	GetLibraryCode = nil,
	HasItem = nil,
	HandleHidingTransparency = nil
}
Connections = {}
if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
    if not LocalPlayer.Character:FindFirstChild("Collision") then
    end
end
Groupboxes.Character = t4.Main:AddLeftGroupbox("Character")
Groupboxes.Other = t4.Main:AddRightGroupbox("Other")
Groupboxes.SpamBuy = t4.Exploits:AddLeftGroupbox("Pre-Run")
Groupboxes.SpamBuy:AddButton({
	Text = "Buy Items",
	DisabledTooltip = "This feature doesn't work in this floor",
	Callback = function()
    task.spawn(function()
        local PreRunShop = game:GetService("ReplicatedStorage").RemotesFolder:FindFirstChild("PreRunShop")

        if not PreRunShop then
            return
        end

        local SpamBuyItemsValue = Options2.SpamBuyItems.Value
        local t7 = {}

        if SpamBuyItemsValue then
            for k, _ in pairs(SpamBuyItemsValue) do
                table.insert(t7, k)
            end
        end

        if #t7 > 0 then
            local t8 = {}

            for _ = 1, Options2.SpamBuyCount.Value do
                for _, v in ipairs(t7) do
                    table.insert(t8, v)
                end
            end

            PreRunShop:FireServer(t8, false)
        end
    end)
end
})
Groupboxes.SpamBuy:AddSlider("SpamBuyCount", {
	Text = "Purchase Count",
	Min = 1,
	Max = 6,
	Default = 3,
	Rounding = 0,
	Compact = true,
	DisabledTooltip = "This feature doesn't work in this floor"
})
Groupboxes.SpamBuy:AddDropdown("SpamBuyItems", {
	Text = "Items to Buy",
	Values = {
		"Lockpick",
		"Lighter",
		"Flashlight",
		"Vitamins",
		"Crucifix",
		"Skeleton Key"
	},
	Multi = true,
	AllowNull = true,
	DisabledTooltip = "This feature doesn't work in this floor"
})
Groupboxes.SpamBuy:AddToggle("ShowCrucifix", {
	Text = "Show Crucifix",
	Default = false,
	Tooltip = "Shows/hides the crucifix in the item shop",
	DisabledTooltip = "This feature doesn't work in this floor"
})
Toggles2.ShowCrucifix:OnChanged(function(p38)
    if v39() ~= "OldHotel" then
        return
    end

    local v256 = LocalPlayer and (LocalPlayer.PlayerGui and (LocalPlayer.PlayerGui:FindFirstChild("MainUI") and (LocalPlayer.PlayerGui.MainUI:FindFirstChild("ItemShop") and (LocalPlayer.PlayerGui.MainUI.ItemShop:FindFirstChild("Items") and LocalPlayer.PlayerGui.MainUI.ItemShop.Items:FindFirstChild("ItemShop_Crucifix")))))

    if v256 then
        v256.Visible = p38
    end
end)
Groupboxes.SpamBuy:AddToggle("ShowSkeletonKey", {
	Text = "Show Skeleton Key",
	Default = false,
	Tooltip = "Shows/hides the skeleton key in the item shop",
	DisabledTooltip = "This feature doesn't work in this floor"
})
Toggles2.ShowSkeletonKey:OnChanged(function(p39)
    if v39() ~= "OldHotel" then
        return
    end

    local v258 = LocalPlayer and (LocalPlayer.PlayerGui and (LocalPlayer.PlayerGui:FindFirstChild("MainUI") and (LocalPlayer.PlayerGui.MainUI:FindFirstChild("ItemShop") and (LocalPlayer.PlayerGui.MainUI.ItemShop:FindFirstChild("Items") and LocalPlayer.PlayerGui.MainUI.ItemShop.Items:FindFirstChild("ItemShop_Skeleton Key")))))

    if v258 then
        v258.Visible = p39
    end
end)
Groupboxes.Prompts = t4.Exploits:AddLeftGroupbox("Prompts")
Groupboxes.Prompts:AddToggle("InstantInteract", {
	Text = "Instant Interact",
	Default = false,
	Tooltip = "Removes the wait time for interactions e.g. locks"
})
Groupboxes.Prompts:AddToggle("PromptClip", {
	Text = "Prompt Clip",
	Default = false,
	Tooltip = "Allows you to interact through walls"
})
Groupboxes.Prompts:AddToggle("PromptReach", {
	Text = "Prompt Reach",
	Default = false,
	Tooltip = "Increases the range of all proximity prompts"
})
Groupboxes.Prompts:AddSlider("PromptRange", {
	Text = "Prompt Range",
	Min = 1,
	Max = 2,
	Default = 1,
	Rounding = 1,
	Compact = true
})
_TrackedPrompts = {}
Options2.PromptRange:OnChanged(function(p40)
    if Toggles2.PromptReach.Value then
        for _, v in ipairs(_TrackedPrompts) do
            v.MaxActivationDistance = v:GetAttribute("MaxActivationDistance_Old") * p40
        end
    end
end)
Groupboxes.Exploits = t4.Exploits:AddLeftGroupbox("Exploits")
Groupboxes.Exploits:AddToggle("InfiniteItemsToggle", {
	Text = "Infinite Items",
	Default = false,
	Tooltip = "Allows you to use lockpicks, shears, skeleton keys, and multitools without draining the usage",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Connections.InfiniteItemsHandler = ProximityPromptService.PromptTriggered:Connect(function(p41)
    if not p41:GetAttribute("FakePrompt") then
        return
    end
    local v
    for _, v2 in ipairs({
		"Lockpick",
		"Shears",
		"SkeletonKey",
		"Key",
		"GeneratorFuse",
		"KeyElectrical",
		"KeyBackdoor",
		"KeyIron",
		"Multitool"
	}) do
        v = Character:FindFirstChild(v2)

        if v then
            break
        end
    end
    local v266 = ({
		UnlockPrompt = true,
		SkullPrompt = true,
		LockPrompt = true,
		ThingToEnable = true,
		FusesPrompt = true
	})[p41.Name] or (p41.Parent and p41.Parent:GetAttribute("Locked") == true or p41.Parent and (p41.Parent.Parent and (p41.Parent.Parent.Name == "Locker_Small_Locked" and p41.Name == "ActivateEventPrompt")))
    if v266 then
        if Options2.AutoInteractIgnoreList.Value.Locks then
            return
        end

        local v267 = false

        for _, v3 in ipairs({
			"Key",
			"GeneratorFuse",
			"KeyBackdoor",
			"KeyElectrical",
			"KeyIron",
			"Lockpick",
			"SkeletonKey",
			"Shears",
			"Multitool"
		}) do
            if Functions.HasItem(v3, true) then
                v267 = true

                break
            end
        end

        for _, v4 in ipairs({
			"Key",
			"GeneratorFuse",
			"KeyElectrical",
			"KeyIron"
		}) do
            if Functions.HasItem(v4) then
                v267 = true

                break
            end
        end

        if not v267 then
            return
        end
    end
    if p41.Parent.Name == "CuttableVines" and not Functions.HasItem("Shears", true) and not Functions.HasItem("Multitool", true) or p41.Parent.Name == "Chest_Vine" and not Functions.HasItem("Shears", true) and not Functions.HasItem("Multitool", true) or p41.Parent.Name == "Cellar" and not Functions.HasItem("Shears", true) and not Functions.HasItem("Multitool", true) then
        return
    end
    if p41.Parent.Name == "SkullLock" and not Functions.HasItem("SkeletonKey", true) then
        return
    end
    if p41.Parent.Name == "Lock1" and not Functions.HasItem("Lockpick", true) and not Functions.HasItem("Multitool", true) or p41.Parent.Name == "Lock2" and not Functions.HasItem("Lockpick", true) and not Functions.HasItem("Multitool", true) then
        return
    end
    if Functions.HasItem("Shears", true) and v266 and p41.Parent.Name ~= "CuttableVines" and p41.Parent.Name ~= "Chest_Vine" and p41.Parent.Name ~= "Cellar" then
        return
    end
    if v and TableFind({
		"Lockpick",
		"Shears",
		"SkeletonKey",
		"Key",
		"KeyElectrical",
		"KeyBackdoor",
		"KeyIron",
		"Multitool"
	}, v.Name) and t12.UseAnimation and t12.UseAnimationBreak then
        t12.UseAnimation:Stop()
        t12.UseAnimationBreak:Stop()
        t12.UseAnimationBreak:Play()

        if v.Name == "Shears" then
            v:WaitForChild("Handle"):WaitForChild("sound_prompt"):Play()
        end
    end
    local Tool = Character:FindFirstChildOfClass("Tool")
    local v273 = Tool and u49[Tool.Name]
    if Tool and v273 and Toggles2.InfiniteItemsToggle.Value and ({
		Lockpicks = true,
		["Skeleton Key"] = true,
		Shears = true,
		Multitool = true
	})[v273] then
        workspace:FindFirstChild("Drops").ChildAdded:Once(function(child)
            local ModulePrompt = child:FindFirstChild("ModulePrompt")
            local v1058 = FakePrompts[p41]

            Functions.FirePrompt(ModulePrompt)
            Functions.FirePrompt(v1058)
        end)
        Character.ChildAdded:Once(function(child)
            if child.Name == "Shears" then
                child:WaitForChild("Handle"):WaitForChild("sound_promptend"):Play()
            end
        end)
        u36.DropItem:FireServer(Tool)

        return
    end
    local v274 = FakePrompts[p41]
    Functions.FirePrompt(v274)
end)
Groupboxes.Exploits:AddToggle("InfiniteCrucifix", {
	Text = "Infinite Crucifix",
	Default = false,
	Tooltip = "Doesn't use your crucifix on rush-like entities",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Groupboxes.Exploits:AddToggle("AutoHeartbeatMinigame", {
	Text = "Win Heartbeat Minigame",
	Default = false,
	Tooltip = "Automatically passes the heartbeat minigame when it starts",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Groupboxes.Exploits:AddButton({
	Text = "Fake Revive",
	Tooltip = "This doesnt always work, and interacting with a bandage will disable it, and will not be able to enable it back. Sometimes you will not be able to fully finish the run, but you can still go through doors/finish",
	DisabledTooltip = "Your executor doesn't support this feature :(",
	Callback = function()
    task.spawn(function()
        local Gravity = workspace.Gravity
        local connection
        local function v1062()
            local v1346 = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") or LocalPlayer.Character:WaitForChild("Humanoid")

            if connection then
                connection:Disconnect()
            end

            connection = v1346.Died:Connect(function()
                task.delay(0.5, function()
                    workspace.Gravity = 0

                    local Character3 = LocalPlayer.Character

                    if not Character3 then
                        return
                    end

                    local Humanoid2 = Character3:FindFirstChild("Humanoid")
                    local HumanoidRootPart2 = Character3:FindFirstChild("HumanoidRootPart")

                    if not Humanoid2 or not HumanoidRootPart2 then
                        return
                    end

                    local PlayerGui3 = LocalPlayer:FindFirstChild("PlayerGui")

                    if PlayerGui3 then
                        local MainUI = PlayerGui3:FindFirstChild("MainUI")

                        if MainUI then
                            local DeathPanel = MainUI:FindFirstChild("DeathPanel")

                            if DeathPanel then
                                DeathPanel:Destroy()
                            end

                            local Death = MainUI:FindFirstChild("Death")

                            if Death then
                                Death:Destroy()
                            end
                        end
                    end

                    Humanoid2.Health = Humanoid2.MaxHealth
                    Humanoid2.AutomaticScalingEnabled = true

                    if Character3:GetAttribute("Stunned") then
                        Character3:SetAttribute("Stunned", false)
                    end

                    task.delay(1, function()
                        if connection then
                            workspace.Gravity = Gravity
                        end
                    end)
                end)
            end)
        end
        local v1063 = LocalPlayer:GetAttribute("CurrentRoom") or ReplicatedStorage:FindFirstChild("GameData") and (ReplicatedStorage.GameData:FindFirstChild("LatestRoom") and ReplicatedStorage.GameData.LatestRoom.Value)
        if v1063 and v1063 >= 1 then
            v1062()
            LocalPlayer.CharacterAdded:Connect(function()
                v1062()
            end)
            task.wait(1)

            if Executor.replicatesignal then
                Executor.replicatesignal(LocalPlayer.kill)
            elseif u36 and u36:FindFirstChild("Underwater") then
                u36.Underwater:FireServer(true)
            else
                LocalPlayer.Character.Humanoid.Health = 0
            end

            task.wait(1)
            game:GetService("CoreGui"):FindFirstChild("RobloxGui").Backpack.Visible = true
            LocalPlayer:SetAttribute("Alive", true)
            LocalPlayer:SetAttribute("FakeDeath", true)
            u2:Notify({
					Title = "Pick up a bandage to restore all interactions",
					Time = 5
				})
        end
    end)
end
})
local function v67(p42)
    if not p42:GetAttribute("HoldDuration_Old") then
        p42:SetAttribute("HoldDuration_Old", p42.HoldDuration)
        p42:SetAttribute("RequiresLineOfSight_Old", p42.RequiresLineOfSight)
        p42:SetAttribute("MaxActivationDistance_Old", p42.MaxActivationDistance)
    end

    if Toggles2.InstantInteract.Value then
        p42.HoldDuration = 0
    end

    if Toggles2.PromptClip.Value then
        p42.RequiresLineOfSight = false
    end

    if Toggles2.PromptReach.Value then
        p42.MaxActivationDistance = p42:GetAttribute("MaxActivationDistance_Old") * Options2.PromptRange.Value
    end
end
FakePrompts = {}
local Folder = Instance.new("Folder")
Folder.Name = "PromptContainer"
Folder.Parent = CoreGui
local function v69(p43)
    if p43.ClassName ~= "ProximityPrompt" or p43:GetAttribute("FakePrompt") then
        return
    end

    table.insert(_TrackedPrompts, p43)
    v67(p43)

    local t9 = {
		UnlockPrompt = true,
		SkullPrompt = true,
		LockPrompt = true,
		ThingToEnable = true,
		FusesPrompt = true
	}

    if Executor.fireproximityprompt then
        local v278 = LocalPlayer:GetAttribute("CurrentFloor") or ""

        if v278 ~= "OldHotel" and v278 ~= "Fools" and (t9[p43.Name] or (p43.Parent and p43.Parent:GetAttribute("Locked") == true or p43.Parent and (p43.Parent.Parent and (p43.Parent.Parent.Name == "Locker_Small_Locked" and p43.Name == "ActivateEventPrompt")))) then
            local clone = p43:Clone()

            clone:SetAttribute("FakePrompt", true)
            task.wait()
            clone.Parent = p43.Parent
            clone:SetAttribute("HoldDuration_Old", p43:GetAttribute("HoldDuration_Old"))
            clone:SetAttribute("RequiresLineOfSight_Old", p43:GetAttribute("RequiresLineOfSight_Old"))
            clone:SetAttribute("MaxActivationDistance_Old", p43:GetAttribute("MaxActivationDistance_Old"))
            clone.HoldDuration = p43.HoldDuration
            clone.RequiresLineOfSight = p43.RequiresLineOfSight
            clone.MaxActivationDistance = p43.MaxActivationDistance

            if Toggles2.InstantInteract.Value then
                clone.HoldDuration = 0
            end

            if Toggles2.PromptClip.Value then
                clone.RequiresLineOfSight = false
            end

            clone.MaxActivationDistance = clone:GetAttribute("MaxActivationDistance_Old") * Options2.PromptRange.Value
            FakePrompts[clone] = p43
            table.insert(_TrackedPrompts, clone)
            clone.Destroying:Once(function()
                local v1064 = TableFind(_TrackedPrompts, clone)

                if v1064 then
                    table.remove(_TrackedPrompts, v1064)
                end
            end)
            pcall(function()
                p43.Parent = Folder
            end)

            local connection = p43:GetPropertyChangedSignal("Enabled"):Connect(function()
                clone.Enabled = p43.Enabled
            end)

            p43:GetPropertyChangedSignal("ActionText"):Once(function()
                p43.Parent = clone.Parent
                clone:Destroy()
                connection:Disconnect()
            end)
            p43.Destroying:Once(function()
                clone:Destroy()
                connection:Disconnect()
            end)
            clone.Enabled = false
            task.wait()
            clone.Enabled = p43.Enabled
        end
    end

    p43.Destroying:Once(function()
        local v1065 = TableFind(_TrackedPrompts, p43)

        if v1065 then
            table.remove(_TrackedPrompts, v1065)
        end
    end)
end
Toggles2.InstantInteract:OnChanged(function()
    for _, v in ipairs(_TrackedPrompts) do
        v.HoldDuration = if not Toggles2.InstantInteract.Value then v:GetAttribute("HoldDuration_Old") else 0
    end
end)
Toggles2.PromptClip:OnChanged(function()
    for _, v in ipairs(_TrackedPrompts) do
        if Toggles2.PromptClip.Value then
            v.RequiresLineOfSight = false
        else
            v.RequiresLineOfSight = v:GetAttribute("RequiresLineOfSight_Old")
        end
    end
end)
Toggles2.PromptReach:OnChanged(function()
    for _, v in ipairs(_TrackedPrompts) do
        if Toggles2.PromptReach.Value then
            v.MaxActivationDistance = v:GetAttribute("MaxActivationDistance_Old") * Options2.PromptRange.Value
        else
            v.MaxActivationDistance = v:GetAttribute("MaxActivationDistance_Old")
        end
    end
end)
Options2.PromptRange:OnChanged(function()
    if Toggles2.PromptReach.Value then
        for _, v in ipairs(_TrackedPrompts) do
            v.MaxActivationDistance = v:GetAttribute("MaxActivationDistance_Old") * Options2.PromptRange.Value
        end
    end
end)
Groupboxes.Exploits_BypassBox = t4.Exploits:AddRightTabbox("Bypasses")
Groupboxes.Exploits_Bypasses = Groupboxes.Exploits_BypassBox:AddTab("Bypasses")
Groupboxes.Exploits_Removals = Groupboxes.Exploits_BypassBox:AddTab("Removals")
Groupboxes.Exploits_Damage = Groupboxes.Exploits_BypassBox:AddTab("Damage")
Groupboxes.Exploits_Bypasses:AddToggle("BypassGiggle", {
	Text = "Anti Giggle",
	Default = false,
	Tooltip = "Prevents Giggle from attacking you"
})
Groupboxes.Exploits_Bypasses:AddToggle("BypassDupe", {
	Text = "Anti Dupe",
	Default = false,
	Tooltip = "Prevents you from opening Dupe doors"
})
Groupboxes.Exploits_Bypasses:AddToggle("BypassEyes", {
	Text = "Anti Eyes",
	Default = false,
	Tooltip = "Prevents Eyes from hurting you",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Groupboxes.Exploits_Bypasses:AddToggle("BypassLookman", {
	Text = "Anti Lookman",
	Default = false,
	Tooltip = "Prevents Lookman from hurting you",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Groupboxes.Exploits_Bypasses:AddToggle("BypassGloombatEggs", {
	Text = "Anti Gloombat Eggs",
	Default = false,
	Tooltip = "Prevents taking damage from stepping on Gloombat eggs"
})
Groupboxes.Exploits_Bypasses:AddToggle("BypassSeekObstructions", {
	Text = "Anti Seek Obstructions",
	Default = false,
	Tooltip = "Prevents obstacles in the Seek chase from harming you"
})
Groupboxes.Exploits_Bypasses:AddToggle("BypassVacuum", {
	Text = "Anti Vacuum",
	Default = false,
	Tooltip = "Prevents you from falling into Vacuum fake doors"
})
Groupboxes.Exploits_Bypasses:AddToggle("BypassSnare", {
	Text = "Anti Snare",
	Default = false,
	Tooltip = "Prevents Snare from trapping you"
})
local t10 = {
	Entities = {},
	EventTriggers = {},
	Obstructions = {},
	HidingSpots = {},
	SeekObstructions = {},
	SeekBridges = {},
	SeekNodes = {},
	SeekDuckBoards = {},
	SeekHighlights = {},
	EyestalkHighlights = {},
	PathLights = {}
}
EntityNames = {
	GiggleCeiling = true,
	DoorFake = true,
	FakeDoor = true,
	GloombatEgg = true,
	GloombatEggs = true,
	Snare = true,
	Figure = true,
	FigureRig = true,
	FigureRagdoll = true,
	GloomPile = true,
	TriggerEventCollision = true,
	Seek_Arm = true,
	ChandelierObstruction = true,
	SeekFloodline = true,
	Bridge = true,
	MinecartRig = true,
	RunnerNodes = true,
	DuckBoard = true,
	PathLights = true,
	SeekMovingNewClone = true,
	SideroomSpace = true
}
local function v71(p44)
    if not p44 or not p44.Parent then
        return
    end

    local p44Name = p44.Name

    if not EntityNames[p44Name] then
        return
    end

    if p44Name == "GiggleCeiling" then
        table.insert(t10.Entities, p44)

        if Toggles2.BypassGiggle.Value then
            p44:WaitForChild("Hitbox").CanTouch = false

            return
        end
    elseif p44Name == "DoorFake" or p44Name == "FakeDoor" then
        if p44.Parent and p44:FindFirstChild("Hidden") then
            if Toggles2.BypassDupe.Value then
                p44:WaitForChild("Hidden").CanTouch = false

                local Lock = p44:FindFirstChild("Lock")

                if Lock and Lock:FindFirstChild("UnlockPrompt") then
                    Lock.UnlockPrompt.Enabled = false
                end
            end

            if Toggles2.EntitiesESP.Value then
                local v292 = Options2.EntitiesESPAllowed and Options2.EntitiesESPAllowed.Value

                if not v292 or #v292 == 0 or v292.Dupe then
                    Functions.AddESP({
						Object = p44,
						Text = "Dupe",
						Color = Options2.EntitiesESPColor.Value
					})
                end
            end

            table.insert(t10.Entities, p44)

            return
        end
    else
        if p44Name == "SideroomSpace" then
            if Toggles2.BypassVacuum.Value then
                p44:WaitForChild("Collision").CanCollide = true
                p44:WaitForChild("Collision").CanTouch = false
            end

            table.insert(t10.Entities, p44)

            return
        end

        if p44Name == "GloombatEgg" or p44Name == "GloombatEggs" or p44Name == "GloomPile" then
            if Toggles2.BypassGloombatEggs.Value then
                for _, descendant in p44:GetDescendants() do
                    if descendant:IsA("BasePart") then
                        descendant.CanTouch = false
                    end
                end
            end

            local connection = p44.DescendantAdded:Connect(function(descendant)
                if descendant:IsA("BasePart") then
                    descendant.CanTouch = false
                end
            end)

            table.insert(Connections, connection)
            table.insert(t10.Entities, p44)

            return
        end

        if p44Name == "Snare" then
            for _, descendant in p44:GetDescendants() do
                if descendant:IsA("BasePart") then
                    descendant.CanTouch = not Toggles2.BypassSnare.Value
                end
            end

            local connection = p44.DescendantAdded:Connect(function(descendant)
                if descendant:IsA("BasePart") then
                    descendant.CanTouch = not Toggles2.BypassSnare.Value
                end
            end)

            table.insert(Connections, connection)
            table.insert(t10.Entities, p44)

            if p44:FindFirstChild("Snare") then
                p44:WaitForChild("Snare"):WaitForChild("Roots").Transparency = 1
                p44:WaitForChild("Snare"):WaitForChild("SnareBase").Transparency = 1
            end

            if p44:FindFirstChild("Void") then
                p44.Void.Transparency = 0
                p44.Void.Color = Color3.fromRGB(76, 67, 55)

                return
            end
        elseif p44Name == "Figure" or p44Name == "FigureRig" or p44Name == "FigureRagdoll" then
            for _, descendant in p44:GetDescendants() do
                if descendant:IsA("BasePart") then
                    descendant.CanTouch = false
                end
            end

            table.insert(t10.Entities, p44)

            if (Toggles2.RemoveFigure and Toggles2.RemoveFigure.Value or Toggles2.RemoveFigureMines and Toggles2.RemoveFigureMines.Value) and Executor.isnetworkowner then
                local v301 = v39()

                if v301 == "Mines" then
                    for _, descendant in p44:GetDescendants() do
                        if descendant:IsA("BasePart") then
                            task.spawn(function()
                                if pcall(function()
                                    return Executor.isnetworkowner(descendant)
                                end) then
                                    descendant.Position = Vector3.new(-49999, -49999, -49999)
                                end
                            end)
                        end
                    end

                    return
                end

                if v301 == "OldHotel" or v301 == "Fools" then
                    local CurrentRooms4 = workspace:FindFirstChild("CurrentRooms")

                    if CurrentRooms4 then
                        CurrentRooms4.ChildAdded:Wait()
                    end

                    for _, descendant in p44:GetDescendants() do
                        if descendant:IsA("BasePart") then
                            descendant.CanCollide = false
                            task.spawn(function()
                                while pcall(function()
                                    return Executor.isnetworkowner(descendant)
                                end) do
                                    descendant.Position = Vector3.new(math.random(-29999, 29999), math.random(-29999, 29999), math.random(-29999, 29999))
                                    task.wait()
                                end
                            end)
                        end
                    end

                    return
                end
            end
        else
            if p44Name == "TriggerEventCollision" then
                table.insert(t10.EventTriggers, p44)
                table.insert(t10.Entities, p44)

                return
            end

            if p44Name == "Seek_Arm" or p44Name == "ChandelierObstruction" then
                local function v307(p45)
                    if p45:IsA("BasePart") then
                        p45.CanTouch = not Toggles2.BypassSeekObstructions.Value
                        table.insert(t10.SeekObstructions, p45)
                    end
                end

                for _, descendant in p44:GetDescendants() do
                    v307(descendant)
                end

                p44.DescendantAdded:Connect(v307)

                return
            end

            if p44Name == "SeekFloodline" then
                p44.CanCollide = Toggles2.BypassSeekObstructions.Value

                local connection = p44:GetPropertyChangedSignal("CanCollide"):Connect(function()
                    if p44.CanCollide ~= Toggles2.BypassSeekObstructions.Value then
                        p44.CanCollide = Toggles2.BypassSeekObstructions.Value
                    end
                end)

                p44.Destroying:Once(function()
                    connection:Disconnect()
                end)
                table.insert(t10.SeekObstructions, p44)

                return
            end

            if p44Name == "Bridge" then
                for _, child in p44:GetChildren() do
                    if child.Name == "PlayerBarrier" and child.Size.Y == 2.75 and (child.Rotation.X == 0 or child.Rotation.X == 180) then
                        local clone = child:Clone()

                        clone.CFrame = clone.CFrame * CFrame.new(0, 0, -5)
                        clone.Name = tostring(math.random(1000000000, 9000000000))
                        clone.Size = Vector3.new(clone.Size.X, clone.Size.Y, 11)
                        clone.Parent = p44
                        clone.CanCollide = Toggles2.BypassSeekObstructions.Value
                        clone.Color = Color3.fromRGB(0, 255, 255)
                        clone.Transparency = not Toggles2.BypassSeekObstructions.Value and 1 or 0
                        clone.Material = Enum.Material.ForceField
                        table.insert(t10.SeekBridges, clone)
                    end

                    task.wait()
                end

                return
            end

            if p44Name == "MinecartRig" then
                t12.Minecart = p44

                return
            end

            if p44Name == "RunnerNodes" then
                local function v314(p46, p47)
                    local Position = (p46.CFrame + p46.CFrame.LookVector).Position
                    local Position2 = (p46.CFrame + p46.CFrame.LookVector * -1).Position

                    return (Position - p47.Position).Magnitude > (Position2 - p47.Position).Magnitude
                end

                for _, child in p44:GetChildren() do
                    local num = tonumber(child.Name:split("MinecartNode")[2])

                    if child:GetAttribute("DeathType") then
                        child:SetAttribute("DistanceBlacklist", true)
                    end

                    for i = 1, 20 do
                        local v319 = num and p44:FindFirstChild("MinecartNode" .. num + i)

                        if v319 and v319:GetAttribute("DeathType") ~= nil then
                            child:SetAttribute("DistanceBlacklist", true)
                        end
                    end

                    local v320 = num and p44:FindFirstChild("MinecartNode" .. num - 1)

                    if v320 and v320:GetAttribute("ForceConnect") then
                        child:SetAttribute("DistanceBlacklist", nil)
                    end

                    task.wait()
                end

                for _, child in p44:GetChildren() do
                    if child:GetAttribute("ForceConnect") then
                        local v323 = (function(p48)
                            local v1077
                            local huge = math.huge
                            local num = tonumber(p48.Name:split("MinecartNode")[2])
                            for _, child2 in p44:GetChildren() do
                                local num2 = tonumber(child2.Name:split("MinecartNode")[2])

                                if child2 ~= p48 and num2 and num and num < num2 then
                                    local Magnitude = (p48.Position - child2.Position).Magnitude

                                    if Magnitude < huge and child2:GetAttribute("DistanceBlacklist") ~= true then
                                        huge = Magnitude
                                        v1077 = child2
                                    end
                                end
                            end

                            return v1077
                        end)(child)

                        if v323 then
                            child:SetAttribute("Turn", (function(p49, p50)
                                local v1075 = p49.CFrame.RightVector:Dot(p49.Position - p50.Position)

                                if v1075 > 0.5 then
                                    if v314(p49, p50) then
                                        return "Right"
                                    end

                                    return "Left"
                                end

                                if v1075 < -0.5 then
                                    if v314(p49, p50) then
                                        return "Left"
                                    end

                                    return "Right"
                                end

                                return "Straight"
                            end)(child, v323))
                            table.insert(t10.SeekNodes, child)
                        end
                    end

                    task.wait()
                end

                return
            end

            if p44Name == "DuckBoard" then
                table.insert(t10.SeekDuckBoards, p44)

                return
            end

            if p44Name == "PathLights" then
                local t11 = {}
                local connection = p44.ChildAdded:Connect(function(child)
                    table.insert(t11, child)
                end)
                for _, child in p44:GetChildren() do
                    table.insert(t11, child)
                end
                local u328
                local function v329(p51)
                    if p51.Name ~= "SeekGuidingLight" or p51:GetAttribute("Highlighted") then
                        return
                    end

                    p51:SetAttribute("Highlighted", true)

                    local Part = Instance.new("Part")

                    Part.Size = Vector3.one
                    Part.Transparency = 1
                    Part.Parent = t12.SeekNodesFolder
                    Part.Anchored = true
                    Part.CFrame = p51.CFrame
                    Part.CanCollide = false
                    Part.Name = "SeekLightNode"

                    local v1087 = u328 or Part

                    u328 = Part

                    local Beam = Instance.new("Beam")
                    local v1089 = rawget(Toggles2, "ShowSeekPathColor") and Options2.ShowSeekPathColor.Value or Color3.fromRGB(0, 255, 0)

                    Beam.Color = ColorSequence.new({
						ColorSequenceKeypoint.new(0, v1089),
						ColorSequenceKeypoint.new(1, v1089)
					})
                    Beam.FaceCamera = true
                    Beam.Width0 = 0.2
                    Beam.Width1 = 0.2
                    Beam.Brightness = 10
                    Beam.LightInfluence = 0
                    Beam.LightEmission = 0
                    Beam.Enabled = true

                    local v1090 = (not rawget(Toggles2, "ShowSeekPathToggle") or not Toggles2.ShowSeekPathToggle.Value) and 1 or 0

                    Beam.Transparency = NumberSequence.new({
						NumberSequenceKeypoint.new(0, v1090),
						NumberSequenceKeypoint.new(1, v1090)
					})
                    Beam.Parent = t12.SeekNodesFolder

                    local Attachment = Instance.new("Attachment", Part)
                    local Attachment2 = Instance.new("Attachment", v1087)

                    Beam.Attachment0 = Attachment
                    Beam.Attachment1 = Attachment2
                    table.insert(t10.SeekHighlights, Beam)
                end
                task.spawn(function()
                    while task.wait() do
                        local v1093 = table.remove(t11, 1)

                        if v1093 then
                            v329(v1093)
                        end
                    end
                end)
                table.insert(Connections, connection)
                table.insert(t10.PathLights, p44)
                p44.Destroying:Once(function()
                    connection:Disconnect()
                end)

                return
            end

            if p44Name == "SeekMovingNewClone" then
                local connection = p44.Destroying:Connect(function()
                    for _, v in pairs(t10.PathLights) do
                        v:ClearAllChildren()
                    end

                    if t12.SeekNodesFolder then
                        t12.SeekNodesFolder:ClearAllChildren()
                    end
                end)

                table.insert(Connections, connection)
            end
        end
    end
end
for _, descendant in workspace:GetDescendants() do
    task.spawn(v71, descendant)
end
workspace.DescendantAdded:Connect(v71)
Toggles2.BypassGiggle:OnChanged(function(p52)
    for _, v in ipairs(t10.Entities) do
        if v.Name == "GiggleCeiling" then
            local Hitbox = v:FindFirstChild("Hitbox")

            if Hitbox then
                Hitbox.CanTouch = not p52
            end
        end
    end
end)
Toggles2.BypassDupe:OnChanged(function(p53)
    for _, v337 in t10.Entities do
        if v337.Name == "DoorFake" or v337.Name == "FakeDoor" then
            v337:WaitForChild("Hidden").CanTouch = not p53

            if v337:FindFirstChild("Lock") then
                v337.Lock.UnlockPrompt.Enabled = not p53
            end
        end
    end
end)
Toggles2.BypassEyes:OnChanged(function(p54)
    if p54 and t12.IsEyes then
        local v339 = v39()

        if v339 == "Fools" or v339 == "OldHotel" then
            u36.MotorReplication:FireServer(0, t12.SpoofOffset ~= 200 and -65 or 65, 0, false)

            return
        end

        u36.MotorReplication:FireServer(-650)
    end
end)
Toggles2.BypassLookman:OnChanged(function(p55)
    if p55 and t12.IsLookman then
        local v341 = v39()

        if v341 == "Fools" or v341 == "OldHotel" then
            u36.MotorReplication:FireServer(0, t12.SpoofOffset ~= 200 and -65 or 65, 0, false)

            return
        end

        u36.MotorReplication:FireServer(-650)
    end
end)
Toggles2.BypassGloombatEggs:OnChanged(function(p56)
    for _, v in ipairs(t10.Entities) do
        if v.Name == "GloombatEgg" or v.Name == "GloombatEggs" or v.Name == "GloomPile" then
            for _, descendant in v:GetDescendants() do
                if descendant:IsA("BasePart") then
                    descendant.CanTouch = not p56
                end
            end
        end
    end
end)
Toggles2.BypassSeekObstructions:OnChanged(function(p57)
    for _, v in ipairs(t10.SeekObstructions) do
        v.CanTouch = not p57

        if v.Name == "SeekFloodline" then
            v.CanCollide = p57
        end
    end

    for _, v in ipairs(t10.SeekBridges) do
        v.CanCollide = p57
        v.Transparency = not p57 and 1 or 0
    end
end)
Toggles2.BypassVacuum:OnChanged(function(p58)
    for _, v354 in t10.Entities do
        if v354.Name == "SideroomSpace" then
            v354:WaitForChild("Collision").CanCollide = p58
            v354:WaitForChild("Collision").CanTouch = not p58
        end
    end
end)
Toggles2.BypassSnare:OnChanged(function(p59)
    for _, v in ipairs(t10.Entities) do
        if v.Name == "Snare" then
            for _, descendant in v:GetDescendants() do
                if descendant:IsA("BasePart") then
                    descendant.CanTouch = not p59
                end
            end
        end
    end
end)
Groupboxes.Exploits_Removals:AddToggle("RemoveScreech", {
	Text = "Remove Screech",
	Default = false,
	Tooltip = "Prevents Screech from spawning"
})
Groupboxes.Exploits_Removals:AddToggle("RemoveA90", {
	Text = "Remove A-90",
	Default = false,
	Tooltip = "Prevents A-90 from spawning"
})
Groupboxes.Exploits_Removals:AddToggle("RemoveDread", {
	Text = "Remove Dread",
	Default = false,
	Tooltip = "Prevents Dread from spawning"
})
Groupboxes.Exploits_Removals:AddDivider()
Groupboxes.Exploits_Removals:AddToggle("RemoveFootstepSounds", {
	Text = "Remove Footstep Sounds",
	Default = false,
	Tooltip = "Removes the sounds when walking"
})
Groupboxes.Exploits_Removals:AddToggle("RemoveJamminMusic", {
	Text = "Remove Jammin Music",
	Default = false,
	Tooltip = "Removes the music and muffle effect from the Jammin modifier"
})
Groupboxes.Exploits_Removals:AddToggle("RemoveInteractingSounds", {
	Text = "Remove Interacting Sounds",
	Default = false,
	Tooltip = "Removes the sounds when interacting with proximity prompts"
})
Groupboxes.Exploits_Removals:AddToggle("RemoveHasteSound", {
	Text = "Remove Haste Sound",
	Default = false,
	Tooltip = "Mutes the ambience sound when Haste is active"
})
Groupboxes.Exploits_Removals:AddToggle("RemoveClosetDelay", {
	Text = "Remove Closet Delay",
	Default = false,
	Tooltip = "Removes the short window where you can't exit out of a closet after the animation finishes."
})
Toggles2.RemoveScreech:OnChanged(function(p60)
    local v361 = v53()

    if not v361 then
        return
    end

    local Screech = v361:FindFirstChild("Screech")

    if Screech then
        Screech.Name = not p60 and "Screech" or "Screech_Disabled"
    end

    local GlitchScreech = v361:FindFirstChild("GlitchScreech")

    if GlitchScreech then
        GlitchScreech.Name = not p60 and "GlitchScreech" or "GlitchScreech_Disabled"
    end
end)
Toggles2.RemoveA90:OnChanged(function(p61)
    local v365 = v53()

    if not v365 then
        return
    end

    local A90 = v365:FindFirstChild("A90")

    if A90 then
        A90.Name = not p61 and "A90" or "A90_Disabled"
    end
end)
Toggles2.RemoveDread:OnChanged(function(p62)
    local v368 = v53()

    if not v368 then
        return
    end

    local Dread = v368:FindFirstChild("Dread")

    if Dread then
        Dread.Name = not p62 and "Dread" or "Dread_Disabled"
    end
end)
Toggles2.RemoveFootstepSounds:OnChanged(function(p63)
    local Character4 = LocalPlayer.Character

    if not Character4 then
        return
    end

    for _, descendant in Character4:GetDescendants() do
        if descendant:IsA("Sound") and descendant.Name == "Sound" then
            descendant.Volume = not p63 and 1 or 0
        end
    end
end)
Toggles2.RemoveJamminMusic:OnChanged(function(p64)
    local PlayerGui4 = LocalPlayer:FindFirstChildOfClass("PlayerGui")
    local v376 = PlayerGui4 and PlayerGui4:FindFirstChild("MainUI")
    local v377 = v376 and v376:FindFirstChild("Initiator")
    local v378 = v377 and v377:FindFirstChild("Main_Game")

    if not v378 then
        return
    end

    local Health = v378:FindFirstChild("Health")

    if not Health then
        return
    end

    local Jam = Health:FindFirstChild("Jam")

    if Jam then
        Jam.Volume = not p64 and 0.45 or 0
    end
end)
Toggles2.RemoveInteractingSounds:OnChanged(function(p65)
    local PlayerGui5 = LocalPlayer:FindFirstChildOfClass("PlayerGui")
    local v383 = PlayerGui5 and PlayerGui5:FindFirstChild("MainUI")
    local v384 = v383 and v383:FindFirstChild("Initiator")
    local v385 = v384 and v384:FindFirstChild("Main_Game")

    if not v385 then
        return
    end

    local PromptService = v385:FindFirstChild("PromptService")

    if PromptService then
        local Triggered = PromptService:FindFirstChild("Triggered")

        if Triggered then
            Triggered.Volume = not p65 and 0.04 or 0
        end

        local Holding = PromptService:FindFirstChild("Holding")

        if Holding then
            Holding.Volume = not p65 and 0.1 or 0
        end

        local Notification = PromptService:FindFirstChild("Notification")

        if Notification then
            Notification.Volume = not p65 and 0.03 or 0
        end
    end

    local Reminder = v385:FindFirstChild("Reminder")

    if Reminder then
        local Caption = Reminder:FindFirstChild("Caption")

        if Caption then
            Caption.Volume = not p65 and 0.1 or 0
        end
    end
end)
Groupboxes.Exploits_Damage:AddToggle("NoScreechDamage", {
	Text = "No Screech Damage",
	Default = false,
	Tooltip = "Prevents Screech from hurting you"
})
Groupboxes.Exploits_Damage:AddToggle("NoA90Damage", {
	Text = "No A-90 Damage",
	Default = false,
	Tooltip = "Prevents A-90 from hurting you"
})
Toggles2.NoScreechDamage:OnChanged(function(p66)
    if not u36 then
        return
    end

    if p66 then
        if FakeEvents.Screech_Real and FakeEvents.Screech_Real.Parent then
            FakeEvents.Screech.Parent = u36
            FakeEvents.Screech_Real.Parent = nil

            return
        end
    elseif FakeEvents.Screech_Real then
        FakeEvents.Screech_Real.Parent = u36
        FakeEvents.Screech.Parent = nil
    end
end)
Groupboxes.Exploits_Damage:AddToggle("NoHaltDamage", {
	Text = "No Halt Damage",
	Default = false,
	Tooltip = "Prevents Halt from hurting you"
})
Toggles2.NoHaltDamage:OnChanged(function(p67)
    if not u36 then
        return
    end

    if p67 then
        if FakeEvents.Shade_Real and FakeEvents.Shade_Real.Parent then
            FakeEvents.Shade.Parent = u36
            FakeEvents.Shade_Real.Parent = nil

            return
        end
    elseif FakeEvents.Shade_Real then
        FakeEvents.Shade_Real.Parent = u36
        FakeEvents.Shade.Parent = nil
    end
end)
Toggles2.NoA90Damage:OnChanged(function(p68)
    if not u36 then
        return
    end

    if p68 then
        if FakeEvents.A90_Real and FakeEvents.A90_Real.Parent then
            FakeEvents.A90.Parent = u36
            FakeEvents.A90_Real.Parent = nil

            return
        end
    elseif FakeEvents.A90_Real then
        FakeEvents.A90_Real.Parent = u36
        FakeEvents.A90.Parent = nil
    end
end)
Groupboxes.Exploits_Automation = t4.Exploits:AddRightGroupbox("Automation")
Groupboxes.Exploits_Automation:AddToggle("AutoUnlockPadlockToggle", {
	Text = "Auto Unlock Padlock",
	Default = false,
	Tooltip = "Instantly unlocks the room 50 padlock when you get close, but you need the books required",
	DisabledTooltip = "This feature doesn't work in this floor"
})
Groupboxes.Exploits_Automation:AddButton({
	Text = "Show Library Code",
	Tooltip = "Displays the current library code in a notification",
	DisabledTooltip = "This feature doesn't work in this floor",
	Callback = function()
    local v395 = Functions.GetLibraryCode()

    if v395 then
        u2:Notify({
				Title = "Library code: " .. v395,
				Time = 5
			})

        return
    end

    u2:Notify({
			Title = "Could not read library code",
			Time = 5
		})
end
})
Groupboxes.Exploits_Automation:AddToggle("AutoBreakerBox", {
	Text = "Auto Breaker Box",
	Default = false,
	Tooltip = "Automatically solves the breaker box.",
	DisabledTooltip = "This feature doesn't work in this floor"
})
Groupboxes.Exploits_Automation:AddDropdown("BreakerBoxMethod", {
	Text = "Breaker Box Method",
	Values = {
		"Instant",
		"Legit"
	},
	Default = 1,
	Multi = false
})
Groupboxes.Exploits_Automation:AddToggle("AutoLibraryBruteForce", {
	Text = "Auto Brute-Force Padlock",
	Default = false,
	Tooltip = "Automatically brute-forces codes in the library code when near the padlock until it finds the correct code",
	DisabledTooltip = "This feature doesn't work in this floor"
})
if ReplicatedStorage:FindFirstChild("FloorReplicated") and ReplicatedStorage.FloorReplicated:FindFirstChild("ClientRemote") and ReplicatedStorage.FloorReplicated.ClientRemote:FindFirstChild("Haste") then
    local connection = ReplicatedStorage.FloorReplicated.ClientRemote.Haste.Ambience:GetPropertyChangedSignal("Playing"):Connect(function()
        if Toggles2.RemoveHasteSound.Value then
            ReplicatedStorage.FloorReplicated.ClientRemote.Haste.Ambience.Playing = false
        end
    end)

    table.insert(Connections, connection)
end
table.insert(Connections, workspace.DescendantAdded:Connect(function(descendant)
    if Toggles2.AntiLag and (Toggles2.AntiLag.Value and descendant:IsA("BasePart")) then
        descendant:SetAttribute("Mat", descendant.Material)
        descendant.Material = "Plastic"
    end
end))
t12 = {
	UseAnimation = nil,
	UseAnimationBreak = nil,
	BreakerBoxNotified = false,
	BreakerBoxStartNotified = false,
	BreakerBoxInteracted = false,
	BreakerBoxFinishedNotified = false,
	FogInstances = {},
	OldFog = nil,
	InLobby = false,
	LobbyGuardConnection = nil,
	RoomsAutoWalkActive = false,
	IsEyes = false,
	IsLookman = false,
	AnticheatDisabled = false,
	SeekNodesFolder = nil,
	AutoMinecartDucked = false,
	LastDuck = 0,
	NearestTurnNode = nil,
	Minecart = nil,
	OriginalGetMoveVector = nil,
	SurgeFrame = nil,
	KnobFarmActive = false,
	KnobFarmStarted = false,
	UILibrary = u1,
	RedeemingCodes = false,
	SpoofOffset = 0,
	CodesList = {
		"67",
		"54",
		"41",
		"CHEDDAR BALLS",
		"XQC",
		"PENGUINZ0",
		"KREEKCRAFT",
		"ISHOWSPEED",
		"DANTDM",
		"KUBZ SCOUTS",
		"FIND THE TROLLFACES",
		"THINKNOODLES",
		"W",
		"RAGDOLL UNIVERSE",
		"RAGDOLL MAYHEM",
		"BIJUU MIKE",
		"8BITRYAN",
		"SCREECHSUCKS",
		"LORE",
		"ABCDEFGHIJKLMNOPQRSTUVWXYZ",
		"FIND THE TROLLFACES",
		"3rd",
		"LAZYDEVS",
		"RAGDOLL COMBAT",
		"VOCAB HAVOC",
		"PATHSWAP",
		"JUMP OVER THE BRICK"
	}
}
u49 = nil
if Character then
    task.spawn(SetupCharacterAnticheat)
end
if Executor.fireproximityprompt then
    Functions.FirePrompt = Executor.fireproximityprompt
    Functions.ForceFirePrompt = Executor.fireproximityprompt
else
    local t13 = {}
    local t14 = {}

    function Functions.FirePrompt(p69)
        if not p69:IsA("ProximityPrompt") or (t14[p69] or not CurrentCamera) then
            return
        end

        table.insert(t13, p69)
    end

    Functions.ForceFirePrompt = Functions.FirePrompt
    task.spawn(function()
        while task.wait() do
            local v399 = table.remove(t13, 1)

            if v399 then
                t14[v399] = true

                local MaxActivationDistance = v399.MaxActivationDistance
                local Enabled = v399.Enabled
                local Parent = v399.Parent
                local HoldDuration = v399.HoldDuration
                local RequiresLineOfSight = v399.RequiresLineOfSight

                v399.MaxActivationDistance = 99999
                v399.Enabled = true
                v399.HoldDuration = 0
                v399.RequiresLineOfSight = false

                local Part = Instance.new("Part")

                Part.Parent = workspace
                Part.CanCollide = false
                Part.CanQuery = false
                Part.CanTouch = false
                Part.Anchored = true
                Part.Transparency = 1
                Part.Size = Vector3.new(0.001, 0.001, 0.001)
                Part.Position = CurrentCamera.CFrame:ToWorldSpace(CFrame.new(0, 0, -0.1)).Position

                if v399 and Parent then
                    pcall(function()
                        v399.Parent = Part
                    end)

                    local u406 = false
                    local u407 = false
                    local connection = game:GetService("ProximityPromptService").PromptShown:Connect(function(prompt)
                        if prompt == v399 then
                            u406 = true
                        end
                    end)
                    local connection2 = v399.Triggered:Connect(function()
                        u407 = true
                    end)
                    local n6 = 0

                    while not u406 and n6 < 5 do
                        n6 += 1
                        task.wait()
                    end

                    local n7 = 0

                    while not u407 and n7 < 5 do
                        v399:InputHoldBegin()
                        v399:InputHoldEnd()
                        n7 += 1
                        task.wait()
                    end

                    v399.MaxActivationDistance = MaxActivationDistance
                    v399.Enabled = Enabled
                    v399.HoldDuration = HoldDuration
                    v399.RequiresLineOfSight = RequiresLineOfSight
                    pcall(function()
                        v399.Parent = Parent
                    end)
                    task.wait()
                    connection:Disconnect()
                    connection2:Disconnect()
                else
                    Part:Destroy()
                end

                t14[v399] = nil
            end
        end
    end)
end
function Functions.HasItem(p70, p71)
    if not p71 and LocalPlayer.Backpack:FindFirstChild(p70) then
        return LocalPlayer.Backpack:FindFirstChild(p70)
    end

    if Character:FindFirstChild(p70) then
        return Character:FindFirstChild(p70)
    end

    return nil
end
function Functions.GetLibraryCode()
    local v414 = Character:FindFirstChild("LibraryHintPaper") or (Character:FindFirstChild("LibraryHintPaperHard") or (LocalPlayer.Backpack:FindFirstChild("LibraryHintPaper") or LocalPlayer.Backpack:FindFirstChild("LibraryHintPaperHard")))

    if v414 and v414:FindFirstChild("UI") then
        local t15 = {}

        for i = 1, (not CurrentFloor or not tostring(CurrentFloor):find("Fools")) and 5 or 10 do
            t15[i] = "_"
        end

        local v417 = LocalPlayer.PlayerGui:FindFirstChild("PermUI") and (not not LocalPlayer.PlayerGui.PermUI:FindFirstChild("Hints") and LocalPlayer.PlayerGui.PermUI.Hints:GetChildren()) or {}
        local children = v414.UI:GetChildren()

        for _, v in ipairs(v417) do
            for _, v6 in ipairs(children) do
                if v:IsA("ImageLabel") and v6:IsA("ImageLabel") and v.ImageRectOffset == v6.ImageRectOffset and t15[tonumber(v6.Name)] then
                    t15[tonumber(v6.Name)] = v.TextLabel.Text
                end
            end
        end

        return table.concat(t15)
    end

    if CurrentFloor and tostring(CurrentFloor):find("Fools") then
        return "__________"
    end

    return "_____"
end
function Functions.HandleHidingTransparency(p72)
    local t16 = {}

    for _, descendant in p72:GetDescendants() do
        if descendant:IsA("BasePart") then
            descendant:SetAttribute("Transparency_Old", descendant.Transparency)
            table.insert(t16, descendant)
        end

        if descendant.Name == "HiddenPlayer" then
            local connection = descendant:GetPropertyChangedSignal("Value"):Connect(function()
                for _, v in ipairs(t16) do
                    if v:GetAttribute("Transparency_Old") then
                        TweenService:Create(v, TweenInfo.new(0.25, Enum.EasingStyle.Linear), {
							Transparency = descendant.Value == Character and (not not Toggles2.TransparentHidingSpotsToggle.Value and Options2.TransparentHidingSpotsSlider.Value) or v:GetAttribute("Transparency_Old")
						}):Play()
                    end
                end
            end)

            table.insert(Connections, connection)
            p72.Destroying:Once(function()
                connection:Disconnect()

                local v1099 = TableFind(Connections, connection)

                if v1099 then
                    table.remove(Connections, v1099)
                end
            end)
        end
    end
end
function Functions.GetMinecart()
    return CurrentCamera:FindFirstChild("MinecartRig") ~= nil
end
function Functions.GetNearestTurnNode()
    local t17 = {
		Distance = math.huge,
		Object = nil
	}
    local v429 = rawget(Options2, "AutoSteerMinecartTurnDistance") and Options2.AutoSteerMinecartTurnDistance.Value or 30

    for _, v in ipairs(t10.SeekNodes) do
        local v432 = LocalPlayer:DistanceFromCharacter(v.Position)

        if v432 < v429 and v432 < t17.Distance then
            t17.Distance = v432
            t17.Object = v
        end
    end

    return t17.Object
end
function Functions.GetNearestDuckBoard()
    local t18 = {
		Distance = math.huge,
		Object = nil
	}
    local v434 = rawget(Options2, "AutoSteerMinecartDuckDistance") and Options2.AutoSteerMinecartDuckDistance.Value or 30

    for _, v in ipairs(t10.SeekDuckBoards) do
        if v.PrimaryPart then
            local v437 = LocalPlayer:DistanceFromCharacter(v.PrimaryPart.Position)

            if v437 < v434 and v437 < t18.Distance then
                t18.Distance = v437
                t18.Object = v
            end
        end
    end

    return t18.Object
end
function Functions.SendChat(p73)
    ((ReplicatedStorage:FindFirstChild("DefaultChatSystemEvents") or Instance.new("Folder")):FindFirstChild("SayMessageRequest") or Instance.new("RemoteEvent")):FireServer(p73, "All");
    (TextChatService:FindFirstChild("TextChannels") and TextChatService.TextChannels:FindFirstChild("RBXGeneral") or Instance.new("TextChannel")):SendAsync(p73)
end
function Functions.GetNearestHidingSpot()
    local t19 = {
		Distance = math.huge,
		Object = nil
	}

    for _, v in ipairs(t10.HidingSpots) do
        if v.PrimaryPart and v:FindFirstChild("HidePrompt") then
            local v442 = LocalPlayer:DistanceFromCharacter(v.PrimaryPart.Position)

            if v442 < t19.Distance and v442 < v.HidePrompt.MaxActivationDistance and v.PrimaryPart.Position.Y > -10 then
                local HiddenPlayer = v:FindFirstChild("HiddenPlayer", true)

                if HiddenPlayer and not HiddenPlayer.Value then
                    t19.Distance = v442
                    t19.Object = v
                end
            end
        end
    end

    return t19.Object
end
function Functions.GetNearestEntity(p74, p75)
    local t20 = {
		RushMoving = 85,
		AmbushMoving = 150,
		A60 = 125,
		A120 = 85,
		GlitchRush = 90,
		GlitchAmbush = 175,
		BackdoorRush = 85,
		CustomEntity = 85
	}
    local t21 = {
		Distance = math.huge,
		Object = nil
	}

    for _, child in workspace:GetChildren() do
        if child and t20[child.Name] and child.PrimaryPart then
            local v450 = EntityNotifyData[child.Name]

            if not p75 or not v450 or not p75[v450.Alias] then
                local v451 = LocalPlayer:DistanceFromCharacter(child.PrimaryPart.Position)

                if v451 < t20[child.Name] and v451 < t21.Distance and (not p74 or child:GetAttribute("Inactive") ~= true) then
                    t21.Distance = v451
                    t21.Object = child
                end
            end
        end
    end

    return t21.Object
end;
(function()
    if not Executor.hookmetamethod or (not Executor.newcclosure or not Executor.getnamecallmethod) then
        return
    end

    MainHook = Executor.hookmetamethod(game, "__namecall", Executor.newcclosure(function(p76, ...)
        local t22 = { ... }
        local v1102 = Executor.getnamecallmethod()

        if p76.Name == "MotorReplication" and (v1102 == "FireServer" and (Toggles2.BypassEyes.Value and t12.IsEyes or Toggles2.BypassLookman.Value and t12.IsLookman)) then
            local _CurrentFloor = CurrentFloor

            if _CurrentFloor == "Fools" or _CurrentFloor == "OldHotel" then
                t22[1] = 0
                t22[2] = t12.SpoofOffset ~= 200 and -65 or 65
                t22[3] = 0
                t22[4] = false
            else
                t22[1] = -650
            end
        end

        if p76.Name == "Crouch" and v1102 == "FireServer" then
            if Toggles2.Godmode and Toggles2.Godmode.Value then
                t22[1] = true
            end

            t22[2] = true
        end

        if Toggles2.AutoHeartbeatMinigame and Toggles2.AutoHeartbeatMinigame.Value and v1102 == "FireServer" then
            if p76.Name == "ClutchHeartbeat" then
                return MainHook(p76, true)
            end

            if p76.Name == "HideMonster" then
                return MainHook(p76, true)
            end
        end

        return MainHook(p76, table.unpack(t22))
    end))
    OtherHook = Executor.hookmetamethod(game, "__index", Executor.newcclosure(function(p77, p78)
        local v1106 = OtherHook(p77, p78)

        if p78 == "MoveDirection" and (p77 == Humanoid and t12.RoomsAutoWalkActive and Toggles2.RoomsAutoWalkSpoofFootsteps.Value and CurrentFloor == "Rooms") and (not Character or not Character:GetAttribute("Hiding")) then
            if HumanoidRootPart then
                local HumanoidRootPartVelocity = HumanoidRootPart.Velocity

                if HumanoidRootPartVelocity.Magnitude > 0.1 then
                    return HumanoidRootPartVelocity.Unit
                end

                return HumanoidRootPart.CFrame.LookVector
            end

            return v1106
        end

        return v1106
    end))
end)()
local function v77(p79)
    local CurrentRooms5 = workspace:FindFirstChild("CurrentRooms")

    if CurrentRooms5 and CurrentRooms5 == p79.Parent then
        local Firedamp = p79:GetAttribute("Firedamp")

        p79:SetAttribute("Firedamp_Old", Firedamp ~= nil and (Firedamp or false))

        if Toggles2.DisableFiredampEffect.Value then
            p79:SetAttribute("Firedamp", false)
        end
    end

    local p79Name = p79.Name

    if p79Name == "Padlock" then
        Connections.PadlockConnection = RunService.Heartbeat:Connect(function()
            if _Unloading then
                return
            end

            if p79.PrimaryPart then
                local v1108 = LocalPlayer:DistanceFromCharacter(p79.PrimaryPart.Position)

                if Toggles2.AutoUnlockPadlockToggle.Value then
                    local v1109 = Functions.GetLibraryCode()

                    if v1109 and tonumber(v1109) and v1108 < 50 then
                        u36.PL:FireServer(v1109)
                    end
                end

                if Toggles2.AutoLibraryBruteForce.Value and v1108 < 50 then
                    local v1110 = Functions.GetLibraryCode()

                    if v1110 and string.find(v1110, "_") then
                        local s2 = ""

                        for i = 1, #v1110 do
                            local v1113 = string.sub(v1110, i, i)

                            s2 ..= v1113 == "_" and math.random(0, 9) or v1113
                        end

                        if u36 and u36:FindFirstChild("PL") then
                            u36.PL:FireServer(s2)
                        end

                        t12.BruteForceCount = (t12.BruteForceCount or 0) + 1

                        if t12.BruteForceCount % 750 == 0 then
                            task.spawn(function()
                                u2:Notify({
									Title = "Brute-force: checked " .. t12.BruteForceCount .. " codes",
									Description = "Not found yet",
									Time = 2
								})
                            end)
                        end
                    end
                end
            end
        end)
        p79.Destroying:Once(function()
            if t12.BruteForceCount and t12.BruteForceCount > 0 then
                task.spawn(function()
                    u2:Notify({
						Title = "Padlock code found after " .. t12.BruteForceCount .. " attempts!",
						Description = "yay",
						Time = 5
					})
                end)
            end

            t12.BruteForceCount = nil

            if Connections.PadlockConnection then
                Connections.PadlockConnection:Disconnect()
                Connections.PadlockConnection = nil
            end
        end)

        return
    end

    if p79Name == "ElevatorBreaker" then
        if Toggles2.AutoBreakerBox.Value and not t12.BreakerBoxNotified then
            t12.BreakerBoxNotified = true
        end

        local Code = p79:WaitForChild("SurfaceGui"):WaitForChild("Frame"):WaitForChild("Code")

        local function v457()
            if Toggles2.AutoBreakerBox.Value then
                local v1114 = Options2.BreakerBoxMethod and Options2.BreakerBoxMethod.Value or "Instant"

                if v1114 == "Instant" then
                    task.spawn(function()
                        task.wait(1)
                        pcall(function()
                            if u36 and u36:FindFirstChild("EBF") then
                                u36.EBF:FireServer()
                            end
                        end)
                    end)
                elseif v1114 == "Legit" then
                    local num = tonumber(Code.Text)

                    if not num then
                        return
                    end

                    for _, descendant in p79:GetDescendants() do
                        if descendant.Name == "BreakerSwitch" and num == descendant:GetAttribute("ID") then
                            local BackgroundTransparency = Code:WaitForChild("Frame").BackgroundTransparency
                            local PrismaticConstraint = descendant:FindFirstChild("PrismaticConstraint")
                            local Light = descendant:FindFirstChild("Light")
                            local Sound = descendant:FindFirstChild("Sound")

                            if BackgroundTransparency == 0 then
                                if descendant:GetAttribute("Enabled") then
                                    return
                                end

                                descendant:SetAttribute("Enabled", true)

                                if PrismaticConstraint then
                                    PrismaticConstraint.TargetPosition = -0.2
                                end

                                if Light then
                                    Light.Material = Enum.Material.Neon

                                    local Spark = Light:FindFirstChild("Spark", true)

                                    if Spark then
                                        Spark:Emit(1)
                                    end
                                end

                                if not Sound then
                                    break
                                end

                                Sound:Play()

                                break
                            end

                            if BackgroundTransparency ~= 1 then
                                break
                            end

                            if not descendant:GetAttribute("Enabled") then
                                return
                            end

                            descendant:SetAttribute("Enabled", false)

                            if PrismaticConstraint then
                                PrismaticConstraint.TargetPosition = 0.2
                            end

                            if Light then
                                Light.Material = Enum.Material.Glass
                            end

                            if not Sound then
                                break
                            end

                            Sound:Play()

                            break
                        end
                    end
                end

                if not t12.BreakerBoxStartNotified then
                    t12.BreakerBoxStartNotified = true
                end
            end

            t12.BreakerBoxInteracted = true
        end

        Connections.BreakerConnection = Code:GetPropertyChangedSignal("Text"):Connect(v457)
        t12.RunBreakerFunc = v457
        p79.Destroying:Once(function()
            t12.RunBreakerFunc = nil
        end)

        return
    end

    if p79Name == "ElevatorCar" then
        local connection = p79.DescendantAdded:Connect(function(descendant)
            if Toggles2.AutoBreakerBox.Value and (descendant.Name == "TouchInterest" and not t12.BreakerBoxFinishedNotified) then
                t12.BreakerBoxFinishedNotified = true
            end
        end)

        p79.Destroying:Once(function()
            connection:Disconnect()
        end)
    end
end
local function v78(p80)
    if not HumanoidRootPart or not p80 then
        return math.huge
    end
    local p80Parent = p80.Parent
    local v461
    if p80Parent:IsA("BasePart") then
        v461 = p80Parent
    elseif p80Parent:IsA("Attachment") and p80Parent.Parent and p80Parent.Parent:IsA("BasePart") then
        v461 = p80Parent.Parent
    elseif p80Parent:IsA("Model") then
        v461 = p80Parent.PrimaryPart or p80Parent:FindFirstChildWhichIsA("BasePart", true)
    end
    if not v461 then
        return math.huge
    end

    return (v461.Position - HumanoidRootPart.Position).Magnitude
end
_promptTriggerLock = false
local function v79(p81, p82)
    if not p81 or not p81:IsA("ProximityPrompt") then
        return false
    end

    if _promptTriggerLock then
        return false
    end

    local v464 = p82 or 20

    if not p81.Enabled then
        return false
    end

    if not Toggles2.PromptClip.Value and v464 < v78(p81) then
        return false
    end

    _promptTriggerLock = true
    pcall(function()
        local HoldDuration = p81.HoldDuration
        local RequiresLineOfSight = p81.RequiresLineOfSight
        local MaxActivationDistance = p81.MaxActivationDistance

        p81.HoldDuration = 0
        p81.RequiresLineOfSight = false
        p81.MaxActivationDistance = math.huge

        if Executor.fireproximityprompt then
            Executor.fireproximityprompt(p81)
        else
            p81:InputHoldBegin()
            p81:InputHoldEnd()
        end

        p81.HoldDuration = HoldDuration
        pcall(function()
            p81.RequiresLineOfSight = RequiresLineOfSight
        end)
        pcall(function()
            p81.MaxActivationDistance = MaxActivationDistance
        end)
    end)
    _promptTriggerLock = false

    return true
end
local u80
Toggles2.InfiniteCrucifix:OnChanged(function(p83)
    if u80 then
        u80:Disconnect()
        u80 = nil
    end

    if not p83 then
        return
    end

    local function v466(p84)
        if not p84 or p84.Name ~= "Crucifix" then
            return
        end

        if p84:GetAttribute("InfCrucifixHooked") then
            return
        end

        p84:SetAttribute("InfCrucifixHooked", true)
        p84.Destroying:Once(function()
            if not Toggles2.InfiniteCrucifix.Value then
                return
            end

            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Crucifix") then
                return
            end

            local Crucifix = workspace.Drops:FindFirstChild("Crucifix")

            if Crucifix then
                local ProximityPrompt = Crucifix:FindFirstChildWhichIsA("ProximityPrompt", true)

                if ProximityPrompt then
                    v79(ProximityPrompt, 20)
                end
            end
        end)
    end

    for _, child in ipairs(LocalPlayer.Backpack:GetChildren()) do
        v466(child)
    end

    if LocalPlayer.Character then
        for _, child in ipairs(LocalPlayer.Character:GetChildren()) do
            v466(child)
        end
    end

    u80 = LocalPlayer.Backpack.ChildAdded:Connect(v466)
end)
Toggles2.AutoBreakerBox:OnChanged(function(p85)
    if not p85 then
        return
    end

    if t12.RunBreakerFunc then
        if t12.BreakerBoxInteracted then
            t12.RunBreakerFunc()

            return
        end
    elseif CurrentRooms:FindFirstChild("ElevatorBreaker", true) then
        if not t12.BreakerBoxInteracted then
            if not t12.BreakerBoxNotified then
                t12.BreakerBoxInteracted = true

                return
            end
        else
            task.spawn(function()
                task.wait(1)
                pcall(function()
                    if u36 and u36:FindFirstChild("EBF") then
                        u36.EBF:FireServer()
                    end
                end)
            end)
        end
    end
end)
Groupboxes.Character:AddSlider("Walkspeed", {
	Text = "Walkspeed",
	Min = 16,
	Max = 150,
	Default = 16,
	Rounding = 0,
	Compact = true
})
Groupboxes.Character:AddDropdown("SpeedBypassMode", {
	Text = "Speed Bypass",
	Values = {
		"Default",
		"Boost"
	},
	Default = 1,
	Multi = false
})
Groupboxes.Character:AddToggle("FlyToggle", {
	Text = "Fly",
	Default = false
})
Toggles2.FlyToggle:AddKeyPicker("FlyKeybind", {
	Text = "Fly",
	Default = "F",
	Mode = "Toggle",
	SyncToggleState = true
})
Groupboxes.Character:AddSlider("FlySpeed", {
	Text = "Fly Speed",
	Min = 25,
	Max = 150,
	Default = 60,
	Rounding = 0,
	Compact = true
})
Groupboxes.Character:AddDivider()
Groupboxes.Character:AddToggle("RemoveAcceleration", {
	Text = "Remove Acceleration",
	Default = false,
	Tooltip = "Removes 'sliding' when moving at high speeds"
})
Groupboxes.Character:AddDivider()
Groupboxes.Character:AddToggle("EnableCharacterJump", {
	Text = "Enable Jumping",
	Default = false,
	Tooltip = "Enables the doors jumping feature"
})
Groupboxes.Character:AddToggle("InfiniteJumps", {
	Text = "Infinite Jump",
	Default = false,
	Tooltip = "Allows you to jump mid-air"
})
Groupboxes.Character:AddDivider()
Groupboxes.Character:AddToggle("EnableCharacterSlide", {
	Text = "Enable Sliding",
	Default = false,
	Tooltip = "Enables the doors sliding feature (only at speeds above 20)"
})
Groupboxes.Character:AddToggle("Noclip", {
	Text = "Noclip",
	Default = false,
	Tooltip = "Allows you to go through walls (rubberbands you)"
})
Toggles2.Noclip:AddKeyPicker("NoclipKeybind", {
	Text = "Noclip",
	Default = "N",
	Mode = "Toggle",
	SyncToggleState = true
})
Groupboxes.Character:AddToggle("Phase", {
	Text = "Phase",
	Default = false,
	Tooltip = "Slowly floats you forward with noclip enabled for no rubberbanding"
})
Toggles2.Phase:AddKeyPicker("PhaseKeybind", {
	Text = "Phase",
	Default = "T",
	Mode = "Toggle",
	SyncToggleState = true
})
Groupboxes.Other:AddToggle("AnticheatManipulation", {
	Text = "Anticheat Manipulation",
	Default = false,
	Tooltip = "Makes your character fly forwards fast to trick the anticheat to send you through walls"
})
Toggles2.AnticheatManipulation:AddKeyPicker("AnticheatManipulationKeybind", {
	Text = "Anticheat Manipulation",
	Default = "B",
	Mode = "Hold",
	SyncToggleState = true
})
Groupboxes.Other:AddToggle("AutoInteract", {
	Text = "Auto Interact",
	Default = false,
	Tooltip = "Auto interacts with prompts near you (you can change it to be a hold keybind)",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Toggles2.AutoInteract:AddKeyPicker("AutoInteractKeybind", {
	Text = "Auto Interact",
	Default = "R",
	Mode = "Toggle",
	SyncToggleState = true
})
Groupboxes.Other:AddDropdown("AutoInteractIgnoreList", {
	Text = "Interact Blacklist",
	Values = {
		"Locks",
		"Glitch Fragments",
		"Dropped Items",
		"Mandrake"
	},
	Default = { "Dropped Items" },
	Multi = true,
	AllowNull = true
})
Groupboxes.Other:AddDivider()
Groupboxes.Other:AddToggle("Godmode", {
	Text = "Godmode",
	Default = false,
	Tooltip = "Moves your hitbox to avoid damage from rush-like entities, be careful when going up/in mid-air"
})
Toggles2.Godmode:AddKeyPicker("GodmodeKeybind", {
	Text = "Godmode",
	Default = "G",
	Mode = "Toggle",
	SyncToggleState = true
})
Groupboxes.Other:AddToggle("NotifyVulnerable", {
	Text = "Notify When Vulnerable",
	Default = false,
	Tooltip = "Notifies you when your character moves 3+ studs upward"
})
Groupboxes.Other:AddToggle("AutoClosetToggle", {
	Text = "Auto Closet",
	Default = false,
	Tooltip = "Automatically hides in a closet next to you when a rush-like entity is near"
})
Toggles2.AutoClosetToggle:AddKeyPicker("AutoClosetKeybind", {
	Text = "Auto Closet",
	Default = "H",
	Mode = "Toggle",
	SyncToggleState = true
})
Groupboxes.Other:AddDropdown("AutoClosetEntityList", {
	Text = "Ignore List",
	Values = {
		"Rush",
		"Ambush",
		"Blitz",
		"A-60",
		"A-120",
		"Glitch Rush",
		"Glitch Ambush"
	},
	Multi = true,
	AllowNull = true
})
Groupboxes.Other:AddDivider()
Groupboxes.Other:AddToggle("DoorReachToggle", {
	Text = "Door Reach",
	Default = false,
	Tooltip = "Increases your door opening reach by your chosen amount"
})
Groupboxes.Other:AddSlider("DoorReachDistance", {
	Text = "Door Reach Distance",
	Min = 15,
	Max = 75,
	Default = 45,
	Rounding = 0,
	Compact = true,
	Suffix = " studs"
})
Fly = {
	Connection = nil,
	Body = nil
}
local function v81()
    if BypassConnection then
        BypassConnection:Disconnect()
        BypassConnection = nil
    end

    local v472 = Character and Character:FindFirstChild("CollisionClone")

    if v472 then
        v472:Destroy()
    end

    if not Toggles2.FlyToggle.Value and Humanoid then
        Humanoid.WalkSpeed = 16
    end

    if Character then
        Character:SetAttribute("SpeedBoost", 0)
        Character:SetAttribute("SpeedBoostBehind", 0)
        Character:SetAttribute("SpeedBoostExtra", 0)
        Character:SetAttribute("SpeedBoostMod", 0)
        Character:SetAttribute("AntiSpeed", nil)
        Character:SetAttribute("Antispeed", nil)
        Character:SetAttribute("SpeedBypass", nil)
    end

    if Humanoid then
        Humanoid:SetAttribute("AntiSpeed", nil)
        Humanoid:SetAttribute("Antispeed", nil)
    end

    pcall(function()
        local v1128 = u36 and u36:FindFirstChild("Crouch")

        if v1128 then
            v1128:FireServer(false, false)
        end
    end)
end
local function v82()
    if SpeedConnection then
        SpeedConnection:Disconnect()
        SpeedConnection = nil
    end
end
local function v83()
    v81()

    if not Toggles2.FlyToggle.Value and Options2.Walkspeed.Value == 16 then
        return
    end

    local v473 = u36 and u36:FindFirstChild("Crouch")

    if u28 and not Character:FindFirstChild("CollisionClone") then
        local clone = u28:Clone()

        clone.Name = "CollisionClone"
        clone.RootPriority = 127
        clone.Anchored = false
        clone.CanCollide = false

        local CollisionCrouch = clone:FindFirstChild("CollisionCrouch")

        if CollisionCrouch then
            CollisionCrouch:Destroy()
        end

        clone.Parent = Character
    end

    BypassConnection = RunService.Heartbeat:Connect(function()
        if _Unloading then
            return
        end

        if not Character or not HumanoidRootPart or not Humanoid then
            v81()

            return
        end

        if v473 then
            v473:FireServer(true, true)
        end

        if not Toggles2.FlyToggle.Value then
            local CollisionClone = Character:FindFirstChild("CollisionClone")

            if CollisionClone then
                CollisionClone.Massless = true
            end
        end
    end)
end
local function v84()
    v82()
    SpeedConnection = RunService.Heartbeat:Connect(function()
        if _Unloading then
            return
        end

        if not Humanoid or not HumanoidRootPart or not Character then
            return
        end

        local WalkspeedValue = Options2.Walkspeed.Value

        if WalkspeedValue == 16 then
            return
        end

        if not BypassConnection then
            v83()
        end

        Character:SetAttribute("AntiSpeed", true)
        Character:SetAttribute("Antispeed", true)
        Character:SetAttribute("SpeedBypass", true)
        Humanoid:SetAttribute("AntiSpeed", true)
        Humanoid:SetAttribute("Antispeed", true)
        Humanoid.WalkSpeed = WalkspeedValue

        local MoveDirection = Humanoid.MoveDirection

        if MoveDirection.Magnitude > 0 then
            local AssemblyLinearVelocity = HumanoidRootPart.AssemblyLinearVelocity

            HumanoidRootPart.AssemblyLinearVelocity = Vector3.new(MoveDirection.X * WalkspeedValue, AssemblyLinearVelocity.Y, MoveDirection.Z * WalkspeedValue)
        end
    end)
end
local function v85()
    v82()
    v81()

    if not Toggles2.FlyToggle.Value and Options2.Walkspeed.Value == 16 then
        return
    end

    SpeedConnection = RunService.Heartbeat:Connect(function()
        if _Unloading then
            return
        end

        if not Humanoid or not HumanoidRootPart or not Character then
            return
        end

        local WalkspeedValue = Options2.Walkspeed.Value

        Character:SetAttribute("SpeedBoost", WalkspeedValue - 16)
        Character:SetAttribute("SpeedBoostBehind", 0)
        Character:SetAttribute("SpeedBoostExtra", 0)

        if u36 and u36:FindFirstChild("Crouch") then
            u36.Crouch:FireServer(true, true)
        end

        if WalkspeedValue ~= 16 then
            Humanoid.WalkSpeed = WalkspeedValue

            local MoveDirection = Humanoid.MoveDirection

            if MoveDirection.Magnitude > 0 then
                local AssemblyLinearVelocity = HumanoidRootPart.AssemblyLinearVelocity

                HumanoidRootPart.AssemblyLinearVelocity = Vector3.new(MoveDirection.X * WalkspeedValue, AssemblyLinearVelocity.Y, MoveDirection.Z * WalkspeedValue)
            end
        end
    end)
end
local function v86()
    if Fly.Connection then
        Fly.Connection:Disconnect()
        Fly.Connection = nil
    end

    if Fly.Body then
        Fly.Body:Destroy()
        Fly.Body = nil
    end

    if HumanoidRootPart then
        HumanoidRootPart.AssemblyLinearVelocity = Vector3.zero
    end

    if Humanoid then
        Humanoid.PlatformStand = false
    end
end
function u43()
    v86()

    if not Toggles2.FlyToggle.Value or not HumanoidRootPart then
        v86()

        return
    end

    Fly.Body = Instance.new("BodyVelocity")
    Fly.Body.MaxForce = Vector3.new(9000000000, 9000000000, 9000000000)
    Fly.Body.P = math.huge
    Fly.Body.Parent = HumanoidRootPart
    Fly.Connection = RunService.RenderStepped:Connect(function()
        if _Unloading then
            return
        end

        if not Toggles2.FlyToggle.Value or not Humanoid then
            v86()

            return
        end

        local zero = Vector3.zero
        local MoveDirection = Humanoid.MoveDirection

        if MoveDirection.Magnitude > 0 then
            local LookVector = CurrentCamera.CFrame.LookVector
            local RightVector = CurrentCamera.CFrame.RightVector
            local v1140 = MoveDirection:Dot(LookVector)
            local v1141 = MoveDirection:Dot(RightVector)

            zero = LookVector * v1140 + RightVector * v1141
        end

        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            zero += Vector3.yAxis
        end

        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
            zero -= Vector3.yAxis
        end

        if zero.Magnitude > 0 then
            zero = zero.Unit
        end

        Fly.Body.Velocity = zero * Options2.FlySpeed.Value
    end)
end
function u42()
    v82()
    v81()

    if Options2.SpeedBypassMode.Value == "Boost" then
        v85()

        return
    end

    v84()

    if Toggles2.FlyToggle.Value or Options2.Walkspeed.Value ~= 16 then
        v83()
    end
end
Options2.Walkspeed:OnChanged(u42)
Options2.SpeedBypassMode:OnChanged(u42)
Toggles2.FlyToggle:OnChanged(function()
    if Toggles2.FlyToggle.Value then
        u42()
        u43()

        return
    end

    v86()
    u42()
end)
Toggles2.RemoveAcceleration:OnChanged(function(p86)
    for k, v in pairs(PartProperties) do
        k.CustomPhysicalProperties = p86 and CustomPhysics or v
    end
end)
Toggles2.EnableCharacterJump:OnChanged(function(p87)
    if Character then
        Character:SetAttribute("CanJump", not not p87 or OldJump)
    end
end)
Toggles2.EnableCharacterSlide:OnChanged(function(p88)
    if Character then
        Character:SetAttribute("CanSlide", not not p88 or OldSlide)
    end
end)
NoclipConnection = nil
NoclipParts = {}
local function v87()
    TableClear(NoclipParts)

    if not Character then
        return
    end

    for _, descendant in Character:GetDescendants() do
        if descendant:IsA("BasePart") then
            table.insert(NoclipParts, descendant)
        end
    end

    if u28 then
        table.insert(NoclipParts, u28)

        local CollisionCrouch = u28:FindFirstChild("CollisionCrouch")

        if CollisionCrouch then
            table.insert(NoclipParts, CollisionCrouch)
        end
    end

    local CollisionClone = Character:FindFirstChild("CollisionClone")

    if CollisionClone then
        table.insert(NoclipParts, CollisionClone)

        local CollisionCrouch = CollisionClone:FindFirstChild("CollisionCrouch")

        if CollisionCrouch then
            table.insert(NoclipParts, CollisionCrouch)
        end
    end
end
local function v88(p89)
    for _, v in ipairs(NoclipParts) do
        if v then
            v.CanCollide = p89
        end
    end
end
function u45()
    if NoclipConnection then
        NoclipConnection:Disconnect()
        NoclipConnection = nil
    end

    if Toggles2.Noclip.Value then
        v87()
        NoclipConnection = RunService.Stepped:Connect(function()
            v88(false)
        end)

        return
    end

    v88(true)
end
Toggles2.Noclip:OnChanged(u45);
(function()
    t12.IsEyes = workspace:FindFirstChild("Eyes") ~= nil or workspace:FindFirstChild("Lookman") ~= nil
    t12.IsLookman = workspace:FindFirstChild("BackdoorLookman") ~= nil
end)()
workspace.ChildAdded:Connect(function(child)
    local childName = child.Name

    if childName == "Eyes" or childName == "Lookman" then
        t12.IsEyes = true

        return
    end

    if childName == "BackdoorLookman" then
        t12.IsLookman = true
    end
end)
workspace.ChildRemoved:Connect(function(child)
    local childName = child.Name

    if childName == "Eyes" or childName == "Lookman" then
        t12.IsEyes = workspace:FindFirstChild("Eyes") ~= nil or workspace:FindFirstChild("Lookman") ~= nil

        return
    end

    if childName == "BackdoorLookman" then
        t12.IsLookman = workspace:FindFirstChild("BackdoorLookman") ~= nil
    end
end)
local connection = RunService.RenderStepped:Connect(function()
    if _Unloading then
        return
    end

    if t12.InLobby then
        return
    end

    if Options2.FieldOfView then
        CurrentCamera.FieldOfView = Options2.FieldOfView.Value
    end

    if PlayerGui and not t12.SurgeFrame then
        local SurgeVignette = PlayerGui:FindFirstChild("SurgeVignette", true)

        if SurgeVignette then
            t12.SurgeFrame = SurgeVignette

            if Toggles2.RemoveSurge and Toggles2.RemoveSurge.Value then
                SurgeVignette.Name = "SurgeVignette_Disabled"
            end
        end
    end

    if Toggles2.BypassEyes.Value and t12.IsEyes or Toggles2.BypassLookman.Value and t12.IsLookman then
        local _CurrentFloor = CurrentFloor

        if _CurrentFloor == "Fools" or _CurrentFloor == "OldHotel" then
            u36.MotorReplication:FireServer(0, t12.SpoofOffset ~= 200 and -65 or 65, 0, false)

            return
        end

        u36.MotorReplication:FireServer(-650)
    end
end)
TPTranspParts = {}
local function v90()
    TableClear(TPTranspParts)

    if not Character then
        return
    end

    for _, descendant in Character:GetDescendants() do
        if descendant:IsA("BasePart") and (descendant.Name == "Head" or descendant.Parent and descendant.Parent:IsA("Accessory")) then
            table.insert(TPTranspParts, descendant)
        end
    end
end
local function v91()
    task.wait()
    v90()
end
function u46()
    if CameraConnection then
        CameraConnection:Disconnect()
        CameraConnection = nil
    end

    if _TPCharConn then
        _TPCharConn:Disconnect()
        _TPCharConn = nil
    end

    v90()

    if Character then
        _TPCharConn = Character.ChildAdded:Connect(v91)
    end

    local raycastParams = RaycastParams.new()

    raycastParams.FilterType = Enum.RaycastFilterType.Exclude
    CameraConnection = RunService.RenderStepped:Connect(function()
        if _Unloading then
            return
        end

        if not Character then
            return
        end

        raycastParams.FilterDescendantsInstances = { Character }

        if Options2.ThirdPersonOffsetX and Options2.ThirdPersonOffsetY and Options2.ThirdPersonOffsetZ then
            local cFrame = CFrame.new(Options2.ThirdPersonOffsetX.Value, Options2.ThirdPersonOffsetY.Value, Options2.ThirdPersonOffsetZ.Value)

            if Toggles2.ThirdPerson.Value then
                local v1143 = (CurrentCamera.CFrame * cFrame).Position - CurrentCamera.CFrame.Position
                local v1144 = workspace:Spherecast(CurrentCamera.CFrame.Position, 0.2, v1143, raycastParams)

                if Toggles2.ThirdPersonWallCheck.Value and v1144 and v1144.Instance.CanCollide then
                    local v1145 = CurrentCamera.CFrame.Position + v1143.Unit * v1144.Distance

                    CurrentCamera.CFrame = CFrame.new(v1145, v1145 + CurrentCamera.CFrame.LookVector)
                else
                    CurrentCamera.CFrame = CurrentCamera.CFrame * cFrame
                end
            end
        end

        for _, v in ipairs(TPTranspParts) do
            v.Transparency = not Toggles2.ThirdPerson.Value and 1 or 0
            v.LocalTransparencyModifier = not Toggles2.ThirdPerson.Value and 1 or 0
        end
    end)
end
local t23 = {
	Saved = {},
	ActiveThreats = {},
	ThreatConns = {},
	DescendantConn = nil,
	Cache = {},
	Connection = nil,
	CreatedClone = false,
	ThreatNames = {
		RushMoving = true,
		Rush = true,
		AmbushMoving = true,
		Ambush = true,
		A60 = true,
		A60Moving = true,
		["A-60"] = true,
		BackdoorRush = true,
		BlitzMoving = true,
		Blitz = true,
		["RNIUSHCG=="] = true,
		GlitchRush = true,
		AR0xMBUSH = true,
		GlitchAmbush = true,
		FrozenAmbush = true,
		CustomEntity = true
	}
}
VulnerableLastY = nil
VulnerableNotifCooldown = 0
local function v93()
    t23.Cache.LowerTorso = Character and Character:FindFirstChild("LowerTorso")
    t23.Cache.Root = t23.Cache.LowerTorso and t23.Cache.LowerTorso:FindFirstChild("Root")
    t23.Cache.Collision = Character and Character:FindFirstChild("Collision")
    t23.Cache.CollisionCrouch = t23.Cache.Collision and t23.Cache.Collision:FindFirstChild("CollisionCrouch")
    t23.Cache.CollisionPart = Character and Character:FindFirstChild("CollisionPart")
    t23.Cache.CollisionPartCrouch = t23.Cache.CollisionPart and t23.Cache.CollisionPart:FindFirstChild("CollisionCrouch")
end
local function v94()
    t23.Saved.HipHeight = Humanoid and Humanoid.HipHeight or nil

    if t23.Cache.Root then
        t23.Saved.RootC1 = t23.Cache.Root.C1
    end

    if t23.Cache.Collision and HumanoidRootPart then
        t23.Saved.CollisionOffset = t23.Cache.Collision.Position - HumanoidRootPart.Position
        t23.Saved.CollisionCanCollide = t23.Cache.Collision.CanCollide
    end

    if t23.Cache.CollisionCrouch and HumanoidRootPart then
        t23.Saved.CollisionCrouchOffset = t23.Cache.CollisionCrouch.Position - HumanoidRootPart.Position
        t23.Saved.CollisionCrouchCanCollide = t23.Cache.CollisionCrouch.CanCollide
    end

    if t23.Cache.CollisionPart and HumanoidRootPart then
        t23.Saved.CollisionPartOffset = t23.Cache.CollisionPart.Position - HumanoidRootPart.Position
        t23.Saved.CollisionPartCanCollide = t23.Cache.CollisionPart.CanCollide
    end

    if t23.Cache.CollisionPartCrouch and HumanoidRootPart then
        t23.Saved.CollisionPartCrouchOffset = t23.Cache.CollisionPartCrouch.Position - HumanoidRootPart.Position
        t23.Saved.CollisionPartCrouchCanCollide = t23.Cache.CollisionPartCrouch.CanCollide
    end
end
local function v95()
    for _, v in pairs(t23.ThreatConns) do
        v:Disconnect()
    end

    t23.ThreatConns = {}
    t23.ActiveThreats = {}

    local Saved = t23.Saved

    t23.Saved = {}
    v93()
    t12.SpoofOffset = 0

    if t23.CreatedClone then
        pcall(function()
            local v1148 = Character and Character:FindFirstChild("CollisionClone")

            if v1148 then
                v1148:Destroy()
            end
        end)
        t23.CreatedClone = false
    end

    if t23.Cache.Collision and HumanoidRootPart then
        if Saved.CollisionOffset then
            t23.Cache.Collision.Position = HumanoidRootPart.Position + Vector3.new(Saved.CollisionOffset.X, math.max(Saved.CollisionOffset.Y, 0.18), Saved.CollisionOffset.Z)
        end

        if Saved.CollisionCanCollide ~= nil then
            t23.Cache.Collision.CanCollide = Saved.CollisionCanCollide
        end

        if t23.Cache.CollisionCrouch and Saved.CollisionCrouchOffset then
            t23.Cache.CollisionCrouch.Position = HumanoidRootPart.Position + Vector3.new(Saved.CollisionCrouchOffset.X, math.max(Saved.CollisionCrouchOffset.Y, 0.18), Saved.CollisionCrouchOffset.Z)
        end

        if t23.Cache.CollisionCrouch and Saved.CollisionCrouchCanCollide ~= nil then
            t23.Cache.CollisionCrouch.CanCollide = Saved.CollisionCrouchCanCollide
        end
    end

    if t23.Cache.CollisionPart and HumanoidRootPart then
        if Saved.CollisionPartOffset then
            t23.Cache.CollisionPart.Position = HumanoidRootPart.Position + Vector3.new(Saved.CollisionPartOffset.X, math.max(Saved.CollisionPartOffset.Y, 0.18), Saved.CollisionPartOffset.Z)
        end

        if Saved.CollisionPartCanCollide ~= nil then
            t23.Cache.CollisionPart.CanCollide = Saved.CollisionPartCanCollide
        end

        if t23.Cache.CollisionPartCrouch and Saved.CollisionPartCrouchOffset then
            t23.Cache.CollisionPartCrouch.Position = HumanoidRootPart.Position + Vector3.new(Saved.CollisionPartCrouchOffset.X, math.max(Saved.CollisionPartCrouchOffset.Y, 0.18), Saved.CollisionPartCrouchOffset.Z)
        end

        if t23.Cache.CollisionPartCrouch and Saved.CollisionPartCrouchCanCollide ~= nil then
            t23.Cache.CollisionPartCrouch.CanCollide = Saved.CollisionPartCrouchCanCollide
        end
    end

    if Humanoid and Saved.HipHeight then
        Humanoid.HipHeight = math.max(Saved.HipHeight, 2)
    end

    if t23.Cache.Root and Saved.RootC1 then
        t23.Cache.Root.C1 = Saved.RootC1
    end

    if HumanoidRootPart then
        HumanoidRootPart.CFrame = HumanoidRootPart.CFrame * CFrame.new(0, 2.346, 0)
    end
end
local function v96()
    if not Character or not HumanoidRootPart then
        return
    end

    if t23.Cache.Root and t23.Saved.RootC1 then
        t23.Cache.Root.C1 = t23.Saved.RootC1 * CFrame.new(0, -2.346, 0)
    end

    local v504 = CurrentFloor and (CurrentFloor:find("Fools") or CurrentFloor == "OldHotel")

    t12.SpoofOffset = 0

    if v504 then
        if t23.Cache.Collision then
            t23.Cache.Collision.Position = HumanoidRootPart.Position + Vector3.new(0, 300, 0)
            t23.Cache.Collision.CanCollide = false
        end

        if t23.Cache.CollisionPart then
            t23.Cache.CollisionPart.Position = HumanoidRootPart.Position + Vector3.new(0, 300, 0)
            t23.Cache.CollisionPart.CanCollide = false
        end

        t12.SpoofOffset = 200
    else
        local v505 = u36 and u36:FindFirstChild("Crouch")

        if v505 then
            v505:FireServer(true, true)
        end

        if t23.Cache.Collision then
            t23.Cache.Collision.Position = HumanoidRootPart.Position + Vector3.new(0, 5, 0)
            t23.Cache.Collision.CanCollide = false
        end

        if t23.Cache.CollisionCrouch then
            t23.Cache.CollisionCrouch.Position = HumanoidRootPart.Position + Vector3.new(0, 1.5, 0)
            t23.Cache.CollisionCrouch.CanCollide = false
        end

        if t23.Cache.CollisionPart then
            t23.Cache.CollisionPart.Position = HumanoidRootPart.Position + Vector3.new(0, 5, 0)
            t23.Cache.CollisionPart.CanCollide = false
        end

        if t23.Cache.CollisionPartCrouch then
            t23.Cache.CollisionPartCrouch.Position = HumanoidRootPart.Position + Vector3.new(0, 1.5, 0)
            t23.Cache.CollisionPartCrouch.CanCollide = false
        end
    end

    local v506 = Character and Character:FindFirstChild("CollisionClone")

    if not v506 and not v504 and t23.Cache.Collision then
        v506 = t23.Cache.Collision:Clone()
        v506.Name = "CollisionClone"
        v506.Massless = true
        v506.Parent = Character
        t23.CreatedClone = true
    end

    if v506 then
        v506.CollisionGroup = t23.Cache.Collision and t23.Cache.Collision.CollisionGroup or v506.CollisionGroup

        if not v504 then
            local v507 = t23.Saved.CollisionOffset or Vector3.new(0, 0.18, 0)
            local v508 = t23.Saved.CollisionCrouchOffset or Vector3.new(0, -0.982, 0)

            v506.Position = HumanoidRootPart.Position + Vector3.new(v507.X, v507.Y, v507.Z)
            v506.CanCollide = true

            local CollisionCrouch = v506:FindFirstChild("CollisionCrouch")

            if CollisionCrouch then
                CollisionCrouch.CollisionGroup = t23.Cache.CollisionCrouch and t23.Cache.CollisionCrouch.CollisionGroup or CollisionCrouch.CollisionGroup
                CollisionCrouch.Position = HumanoidRootPart.Position + Vector3.new(v508.X, v508.Y, v508.Z)
                CollisionCrouch.CanCollide = false

                return
            end
        elseif not t23.CreatedClone then
            v506.CanCollide = false

            local CollisionCrouch = v506:FindFirstChild("CollisionCrouch")

            if CollisionCrouch then
                CollisionCrouch.CanCollide = false
            end
        end
    end
end
local function v97()
    if not Character or (not Humanoid or not HumanoidRootPart) then
        return
    end

    v93()
    v94()

    if not CurrentFloor or not CurrentFloor:find("Fools") and CurrentFloor ~= "OldHotel" then
        HumanoidRootPart.CFrame = HumanoidRootPart.CFrame * CFrame.new(0, -2.346, 0)
    end

    Humanoid.HipHeight = 0.05
    v96()
end
local function v98(p90)
    if not t23.ThreatNames[p90.Name] then
        return
    end

    if t23.ActiveThreats[p90] then
        return
    end

    local v512 = next(t23.ActiveThreats) == nil

    t23.ActiveThreats[p90] = true

    local connection3 = p90.AncestryChanged:Connect(function(_, parent)
        if not parent then
            t23.ActiveThreats[p90] = nil

            if t23.ThreatConns[p90] then
                t23.ThreatConns[p90]:Disconnect()
                t23.ThreatConns[p90] = nil
            end

            if next(t23.ActiveThreats) == nil then
                v95()
            end
        end
    end)

    t23.ThreatConns[p90] = connection3

    if v512 then
        v97()
    end
end
function u48()
    if t23.Connection then
        t23.Connection:Disconnect()
        t23.Connection = nil
    end

    if t23.DescendantConn then
        t23.DescendantConn:Disconnect()
        t23.DescendantConn = nil
    end

    v93()
    v95()

    if not Toggles2.Godmode.Value or not Humanoid or not HumanoidRootPart then
        return
    end

    t23.DescendantConn = workspace.DescendantAdded:Connect(v98)

    for _, descendant in workspace:GetDescendants() do
        if t23.ThreatNames[descendant.Name] then
            v98(descendant)
        end
    end

    t23.Connection = RunService.Heartbeat:Connect(function()
        if _Unloading then
            return
        end

        if not Humanoid or not Humanoid.Parent or not HumanoidRootPart or not HumanoidRootPart.Parent then
            u48()

            return
        end

        if not Toggles2 or not Toggles2.Godmode or not Toggles2.Godmode.Value then
            u48()

            return
        end

        if next(t23.ActiveThreats) ~= nil and t23.Cache then
            v96()
        end
    end)
end
Toggles2.Godmode:OnChanged(u48)
Phase = {
	Connection = nil,
	Body = nil,
	Parts = {},
	CharConn = nil
}
local function v99()
    TableClear(Phase.Parts)

    if not Character then
        return
    end

    for _, descendant in Character:GetDescendants() do
        if descendant:IsA("BasePart") then
            table.insert(Phase.Parts, descendant)
        end
    end
end
function u47()
    if Phase.Connection then
        Phase.Connection:Disconnect()
        Phase.Connection = nil
    end

    if Phase.Body then
        Phase.Body:Destroy()
        Phase.Body = nil
    end

    if Phase.CharConn then
        Phase.CharConn:Disconnect()
        Phase.CharConn = nil
    end

    if Toggles2.Phase.Value then
        v99()

        if Character then
            Phase.CharConn = Character.ChildAdded:Connect(function()
                task.wait()
                v99()
            end)
        end

        Phase.Connection = RunService.Stepped:Connect(function()
            if not Character then
                return
            end

            for _, v in ipairs(Phase.Parts) do
                if v then
                    v.CanCollide = false
                end
            end

            if u28 then
                u28.CanCollide = false

                local CollisionCrouch = u28:FindFirstChild("CollisionCrouch")

                if CollisionCrouch then
                    CollisionCrouch.CanCollide = false
                end
            end

            local CollisionClone = Character:FindFirstChild("CollisionClone")

            if CollisionClone then
                CollisionClone.CanCollide = false

                local CollisionCrouch = CollisionClone:FindFirstChild("CollisionCrouch")

                if CollisionCrouch then
                    CollisionCrouch.CanCollide = false
                end
            end

            if HumanoidRootPart then
                Phase.Body.Velocity = HumanoidRootPart.CFrame.LookVector * 2.25
            end
        end)
        Phase.Body = Instance.new("BodyVelocity")
        Phase.Body.MaxForce = Vector3.new(9000000000, 9000000000, 9000000000)
        Phase.Body.Velocity = HumanoidRootPart and HumanoidRootPart.CFrame.LookVector * 2.25 or Vector3.zero

        if HumanoidRootPart then
            Phase.Body.Parent = HumanoidRootPart

            return
        end
    elseif not Toggles2.Noclip.Value then
        v87()
        v88(true)
    end
end
Toggles2.Phase:OnChanged(u47)
Toggles2.AnticheatManipulation:OnChanged(function(p92)
    if AnticheatConnection then
        AnticheatConnection:Disconnect()
        AnticheatConnection = nil
    end

    if p92 then
        AnticheatConnection = RunService.Heartbeat:Connect(function()
            if _Unloading then
                return
            end

            if not Character or not HumanoidRootPart then
                return
            end

            local v1156 = LocalPlayer:GetAttribute("CurrentFloor") or ""

            if v1156 ~= "Fools" and v1156 ~= "OldHotel" then
                Character:PivotTo(Character:GetPivot() * CFrame.new(0, 0, 750))
            end
        end)
    end
end)
local t24 = {
	Connection = nil,
	LastFire = 0,
	Debounce = false,
	Blacklist = {
		HidePrompt = true,
		RiftPrompt = true,
		StarRiftPrompt = true,
		InteractPrompt = true,
		ClimbPrompt = true,
		DonatePrompt = true,
		DialoguePrompt = true,
		RevivePrompt = true,
		EnterPrompt = true,
		AnimatePrompt = true,
		ToolEventPrompt = true,
		Prompt = true,
		PropPrompt = true
	}
}
HidingSpotNames = {
	Wardrobe = true,
	Backdoor_Wardrobe = true,
	Toolshed = true,
	RetroWardrobe = true,
	["Wardrobe-FOOLS26"] = true,
	Locker_Large = true,
	Rooms_Locker = true,
	Rooms_Locker_Fridge = true,
	Bed = true,
	Double_Bed = true,
	CircularVent = true,
	Dumpster = true
}
LockPromptNames = {
	UnlockPrompt = true,
	SkullPrompt = true,
	LockPrompt = true,
	ThingToEnable = true,
	FusesPrompt = true
}
local function v101(p93)
    if t24.Blacklist[p93.Name] then
        return
    end

    if not p93 or not p93.Parent then
        return
    end

    if t24.Debounce then
        return
    end

    if LockPromptNames[p93.Name] or (p93.Parent and p93.Parent:GetAttribute("Locked") == true or p93.Parent and (p93.Parent.Parent and (p93.Parent.Parent.Name == "Locker_Small_Locked" and p93.Name == "ActivateEventPrompt"))) then
        if Options2.AutoInteractIgnoreList.Value.Locks then
            return
        end

        local v520 = false

        for _, v in ipairs({
			"Key",
			"GeneratorFuse",
			"KeyBackdoor",
			"KeyElectrical",
			"KeyIron",
			"Lockpick",
			"SkeletonKey",
			"Shears",
			"Multitool"
		}) do
            if Functions.HasItem(v, true) then
                v520 = true

                break
            end
        end

        for _, v in ipairs({
			"Key",
			"GeneratorFuse",
			"KeyElectrical",
			"KeyIron"
		}) do
            if Functions.HasItem(v) then
                v520 = true

                break
            end
        end

        if not v520 then
            return
        end
    end

    if HidingSpotNames[p93.Parent.Name] then
        return
    end

    if p93.Parent.Name == "GlitchCube" and Options2.AutoInteractIgnoreList.Value["Glitch Fragments"] then
        return
    end

    local p93Parent = p93.Parent

    if Options2.AutoInteractIgnoreList.Value.Mandrake and p93Parent and p93Parent.Name == "Hole" and p93Parent.Parent and p93Parent.Parent.Name == "MandrakeHole" and p93Parent.Parent.Parent and p93Parent.Parent.Parent.Name == "Mandrakes" then
        return
    end

    local Drops = workspace:FindFirstChild("Drops")

    if Drops and p93:IsDescendantOf(Drops) and Options2.AutoInteractIgnoreList.Value["Dropped Items"] then
        return
    end

    if p93.Parent.Name == "TrackLever" then
        return
    end

    if p93.Name == "ActivateEventPrompt" then
        if p93.ActionText == "Close" then
            return
        end

        if p93.Parent.Name == "Padlock" or p93.Parent.Name == "MinesAnchor" then
            return
        end
    end

    if p93.Parent.Name == "LeverForGate" and p93:GetAttribute("Interactions") then
        return
    end

    local ParentParent = p93.Parent.Parent

    if ParentParent and (ParentParent.Name == "DoorFake" or ParentParent.Name == "FakeDoor") then
        return
    end

    if p93.Parent:GetAttribute("JeffShop") then
        return
    end

    if p93:GetAttribute("AutoInteractIgnore") then
        return
    end

    if p93.Name == "PushPrompt" then
        return
    end

    if (p93.Parent.Name == "LibraryHintPaper" or p93.Parent.Name == "PickupItem") and (Functions.HasItem("LibraryHintPaper") or Functions.HasItem("LibraryHintPaperHard")) then
        return
    end

    Functions.ForceFirePrompt(p93)
    t24.Debounce = true

    if v39() == "OldHotel" then
        task.wait()
    end

    t24.Debounce = false
end
Toggles2.AutoInteract:OnChanged(function(p94)
    if t24.Connection then
        t24.Connection:Disconnect()
        t24.Connection = nil
    end

    if p94 then
        t24.Connection = RunService.Heartbeat:Connect(function()
            if _Unloading then
                return
            end

            if (not Toggles2 or (not Toggles2.AutoInteract or not Toggles2.AutoInteract.Value)) and (not Options2 or (not Options2.AutoInteractKeybind or not Options2.AutoInteractKeybind:GetState())) then
                return
            end

            local timestamp = tick()

            if timestamp - t24.LastFire < 0.1 then
                return
            end

            t24.LastFire = timestamp

            for _, v in ipairs(_TrackedPrompts) do
                local ParentRoom = v:GetAttribute("ParentRoom")

                if (not ParentRoom or tonumber(ParentRoom) == tonumber(LocalPlayer:GetAttribute("CurrentRoom"))) and v.Parent and ((v.Parent:IsA("BasePart") or v.Parent:IsA("Model")) and ((if not v.Parent:IsA("BasePart") then LocalPlayer:DistanceFromCharacter(v.Parent:GetPivot().Position) else LocalPlayer:DistanceFromCharacter(v.Parent.Position))) <= (v:GetAttribute("MaxActivationDistance_Old") or v.MaxActivationDistance) + 5) and (v.Enabled or v.Name == "LongPushPrompt" or v.Name == "BigPropPrompt") then
                    task.spawn(v101, v)
                end
            end
        end)
    end
end)
function u44()
    if InfiniteJumpConnection then
        InfiniteJumpConnection:Disconnect()
        InfiniteJumpConnection = nil
    end

    if not Toggles2.InfiniteJumps.Value then
        return
    end

    InfiniteJumpConnection = UserInputService.InputBegan:Connect(function(input)
        if input.KeyCode == Enum.KeyCode.Space and Humanoid then
            Humanoid:ChangeState(Enum.HumanoidStateType.Jumping)
        end
    end)
end
Toggles2.InfiniteJumps:OnChanged(u44)
ESPCategories = {
	Entities = {},
	Doors = {},
	Objectives = {},
	Items = {},
	Chests = {},
	HidingSpots = {},
	Ladders = {}
}
ESPBlacklist = {}
ESPCleanup = {}
ESPConnections = {}
function Functions.AddESP(p95, p96)
    local Object = p95.Object

    if not Object then
        return
    end

    if ESPBlacklist[Object] then
        return
    end

    if p96 then
        local num = tonumber(LocalPlayer:GetAttribute("CurrentRoom"))
        local num3 = tonumber(Object:GetAttribute("ParentRoom"))

        if num3 == num or TableFind(ESPCategories.Doors, Object) and num3 == num + 1 then
            v15:AddESP(p95)
        end

        local connection4 = LocalPlayer:GetAttributeChangedSignal("CurrentRoom"):Connect(function()
            if v15.ColorTable and v15.ColorTable[Object] then
                p95.Color = v15.ColorTable[Object]
            end

            local num4 = tonumber(LocalPlayer:GetAttribute("CurrentRoom"))
            local num5 = tonumber(Object:GetAttribute("ParentRoom"))

            if num5 == num4 or TableFind(ESPCategories.Doors, Object) and num5 == num4 + 1 then
                if v15 then
                    v15:AddESP(p95)

                    for _, v in ipairs({
						"Entities",
						"Doors",
						"Objectives",
						"Items",
						"Chests",
						"HidingSpots",
						"Ladders"
					}) do
                        local v1166 = v .. "Tracers"

                        if Toggles2[v1166] and Toggles2[v1166].Value and TableFind(ESPCategories[v], Object) then
                            v15:SetTracersEnabled(Object, true)

                            return
                        end
                    end

                    return
                end
            elseif v15 then
                v15:RemoveESP(Object)
                v15:SetTracersEnabled(Object, false)
            end
        end)

        table.insert(Connections, connection4)
        ESPConnections[Object] = connection4
        Object.Destroying:Once(function()
            connection4:Disconnect()

            if v15 then
                v15:RemoveESP(Object)
                v15:SetTracersEnabled(Object, false)
            end

            local v1167 = TableFind(Connections, connection4)

            if v1167 then
                table.remove(Connections, v1167)
            end

            ESPConnections[Object] = nil
        end)

        return
    end

    v15:AddESP(p95)
end
function Functions.RemoveESP(p97)
    local v536 = ESPConnections[p97]

    if v536 then
        v536:Disconnect()
        ESPConnections[p97] = nil

        local v537 = TableFind(Connections, v536)

        if v537 then
            table.remove(Connections, v537)
        end
    end

    if v15 then
        v15:RemoveESP(p97)
        v15:SetTracersEnabled(p97, false)
    end
end
local t25 = {
	Door = true,
	Ladder = true,
	GoldPile = true,
	StardustPickup = true,
	KeyObtain = true,
	ElectricalKeyObtain = true,
	MinesGenerator = true,
	FuseObtain = true,
	LiveHintBook = true,
	LiveBreakerPolePickup = true,
	LibraryHintPaper = true,
	PickupItem = true,
	CringlePresent = true,
	LeverForGate = true,
	MinesGateButton = true,
	GardenGateButton = true,
	MinesAnchor = true,
	WaterPump = true,
	TimerLever = true,
	VineGuillotine = true,
	KeyObtainFake = true,
	GiggleCeiling = true,
	Snare = true,
	GrumbleRig = true,
	Drakobloxxer = true,
	LiveEntityBramble = true,
	Groundskeeper = true,
	Figure = true,
	FigureRig = true,
	FigureRagdoll = true,
	MandrakeLive = true,
	ChestBox = true,
	ChestBoxLocked = true,
	Chest_Vine = true,
	Toolbox = true,
	Toolbox_Locked = true,
	Toolshed_Small = true,
	Locker_Small_Locked = true,
	MouseHole = true,
	Wardrobe = true,
	Backdoor_Wardrobe = true,
	Toolshed = true,
	RetroWardrobe = true,
	["Wardrobe-FOOLS26"] = true,
	Locker_Large = true,
	Rooms_Locker = true,
	Rooms_Locker_Fridge = true,
	Bed = true,
	Double_Bed = true,
	CircularVent = true,
	Dumpster = true,
	Lighter = true,
	Flashlight = true,
	Lockpick = true,
	Vitamins = true,
	Bandage = true,
	StarVial = true,
	StarBottle = true,
	StarJug = true,
	Shakelight = true,
	Straplight = true,
	Bulklight = true,
	Battery = true,
	Candle = true,
	Crucifix = true,
	CrucifixWall = true,
	Glowsticks = true,
	SkeletonKey = true,
	Candy = true,
	ShieldMini = true,
	ShieldBig = true,
	BandagePack = true,
	BatteryPack = true,
	RiftCandle = true,
	LaserPointer = true,
	HolyGrenade = true,
	Shears = true,
	Smoothie = true,
	Cheese = true,
	Bread = true,
	AlarmClock = true,
	RiftSmoothie = true,
	GweenSoda = true,
	GlitchCube = true,
	Scanner = true,
	Bomb = true,
	Knockbomb = true,
	Nanner = true,
	BigBomb = true,
	SnakeBox = true,
	GoldGun = true,
	StopSign = true,
	TipJar = true,
	Lantern = true,
	IronKey = true,
	LotusPetal = true,
	Compass = true,
	LotusPetalPickup = true,
	LanternLitItem = true,
	KeyIron = true,
	IronKeyForCrypt = true,
	LotusHolder = true,
	Multitool = true,
	RiftJar = true,
	AloeVera = true,
	Donut = true,
	Lotus = true,
	BoxingGloves = true,
	Green_Herb = true
}
local t26 = {
	RushMoving = true,
	AmbushMoving = true,
	A60 = true,
	A120 = true,
	Eyes = true,
	Lookman = true,
	BackdoorRush = true,
	BackdoorLookman = true,
	Groundskeeper = true,
	GloombatSwarm = true,
	AR0xMBUSH = true,
	["RNIUSHCG=="] = true,
	MonumentEntity = true,
	JeffTheKiller = true,
	CustomEntity = true,
	FrozenAmbush = true,
	SallyMoving = true,
	Figure = true,
	FigureRig = true,
	FigureRagdoll = true,
	GiggleCeiling = true,
	GrumbleRig = true
}
u49 = {
	Lighter = "Lighter",
	Flashlight = "Flashlight",
	Lockpick = "Lockpicks",
	Vitamins = "Vitamins",
	Bandage = "Bandage",
	StarVial = "Starlight Vial",
	StarBottle = "Starlight Bottle",
	StarJug = "Starlight Barrel",
	Shakelight = "Gummy Flashlight",
	Straplight = "Straplight",
	Bulklight = "Spotlight",
	Battery = "Battery",
	Candle = "Candle",
	Crucifix = "Crucifix",
	CrucifixWall = "Crucifix",
	Glowsticks = "Glowstick",
	SkeletonKey = "Skeleton Key",
	Candy = "Candy",
	ShieldMini = "Mini Shield Potion",
	ShieldBig = "Big Shield Potion",
	BandagePack = "Bandage Pack",
	BatteryPack = "Battery Pack",
	RiftCandle = "Moonlight Candle",
	LaserPointer = "Laser Pointer",
	HolyGrenade = "Holy Hand Grenade",
	Shears = "Shears",
	Smoothie = "Smoothie",
	Cheese = "Cheese",
	Bread = "Bread",
	AlarmClock = "Alarm Clock",
	RiftSmoothie = "Moonlight Smoothie",
	GweenSoda = "Gween Soda",
	GlitchCube = "Glitch Fragment",
	Scanner = "Tablet",
	Bomb = "Bomb",
	Knockbomb = "Knockbomb",
	Nanner = "Nanner",
	BigBomb = "Big Bomb",
	SnakeBox = "Hiding Box",
	GoldGun = "Golden Gun",
	StopSign = "Stop Sign",
	TipJar = "Tip Jar",
	Lantern = "Lantern",
	IronKey = "Iron Key",
	LotusPetal = "Lotus Petal",
	Compass = "Compass",
	LotusPetalPickup = "Lotus Petal",
	LanternLitItem = "Lantern",
	KeyIron = "Iron Key",
	IronKeyForCrypt = "Iron Key",
	LotusHolder = "Lotus Petal",
	Multitool = "Multitool",
	RiftJar = "Rift Jar",
	AloeVera = "Aloe Vera",
	Donut = "Donut",
	Lotus = "Lotus",
	BoxingGloves = "Boxing Gloves"
}
function FunctionGetDoorNumber(p98)
    local v539 = tonumber(p98.Parent.Name) or tonumber(p98.Parent.Parent and p98.Parent.Parent.Name)

    if v539 then
        v539 += 1
    end

    if CurrentFloor == "Mines" then
        v539 = (v539 or 0) + 100
    end

    if CurrentFloor == "Backdoor" then
        v539 = (v539 or 0) - 50
    end

    return (tostring(v539))
end
ESPToggleKeys = {
	Entities = "EntitiesESP",
	Doors = "DoorsESP",
	Objectives = "ObjectivesESP",
	Items = "ItemsESP",
	Chests = "ChestsESP",
	HidingSpots = "HidingSpotsESP",
	Ladders = "LaddersESP"
}
local t27 = {
	Entities = "EntitiesESPColor",
	Doors = "DoorsESPColor",
	Objectives = "ObjectivesESPColor",
	Items = "ItemsESPColor",
	Chests = "ChestsESPColor",
	HidingSpots = "HidingSpotsESPColor",
	Ladders = "LaddersESPColor"
}
function GetESPTarget(p99, _)
    return p99
end
local u105
local t28 = {
	PlaySound = nil,
	ShowAchievement = nil,
	ActiveItem = 0,
	SoundAssets = nil,
	SoundInstance = nil,
	ActiveAchievement = nil,
	ActiveEntity = 0
}
local function v107(p101, p102, p103)
    local v545 = ESPToggleKeys[p102]
    local v546 = t27[p102]

    if not Toggles2[v545].Value then
        return
    end

    local Value = Options2[v546].Value
    local v548 = Toggles2.ESPShowText.Value and p103 or ""
    local v549 = GetESPTarget(p101, p102)
    local v550 = p101:GetAttribute("ParentRoom") ~= nil

    Functions.AddESP({
		Object = v549,
		Text = v548,
		Color = Value
	}, v550)
end
local function v108(p104)
    for _, v in ipairs(ESPCategories[p104]) do
        Functions.RemoveESP(GetESPTarget(v, p104))
    end
end
local function v109(p105, p106)
    if not Toggles2.NotifyItems.Value then
        return
    end

    local ItemListValue = Options2.ItemList.Value

    if ItemListValue and next(ItemListValue) ~= nil and not ItemListValue[p105] then
        return
    end

    if t28.ActiveItem >= 6 then
        return
    end

    t28.ActiveItem = t28.ActiveItem + 1
    t28.PlaySound("ItemNotifySound")

    local ItemNotifyStyleValue = Options2.ItemNotifyStyle.Value
    local s3 = ""

    if p106 and p106.PrimaryPart then
        s3 = " (" .. math.floor(LocalPlayer:DistanceFromCharacter(p106.PrimaryPart.Position) + 0.5) .. " m)"
    end

    u2:Notify({
		Title = "An item has spawned",
		Description = p105 .. s3,
		Time = 4
	})

    if ItemNotifyStyleValue ~= "Obsidian" then
        task.spawn(function()
            t28.ShowAchievement("An item has spawned", p105, s3)
        end)
    end

    if Toggles2.ItemChatToggle.Value then
        Functions.SendChat(p105 .. " " .. Options2.ItemChatMessage.Value)
    end

    task.delay(5, function()
        t28.ActiveItem = math.max(0, t28.ActiveItem - 1)
    end)
end
local function v110(p107)
    if not p107 or not p107.Parent then
        return
    end

    if ESPBlacklist[p107] then
        return
    end

    if not t25[p107.Name] and not t26[p107.Name] then
        return
    end

    local p107Name = p107.Name

    if p107Name == "Door" and not p107:GetAttribute("ESPDelayed") then
        p107:SetAttribute("ESPDelayed", true)
        task.spawn(function()
            task.wait(0.25)
            v110(p107)
        end)

        return
    end

    if p107Name == "Door" and p107.Parent and tonumber(p107.Parent.Name) then
        local t29 = {}

        for _, child in p107:GetChildren() do
            if child.Name == "Door" and child:IsA("BasePart") then
                table.insert(t29, child)
            end
        end

        if #t29 == 0 then
            return
        end

        local v564 = "Door " .. FunctionGetDoorNumber(p107)

        if CurrentFloor == "OldHotel" then
            p107:SetAttribute("ParentRoom", (tonumber(p107.Parent.Name)))
            table.insert(ESPCategories.Doors, p107)
            v107(p107, "Doors", v564)

            if not ESPCleanup[p107] then
                ESPCleanup[p107] = p107.Destroying:Once(function()
                    Functions.RemoveESP(p107)
                    ESPCleanup[p107] = nil
                end)
            end
        else
            local u570

            if #t29 == 2 then
                local Model = Instance.new("Model")

                Model.Name = "HighlightModel"
                Instance.new("Humanoid", Model).Name = "HighlightHumanoid"
                Model:SetAttribute("ParentRoom", (tonumber(p107.Parent.Name)))

                for _, v in ipairs(t29) do
                    local Part = Instance.new("Part", Model)

                    Part.Transparency = 0.999
                    Part.Size = v.Size
                    Part.CanCollide = false
                    Part.CFrame = v.CFrame
                    Part.Name = "HighlightPart"
                    Part.Material = Enum.Material.Glass
                    Part:SetAttribute("ParentRoom", (tonumber(p107.Parent.Name)))

                    local WeldConstraint = Instance.new("WeldConstraint", Part)

                    WeldConstraint.Part0 = Part
                    WeldConstraint.Part1 = v
                    WeldConstraint.Enabled = true
                end

                Model.Parent = workspace
                u570 = Model
            else
                local v571 = t29[1]

                u570 = Instance.new("Part")
                u570.Transparency = 0.999
                u570.Size = v571.Size
                u570.CanCollide = false
                u570.CFrame = v571.CFrame
                u570.Name = "HighlightPart"
                u570.Material = Enum.Material.Glass
                u570:SetAttribute("ParentRoom", (tonumber(p107.Parent.Name)))

                local WeldConstraint = Instance.new("WeldConstraint", u570)

                WeldConstraint.Part0 = u570
                WeldConstraint.Part1 = v571
                WeldConstraint.Enabled = true
                Instance.new("Humanoid", p107).Name = "HighlightHumanoid"
                u570.Parent = workspace
            end

            table.insert(ESPCategories.Doors, u570)
            v107(u570, "Doors", v564)

            if not ESPCleanup[u570] then
                ESPCleanup[u570] = p107.Destroying:Once(function()
                    Functions.RemoveESP(u570)
                    ESPCleanup[u570] = nil
                end)
            end
        end

        local timestamp = tick()
        local connection5 = RunService.Heartbeat:Connect(function()
            if _Unloading then
                return
            end

            if p107:FindFirstChild("Door") and p107:FindFirstChild("ClientOpen") and Options2 and Options2.DoorReachDistance and LocalPlayer:DistanceFromCharacter(p107.Door.Position) < Options2.DoorReachDistance.Value and tick() - timestamp > 0.1 and Toggles2 and Toggles2.DoorReachToggle and Toggles2.DoorReachToggle.Value then
                p107.ClientOpen:FireServer()
                timestamp = tick()
            end
        end)
        local Door = p107:FindFirstChild("Door")

        if Door then
            local Open = Door:FindFirstChild("Open")

            if Open then
                Open.Played:Once(function()
                    connection5:Disconnect()
                end)
            end
        end

        p107.Destroying:Once(function()
            connection5:Disconnect()
        end)

        return
    end

    if t26[p107Name] then
        task.spawn(function()
            while not p107.PrimaryPart and p107.Parent do
                for _, child in p107:GetChildren() do
                    if child:IsA("BasePart") then
                        p107.PrimaryPart = child

                        break
                    end
                end

                task.wait()
            end

            task.wait(0.1)

            if not p107.Parent then
                return
            end

            if p107.PrimaryPart and LocalPlayer:DistanceFromCharacter(p107.PrimaryPart.Position) >= 10000 then
                return
            end

            if not p107.PrimaryPart then
                local BasePart = p107:FindFirstChildWhichIsA("BasePart")

                if BasePart then
                    p107.PrimaryPart = BasePart
                end
            end

            if not p107:FindFirstChildOfClass("Humanoid") then
                local Humanoid3 = Instance.new("Humanoid")

                Humanoid3.Name = "HighlightHumanoid"
                Humanoid3.Parent = p107
            end

            local PrimaryPart = p107.PrimaryPart

            if PrimaryPart then
                PrimaryPart.Transparency = 0.999
                PrimaryPart.Material = Enum.Material.Glass
            end

            table.insert(ESPCategories.Entities, p107)

            if v15 and v15.ObjectMaxDistance then
                v15.ObjectMaxDistance[p107] = 10000
            end

            if Toggles2.EntitiesESP.Value then
                local v1173 = if p107Name ~= "FigureRig" and p107Name ~= "FigureRagdoll" then EntityNotifyData[p107Name] and EntityNotifyData[p107Name].Alias or p107Name else "Figure"
                local v1174 = Options2.EntitiesESPAllowed and Options2.EntitiesESPAllowed.Value

                if not v1174 or #v1174 == 0 or v1174[v1173] then
                    local v1175 = p107:GetAttribute("ParentRoom") ~= nil

                    Functions.AddESP({
						Object = p107,
						Text = v1173,
						Color = Options2.EntitiesESPColor.Value
					}, v1175)
                end
            end

            u105(p107)
        end)

        return
    end

    if p107Name == "Ladder" then
        table.insert(ESPCategories.Ladders, p107)

        if CurrentFloor == "Mines" then
            v107(p107, "Ladders", "Ladder")
        end

        return
    end

    local s4, u578

    if p107Name == "GoldPile" and p107:GetAttribute("GoldValue") then
        s4 = "Objectives"
        u578 = "Gold [" .. p107:GetAttribute("GoldValue") .. "]"
    elseif p107Name == "StardustPickup" then
        s4 = "Objectives"
        u578 = "Stardust"
    elseif u49[p107Name] and p107:FindFirstChild("ModulePrompt") then
        s4 = "Items"
        u578 = u49[p107Name]

        if not p107:IsDescendantOf(LocalPlayer) and p107.Parent.Name ~= "Drops" then
            v109(u49[p107Name], p107)
        end
    elseif p107Name == "Green_Herb" then
        s4 = "Items"
        u578 = "Green Herb"
    elseif p107Name == "ChestBox" or p107Name == "ChestBoxLocked" then
        s4 = "Chests"
        u578 = not p107:GetAttribute("Locked") and "Chest" or "Locked Chest"
    elseif p107Name == "Toolbox" or p107Name == "Toolbox_Locked" then
        s4 = "Chests"
        u578 = not p107:GetAttribute("Locked") and "Toolbox" or "Locked Toolbox"
    elseif p107Name == "Chest_Vine" then
        s4 = "Chests"
        u578 = "Vine Chest"
    elseif p107Name == "Toolshed_Small" then
        s4 = "Chests"
        u578 = "Toolshed"
    elseif p107Name == "Locker_Small_Locked" then
        s4 = "Chests"
        u578 = "Locked Locker"
    elseif p107Name == "MouseHole" then
        s4 = "Chests"
        u578 = "Mouse"
    elseif ({
		Wardrobe = true,
		Backdoor_Wardrobe = true,
		Toolshed = true,
		RetroWardrobe = true,
		["Wardrobe-FOOLS26"] = true,
		Locker_Large = true,
		Rooms_Locker = true,
		Rooms_Locker_Fridge = true,
		Bed = true,
		Double_Bed = true,
		CircularVent = true,
		Dumpster = true
	})[p107Name] then
        local t30 = {
			Wardrobe = "Closet",
			Backdoor_Wardrobe = "Closet",
			Toolshed = "Closet",
			RetroWardrobe = "Closet",
			["Wardrobe-FOOLS26"] = "Closet",
			Locker_Large = "Locker",
			Rooms_Locker = "Locker",
			Rooms_Locker_Fridge = "Locker",
			Bed = "Bed",
			Double_Bed = "Double Bed",
			CircularVent = "Vent",
			Dumpster = "Dumpster"
		}

        s4 = "HidingSpots"
        u578 = t30[p107Name]
        Functions.HandleHidingTransparency(p107)
        table.insert(t10.HidingSpots, p107)
    else
        if not ({
			KeyObtain = "Key",
			ElectricalKeyObtain = "Electrical Key",
			MinesGenerator = "Generator",
			FuseObtain = "Fuse",
			LiveHintBook = "Book",
			LiveBreakerPolePickup = "Fuse Breaker",
			LibraryHintPaper = "LibraryPaper",
			PickupItem = "Library Paper",
			CringlePresent = "Present",
			LeverForGate = "Gate Lever",
			MinesGateButton = "Gate Button",
			GardenGateButton = "Gate Button",
			MinesAnchor = "Anchor",
			WaterPump = "Water Pump",
			TimerLever = "Timer Lever",
			VineGuillotine = "Vine Guillotine"
		})[p107Name] then
            return
        end

        local t31 = {
			KeyObtain = "Key",
			ElectricalKeyObtain = "Electrical Key",
			MinesGenerator = "Generator",
			FuseObtain = "Fuse",
			LiveHintBook = "Book",
			LiveBreakerPolePickup = "Fuse Breaker",
			LibraryHintPaper = "Library Paper",
			PickupItem = "Hint Paper",
			CringlePresent = "Present",
			LeverForGate = "Gate Lever",
			MinesGateButton = "Gate Button",
			GardenGateButton = "Gate Button",
			MinesAnchor = "Anchor",
			WaterPump = "Water Pump",
			TimerLever = "Timer Lever",
			VineGuillotine = "Vine Guillotine"
		}

        s4 = "Objectives"
        u578 = t31[p107Name]

        if p107Name == "KeyObtain" then
            task.spawn(function()
                task.wait(2)

                if not p107 or not p107.Parent then
                    return
                end

                table.insert(ESPCategories[s4], p107)
                v107(p107, s4, u578)
            end)
        end

        if p107Name == "TimerLever" then
            task.spawn(function()
                task.wait(1.25)

                if not p107 or not p107.Parent then
                    return
                end

                table.insert(ESPCategories[s4], p107)
                v107(p107, s4, u578)
            end)

            return
        end

        if p107Name == "WaterPump" then
            task.spawn(function()
                local Wheel = p107:FindFirstChild("Wheel")

                if Wheel then
                    local Sound = Wheel:FindFirstChild("Sound")

                    if Sound then
                        Sound.Played:Once(function()
                            p107:SetAttribute("CH_Completed", true)
                        end)
                    end
                end
            end)
        end
    end

    table.insert(ESPCategories[s4], p107)
    v107(p107, s4, u578)
end
local function v111(p108, p109)
    local p108Name = p108.Name

    if p109 == "Doors" then
        local ParentRoom = p108:GetAttribute("ParentRoom")

        return "Door " .. (ParentRoom and tostring(ParentRoom) or "")
    end

    if p109 == "Items" then
        if p108Name == "Green_Herb" then
            return "Green Herb"
        end

        return u49[p108Name] or p108Name
    end

    if p109 == "Ladders" then
        return "Ladder"
    end

    if p109 == "Entities" then
        return EntityNotifyData[p108Name] and EntityNotifyData[p108Name].Alias or p108Name
    end

    local t32 = {
		Wardrobe = "Closet",
		Backdoor_Wardrobe = "Closet",
		Toolshed = "Closet",
		RetroWardrobe = "Closet",
		["Wardrobe-FOOLS26"] = "Closet",
		Locker_Large = "Locker",
		Rooms_Locker = "Locker",
		Rooms_Locker_Fridge = "Locker",
		Bed = "Bed",
		Double_Bed = "Double Bed",
		CircularVent = "Vent",
		Dumpster = "Dumpster"
	}

    if t32[p108Name] then
        return t32[p108Name]
    end

    return ({
		KeyObtain = "Key",
		ElectricalKeyObtain = "Electrical Key",
		MinesGenerator = "Generator",
		FuseObtain = "Generator Fuse",
		LiveHintBook = "Book",
		LiveBreakerPolePickup = "Fuse Breaker",
		LibraryHintPaper = "Library Paper",
		PickupItem = "Library Paper",
		CringlePresent = "Present",
		LeverForGate = "Gate Lever",
		MinesGateButton = "Gate Button",
		GardenGateButton = "Gate Button",
		MinesAnchor = "Anchor",
		WaterPump = "Water Pump",
		TimerLever = "Timer Lever",
		VineGuillotine = "Vine Guillotine",
		ChestBox = "Chest",
		ChestBoxLocked = "Locked Chest",
		Toolbox = "Toolbox",
		Toolbox_Locked = "Locked Toolbox",
		Chest_Vine = "Vine Chest",
		Toolshed_Small = "Toolshed",
		Locker_Small_Locked = "Locked Locker",
		MouseHole = "Mouse",
		GoldPile = "Gold",
		StardustPickup = "Stardust"
	})[p108Name] or p108Name
end
local function v112(p110)
    local Value = Options2[t27[p110]].Value
    local ESPShowTextValue = Toggles2.ESPShowText.Value
    local v589 = Toggles2[p110 .. "Tracers"] and Toggles2[p110 .. "Tracers"].Value
    local v590 = p110 == "Entities" and (Options2.EntitiesESPAllowed and Options2.EntitiesESPAllowed.Value)

    for _, v in ipairs(ESPCategories[p110]) do
        local v593 = true

        if v590 and #v590 > 0 and not v590[EntityNotifyData[v.Name] and EntityNotifyData[v.Name].Alias or v.Name] then
            v593 = false
        end

        if v593 then
            local v594 = GetESPTarget(v, p110)

            if v594 then
                local v595 = v:GetAttribute("ParentRoom") ~= nil

                Functions.AddESP({
					Object = v594,
					Text = ESPShowTextValue and v111(v, p110) or "",
					Color = Value
				}, v595)

                if v589 then
                    v15:SetTracersEnabled(v594, true)
                end
            end
        end
    end
end
local function v113(p111, p112)
    local v598 = ESPToggleKeys[p111]
    local v599 = t27[p111]
    local _ = p111 .. "Tracers"
    local t33 = {
		Entities = Color3.fromRGB(255, 0, 0),
		Doors = Color3.fromRGB(0, 255, 0),
		Objectives = Color3.fromRGB(121, 31, 255),
		Items = Color3.fromRGB(0, 200, 255),
		Chests = Color3.fromRGB(210, 150, 100),
		HidingSpots = Color3.fromRGB(255, 99, 225),
		Ladders = Color3.fromRGB(255, 255, 0)
	}

    Groupboxes.Visuals_ESP:AddToggle(v598, {
		Text = p112,
		Default = false
	})
    Toggles2[v598]:AddColorPicker(v599, {
		Text = p112,
		Default = t33[p111]
	})
    Toggles2[v598]:OnChanged(function(p113)
        if p113 then
            v112(p111)

            return
        end

        v108(p111)
    end)
    Options2[v599]:OnChanged(function(p114)
        for _, v in ipairs(ESPCategories[p111]) do
            local v1182 = GetESPTarget(v, p111)

            if v15.ColorTable and v15.ColorTable[v1182] then
                v15:UpdateObjectColor(v1182, p114)
            end
        end
    end)
end
Groupboxes.Visuals_ESPBox = t4.Visuals:AddLeftTabbox("ESP / Settings")
Groupboxes.Visuals_ESP = Groupboxes.Visuals_ESPBox:AddTab("ESP")
Groupboxes.Visuals_Settings = Groupboxes.Visuals_ESPBox:AddTab("Settings")
v113("Entities", "Entities")
Groupboxes.Visuals_ESP:AddDropdown("EntitiesESPAllowed", {
	Text = "Allowed Entities",
	Values = {
		"Rush",
		"Ambush",
		"A-60",
		"A-120",
		"Eyes",
		"Lookman",
		"Figure",
		"Backdoor Rush",
		"Backdoor Lookman",
		"Groundskeeper",
		"Gloombat Swarm",
		"Glitch Rush",
		"Glitch Ambush",
		"Frozen Ambush",
		"Monument",
		"Jeff the Killer",
		"Sally",
		"Dupe",
		"Giggle",
		"Snare",
		"Gloombat Eggs",
		"Grumble"
	},
	Multi = true,
	AllowNull = true,
	Tooltip = "Only show ESP for selected entities (leave empty to show all)"
})
v113("Doors", "Doors")
v113("Objectives", "Objectives")
v113("Items", "Items")
v113("Chests", "Chests")
v113("HidingSpots", "Hiding Spots")
Groupboxes.Visuals_ESP:AddToggle("PlayersESP", {
	Text = "Players",
	Default = false
})
Toggles2.PlayersESP:AddColorPicker("PlayersESPColor", {
	Text = "Players",
	Default = Color3.fromRGB(255, 255, 255)
})
local t34 = {}
local function v115()
    for _, v in ipairs(t34) do
        v15:RemoveESP(v)

        local v604 = ESPCleanup[v]

        if v604 then
            v604:Disconnect()
            ESPCleanup[v] = nil
        end
    end

    for i = #t34, 1, -1 do
        t34[i] = nil
    end

    if not Toggles2.PlayersESP.Value then
        return
    end

    for _, player in Players:GetPlayers() do
        if player ~= LocalPlayer then
            local Character5 = player.Character

            if Character5 and (Character5:FindFirstChild("HumanoidRootPart") or Character5:FindFirstChildOfClass("BasePart")) then
                local Humanoid4 = Character5:FindFirstChildOfClass("Humanoid")
                local v610 = player.Name .. (Humanoid4 and " [" .. math.floor(Humanoid4.Health) .. " HP]" or "")

                v15:AddESP({
					Object = Character5,
					Text = v610,
					Color = Options2.PlayersESPColor.Value
				})

                local connection6 = Character5.Destroying:Once(function()
                    v15:RemoveESP(Character5)
                    ESPCleanup[Character5] = nil
                end)

                ESPCleanup[Character5] = connection6
                table.insert(t34, Character5)
            end
        end
    end
end
local function v116(p115)
    if p115 == LocalPlayer then
        return
    end

    local connection8 = p115.CharacterAdded:Connect(function(character)
        task.wait(0.5)

        if Toggles2.PlayersESP and (Toggles2.PlayersESP.Value and character and (character:FindFirstChild("HumanoidRootPart") or character:FindFirstChildOfClass("BasePart"))) then
            local Humanoid5 = character:FindFirstChildOfClass("Humanoid")
            local v1185 = p115.Name .. (Humanoid5 and " [" .. math.floor(Humanoid5.Health) .. " HP]" or "")

            v15:AddESP({
				Object = character,
				Text = v1185,
				Color = Options2.PlayersESPColor.Value
			})

            local connection7 = character.Destroying:Once(function()
                v15:RemoveESP(character)
                ESPCleanup[character] = nil
            end)

            ESPCleanup[character] = connection7
            table.insert(t34, character)
        end
    end)

    table.insert(Connections, connection8)
end
Toggles2.PlayersESP:OnChanged(function(p116)
    if p116 then
        v115()

        return
    end

    v115()
end)
Options2.PlayersESPColor:OnChanged(function(p117)
    for _, v in ipairs(t34) do
        v15:UpdateObjectColor(v, p117)
    end
end)
Players.PlayerAdded:Connect(v116)
for _, player in Players:GetPlayers() do
    if player ~= LocalPlayer then
        v116(player)
    end
end
v115()
Groupboxes.Visuals_ESP:AddToggle("ESPFade", {
	Text = "ESP Fade",
	Default = false,
	Tooltip = "Enables smooth fading effect on ESP highlights"
})
Groupboxes.Visuals_ESP:AddSlider("ESPFadeTime", {
	Text = "Fade Time",
	Min = 0,
	Max = 1,
	Default = 0.25,
	Rounding = 2,
	Compact = true,
	Suffix = "s"
})
Groupboxes.Visuals_ESP:AddToggle("ESPArrowsToggle", {
	Text = "Enable Arrows",
	Default = false,
	Tooltip = "Shows arrows that point to off-screen objects"
})
Groupboxes.Visuals_ESP:AddSlider("ESPArrowsRadius", {
	Text = "Arrow Radius",
	Min = 100,
	Max = 500,
	Default = 250,
	Rounding = 0,
	Compact = true,
	Suffix = " px"
})
local t35 = {
	MainTab = nil,
	MainButton = nil,
	NoclipConn = nil
}
LobbyFlyState = {
	Flying = false,
	FlyKeyDown = nil,
	FlyKeyUp = nil,
	BodyGyro = nil,
	BodyVelocity = nil,
	Control = nil,
	LastControl = nil,
	Speed = 0,
	SpeedCoeff = 1
}
local function v120()
    if not LobbyFlyState.Flying then
        return
    end

    LobbyFlyState.Flying = false

    if LobbyFlyState.FlyKeyDown then
        LobbyFlyState.FlyKeyDown:Disconnect()
        LobbyFlyState.FlyKeyDown = nil
    end

    if LobbyFlyState.FlyKeyUp then
        LobbyFlyState.FlyKeyUp:Disconnect()
        LobbyFlyState.FlyKeyUp = nil
    end

    if LobbyFlyState.BodyGyro then
        LobbyFlyState.BodyGyro:Destroy()
        LobbyFlyState.BodyGyro = nil
    end

    if LobbyFlyState.BodyVelocity then
        LobbyFlyState.BodyVelocity:Destroy()
        LobbyFlyState.BodyVelocity = nil
    end

    local v618 = Character and Character:FindFirstChildOfClass("Humanoid")

    if v618 then
        v618.PlatformStand = false
    end

    pcall(function()
        workspace.CurrentCamera.CameraType = Enum.CameraType.Custom
    end)
end
local function v121()
    if LobbyFlyState.Flying then
        return
    end

    if not Character then
        return
    end

    local Humanoid6 = Character:FindFirstChildOfClass("Humanoid")

    if not Humanoid6 then
        return
    end

    local v620 = Humanoid6.RootPart or (Character:FindFirstChild("HumanoidRootPart") or Character:FindFirstChildOfClass("Part"))

    if not v620 then
        return
    end

    v120()

    local _LobbyFlyState = LobbyFlyState

    _LobbyFlyState.Flying = true
    _LobbyFlyState.Control = {
		F = 0,
		B = 0,
		L = 0,
		R = 0,
		Q = 0,
		E = 0
	}
    _LobbyFlyState.LastControl = {
		F = 0,
		B = 0,
		L = 0,
		R = 0,
		Q = 0,
		E = 0
	}
    _LobbyFlyState.Speed = 0
    _LobbyFlyState.SpeedCoeff = (Options2.LobbyFlySpeed and Options2.LobbyFlySpeed.Value or 35) / 50

    local BodyGyro = Instance.new("BodyGyro")

    BodyGyro.P = 90000
    BodyGyro.MaxTorque = Vector3.new(9000000000, 9000000000, 9000000000)
    BodyGyro.CFrame = v620.CFrame
    BodyGyro.Parent = v620

    local BodyVelocity = Instance.new("BodyVelocity")

    BodyVelocity.Velocity = Vector3.new(0, 0, 0)
    BodyVelocity.MaxForce = Vector3.new(9000000000, 9000000000, 9000000000)
    BodyVelocity.Parent = v620
    _LobbyFlyState.BodyGyro = BodyGyro
    _LobbyFlyState.BodyVelocity = BodyVelocity
    _LobbyFlyState.FlyKeyDown = UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end

        local Control = _LobbyFlyState.Control
        local SpeedCoeff = _LobbyFlyState.SpeedCoeff

        if input.KeyCode == Enum.KeyCode.W then
            Control.F = SpeedCoeff
        elseif input.KeyCode == Enum.KeyCode.S then
            Control.B = -SpeedCoeff
        elseif input.KeyCode == Enum.KeyCode.A then
            Control.L = -SpeedCoeff
        elseif input.KeyCode == Enum.KeyCode.D then
            Control.R = SpeedCoeff
        elseif input.KeyCode == Enum.KeyCode.E then
            Control.Q = SpeedCoeff * 2
        elseif input.KeyCode == Enum.KeyCode.Q then
            Control.E = -SpeedCoeff * 2
        end

        pcall(function()
            workspace.CurrentCamera.CameraType = Enum.CameraType.Track
        end)
    end)
    _LobbyFlyState.FlyKeyUp = UserInputService.InputEnded:Connect(function(input, gameProcessed)
        if gameProcessed then
            return
        end

        local Control = _LobbyFlyState.Control

        if input.KeyCode == Enum.KeyCode.W then
            Control.F = 0

            return
        end

        if input.KeyCode == Enum.KeyCode.S then
            Control.B = 0

            return
        end

        if input.KeyCode == Enum.KeyCode.A then
            Control.L = 0

            return
        end

        if input.KeyCode == Enum.KeyCode.D then
            Control.R = 0

            return
        end

        if input.KeyCode == Enum.KeyCode.E then
            Control.Q = 0

            return
        end

        if input.KeyCode == Enum.KeyCode.Q then
            Control.E = 0
        end
    end)
    task.spawn(function()
        while true do
            task.wait()

            if not _LobbyFlyState.Flying then
                break
            end

            local CurrentCamera2 = workspace.CurrentCamera

            Humanoid6.PlatformStand = true

            local Control = _LobbyFlyState.Control
            local LastControl = _LobbyFlyState.LastControl
            local _LobbyFlyStateSpeed = _LobbyFlyState.Speed

            if Control.L + Control.R ~= 0 or Control.F + Control.B ~= 0 or Control.Q + Control.E ~= 0 then
                _LobbyFlyStateSpeed = 50
            elseif Control.L + Control.R == 0 and Control.F + Control.B == 0 and Control.Q + Control.E == 0 and _LobbyFlyStateSpeed ~= 0 then
                _LobbyFlyStateSpeed = 0
            end

            if Control.L + Control.R ~= 0 or Control.F + Control.B ~= 0 or Control.Q + Control.E ~= 0 then
                pcall(function()
                    BodyVelocity.Velocity = (CurrentCamera2.CFrame.LookVector * (Control.F + Control.B) + (CurrentCamera2.CFrame * CFrame.new(Control.L + Control.R, (Control.F + Control.B + Control.Q + Control.E) * 0.2, 0).Position - CurrentCamera2.CFrame.Position)) * _LobbyFlyStateSpeed
                end)

                local F = Control.F
                local B = Control.B
                local L = Control.L
                local R = Control.R

                LastControl.F = F
                LastControl.B = B
                LastControl.L = L
                LastControl.R = R
            elseif Control.L + Control.R == 0 and Control.F + Control.B == 0 and Control.Q + Control.E == 0 and _LobbyFlyStateSpeed ~= 0 then
                pcall(function()
                    BodyVelocity.Velocity = (CurrentCamera2.CFrame.LookVector * (LastControl.F + LastControl.B) + (CurrentCamera2.CFrame * CFrame.new(LastControl.L + LastControl.R, (LastControl.F + LastControl.B + Control.Q + Control.E) * 0.2, 0).Position - CurrentCamera2.CFrame.Position)) * _LobbyFlyStateSpeed
                end)
            else
                pcall(function()
                    BodyVelocity.Velocity = Vector3.new(0, 0, 0)
                end)
            end

            pcall(function()
                BodyGyro.CFrame = CurrentCamera2.CFrame
            end)
            _LobbyFlyState.Speed = _LobbyFlyStateSpeed

            if not _LobbyFlyState.Flying then
                break
            end
        end

        v120()
    end)
end
function t35.MainTab()
    if type(t35.MainTab) ~= "function" then
        return
    end

    local v624 = v54:AddTab("LobbyMain", "house")

    t35.MainTab = v624

    for _, v in ipairs(u2.TabButtons) do
        if v.Label and v.Label.Text == "LobbyMain" then
            v.Label.Text = "Main"
            t35.MainButton = v.Label.Parent
        end
    end

    if t35.MainButton then
        t35.MainButton.Visible = false
    end

    local v627 = v624:AddLeftGroupbox("Stuff")

    v627:AddButton({
		Text = "Free Rooms",
		Callback = function()
        local v1202 = u36 and u36:FindFirstChild("CreateElevator")

        if v1202 then
            v1202:FireServer({
					Mods = { "AdminPanel" },
					Settings = {},
					Destination = "Rooms",
					FriendsOnly = false,
					MaxPlayers = "1"
				})
        end
    end
	})
    v627:AddButton({
		Text = "Free Outdoors",
		Callback = function()
        local v1203 = u36 and u36:FindFirstChild("CreateElevator")

        if v1203 then
            v1203:FireServer({
					Mods = { "AdminPanel" },
					Settings = {},
					Destination = "Garden",
					FriendsOnly = false,
					MaxPlayers = "1"
				})
        end
    end
	})
    v627:AddLabel({
		Text = "<font size=\"16\">This puts you into a game of rooms/outdoors, but with the admin panel modifier, meaning that it is free, but you can not get any achievements/progress.</font>",
		DoesWrap = true
	})
    v627:AddDivider()
    v627:AddToggle("AutoJoinElevator", {
		Text = "Auto Join Elevator",
		Default = false,
		Tooltip = "Automatically joins the selected player's elevator."
	})
    v627:AddDropdown("AutoJoinElevatorTarget", {
		Text = "Target",
		SpecialType = "Player",
		ExcludeLocalPlayer = true,
		Searchable = true
	})
    v627:AddButton({
		Text = "Redeem All Codes",
		Callback = function()
        if t12.RedeemingCodes then
            return
        end

        u2:Notify({
				Title = "Redeeming " .. #t12.CodesList .. " codes.",
				Time = 5
			})
        t12.RedeemingCodes = true

        for _, v in ipairs(t12.CodesList) do
            local v1206 = u36 and u36:FindFirstChild("ShopCode")

            if v1206 then
                v1206:FireServer(v)
            end

            task.wait(5.1)
        end

        t12.RedeemingCodes = false
    end
	})

    local v628 = v624:AddRightGroupbox("Other stuff")

    v628:AddToggle("LobbyNoclip", {
		Text = "Noclip",
		Default = false
	})
    Toggles2.LobbyNoclip:OnChanged(function(p118)
        if t35.NoclipConn then
            t35.NoclipConn:Disconnect()
        end

        if p118 and Character then
            for _, descendant in Character:GetDescendants() do
                if descendant:IsA("BasePart") then
                    descendant.CanCollide = false
                end
            end

            t35.NoclipConn = Character.ChildAdded:Connect(function(child)
                task.wait()

                if child:IsA("BasePart") then
                    child.CanCollide = false
                end
            end)
        end
    end)
    v628:AddToggle("LobbyFly", {
		Text = "Fly",
		Default = false
	})
    Toggles2.LobbyFly:OnChanged(function(p119)
        if p119 then
            v121()

            return
        end

        v120()
    end)
    v628:AddSlider("LobbyFlySpeed", {
		Text = "Fly Speed",
		Min = 25,
		Max = 150,
		Default = 35,
		Rounding = 0,
		Compact = true
	})
    Options2.LobbyFlySpeed:OnChanged(function()
        LobbyFlyState.SpeedCoeff = (Options2.LobbyFlySpeed and Options2.LobbyFlySpeed.Value or 35) / 50
    end)
end
local t36 = {
	"Main",
	"Visuals",
	"Exploits",
	"Miscellaneous",
	"Mines",
	"Settings"
}
local function v123()
    local v629 = v39()

    if v629 == CurrentFloor then
        return
    end

    CurrentFloor = v629

    local v630 = v629 == "" or v629 == "Lobby"
    local v631 = v629 == "Hotel" or v629 == "OldHotel"
    local v632 = v629 == "Mines"
    local v633 = v629 == "Garden"
    local v634 = v629 == "Fools"
    local v635 = v629 == "OldHotel"
    local v636 = v629 == "Rooms"

    if v630 then
        t12.InLobby = true
        t35.MainTab()

        for _, v in ipairs(u2.TabButtons) do
            if v.Label then
                local v639 = false

                for _, v9 in ipairs(t36) do
                    if v9 == v.Label.Text then
                        v639 = true

                        break
                    end
                end

                if v.Label.Text == "Home" then
                    v.Label.Parent.Visible = true
                elseif v639 then
                    v.Label.Parent.Visible = false
                end
            end
        end

        if t35.MainButton then
            t35.MainButton.Visible = true
        end

        if u2.ActiveTab ~= t4.Home and u2.ActiveTab ~= t35.MainTab then
            t4.Home:Show()
        end

        for _, v in pairs(Toggles2) do
            if v.Value then
                v:SetValue(false)
            end
        end

        if not t12.LobbyGuardConnection then
            t12.LobbyGuardConnection = game:GetService("RunService").Heartbeat:Connect(function()
                if _Unloading then
                    return
                end

                if not t12.InLobby then
                    return
                end

                for _, v in ipairs({
					"Godmode",
					"ThirdPerson",
					"Noclip",
					"FlyToggle",
					"Phase",
					"AnticheatManipulation",
					"AutoInteract"
				}) do
                    local v1213 = Toggles2[v]

                    if v1213 and v1213.Value then
                        v1213:SetValue(false)
                    end
                end

                if Toggles2.AutoJoinElevator and Toggles2.AutoJoinElevator.Value then
                    local Character6
                    if typeof(Options2.AutoJoinElevatorTarget.Value) == "Instance" then
                        Character6 = Options2.AutoJoinElevatorTarget.Value.Character
                    elseif typeof(Options2.AutoJoinElevatorTarget.Value) == "string" then
                        Character6 = game:GetService("Players"):FindFirstChild(Options2.AutoJoinElevatorTarget.Value).Character
                    end
                    local v1215 = false
                    local v1216 = workspace:FindFirstChild("Lobby") and workspace.Lobby:FindFirstChild("LobbyElevators")
                    if v1216 then
                        for _, child in v1216:GetChildren() do
                            if Character6 and child and Character6:GetAttribute("InGameElevator") and child:GetAttribute("ID") and child:GetAttribute("ID") == Character6:GetAttribute("InGameElevator") then
                                v1215 = true

                                if u36 and u36:FindFirstChild("ElevatorJoin") then
                                    u36.ElevatorJoin:FireServer(child)
                                end
                            end
                        end
                    end
                    if not v1215 and u36 and u36:FindFirstChild("ElevatorExit") then
                        u36.ElevatorExit:FireServer()
                    end
                end
            end)
        end

        if t4.FoolsHotel then
            t4.FoolsHotel:SetVisible(false)
        end

        if t4.Rooms then
            t4.Rooms:SetVisible(false)
        end

        return
    end

    t12.InLobby = false

    if t12.LobbyGuardConnection then
        t12.LobbyGuardConnection:Disconnect()
        t12.LobbyGuardConnection = nil
    end

    if t35.MainTab then
        if t35.MainButton then
            t35.MainButton.Visible = false
        end

        if Toggles2.LobbyNoclip then
            Toggles2.LobbyNoclip:SetValue(false)
        end

        if Toggles2.LobbyFly then
            Toggles2.LobbyFly:SetValue(false)
        end

        if u2.ActiveTab == t35.MainTab then
            t4.Home:Show()
        end
    end

    if CurrentFloor and CurrentFloor ~= "Lobby" then
        TableClear(ESPCleanup)
        TableClear(ESPBlacklist)

        for _, v in pairs(ESPCategories) do
            TableClear(v)
        end

        for _, v in ipairs(t10.Entities) do
            if v.Name == "FakeDoor" or v.Name == "DoorFake" then
                local Hidden = v:FindFirstChild("Hidden")

                if Hidden then
                    Hidden.CanTouch = true
                end

                local Lock = v:FindFirstChild("Lock")

                if Lock and Lock:FindFirstChild("UnlockPrompt") then
                    Lock.UnlockPrompt.Enabled = true
                end
            end

            if v.Name == "SideroomSpace" then
                local Collision = v:FindFirstChild("Collision")

                if Collision then
                    Collision.CanCollide = false
                    Collision.CanTouch = true
                end
            end
        end

        for _, v in ipairs(t10.Entities) do
            v:Destroy()
        end

        TableClear(t10.Entities)
        TableClear(t10.HidingSpots)
        TableClear(t10.Obstructions)
        TableClear(t10.SeekObstructions)
        TableClear(t10.SeekBridges)
        TableClear(t10.SeekNodes)
        TableClear(t10.SeekDuckBoards)
        TableClear(t10.SeekHighlights)
        TableClear(t10.EyestalkHighlights)
        TableClear(t10.PathLights)
        TableClear(t10.EventTriggers)
    end

    local v653 = not (v631 or v632)

    if Options2.SpamBuyCount then
        pcall(function()
            Options2.SpamBuyCount:SetDisabled(v653)
        end)
    end

    if Options2.SpamBuyItems then
        pcall(function()
            Options2.SpamBuyItems:SetDisabled(v653)
        end)
    end

    if v631 and Options2.SpamBuyItems then
        local t37 = {
			"Lockpick",
			"Lighter",
			"Flashlight",
			"Vitamins"
		}

        if v635 then
            table.insert(t37, "Crucifix")
            table.insert(t37, "Skeleton Key")
        end

        pcall(Options2.SpamBuyItems.SetValues, Options2.SpamBuyItems, t37)
    elseif v632 and Options2.SpamBuyItems then
        pcall(Options2.SpamBuyItems.SetValues, Options2.SpamBuyItems, {
			"Straplight",
			"Bulklight",
			"BandagePack",
			"Lockpick"
		})
    end

    if Toggles2.ShowCrucifix then
        pcall(function()
            Toggles2.ShowCrucifix:SetDisabled(not v635)
        end)
    end

    if Toggles2.ShowSkeletonKey then
        pcall(function()
            Toggles2.ShowSkeletonKey:SetDisabled(not v635)
        end)
    end

    local v655 = not (v631 or type(v629) == "string" and v629:find("Fools"))

    if Toggles2.AutoUnlockPadlockToggle then
        pcall(function()
            Toggles2.AutoUnlockPadlockToggle:SetDisabled(v655)
        end)
    end

    if Toggles2.AutoBreakerBox then
        pcall(function()
            Toggles2.AutoBreakerBox:SetDisabled(v655)
        end)
    end

    if Toggles2.AutoLibraryBruteForce then
        pcall(function()
            Toggles2.AutoLibraryBruteForce:SetDisabled(v655)
        end)
    end

    for _, v in ipairs(u2.TabButtons) do
        if v.Label and v.Label.Parent ~= t35.MainButton then
            local LabelText = v.Label.Text

            if LabelText == "Mines" then
                v.Label.Parent.Visible = v632
            else
                local v659 = false

                for _, v10 in ipairs(t36) do
                    if LabelText == v10 then
                        v659 = true

                        break
                    end
                end

                if v659 then
                    v.Label.Parent.Visible = true
                end
            end
        end
    end

    if t4.Mines and not v632 and u2.ActiveTab == t4.Mines then
        t4.Home:Show()
    end

    if t4.FoolsHotel then
        if v634 or v635 then
            t4.FoolsHotel:SetVisible(true)

            if t4.FoolsHotel.TabLabelRef then
                t4.FoolsHotel.TabLabelRef.Text = not v634 and "Hotel-/Fools" or "Fools"
            end

            for i, v in ipairs(Groupboxes.SubfloorsFools.Elements) do
                if i <= 5 then
                    v.Holder.Visible = v634
                end
            end
        else
            if u2.ActiveTab == t4.FoolsHotel then
                t4.Home:Show()
            end

            t4.FoolsHotel:SetVisible(false)
        end
    end

    if t4.Garden then
        if v633 then
            t4.Garden:SetVisible(true)
        else
            if u2.ActiveTab == t4.Garden then
                t4.Home:Show()
            end

            t4.Garden:SetVisible(false)
        end
    end

    if t4.Rooms then
        if v636 then
            t4.Rooms:SetVisible(true)
        else
            if u2.ActiveTab == t4.Rooms then
                t4.Home:Show()
            end

            t4.Rooms:SetVisible(false)
        end
    end

    if Toggles2.LaddersESP and Toggles2.LaddersESP.Value then
        for _, v in ipairs(ESPCategories.Ladders) do
            if v632 then
                v107(v, "Ladders", "Ladder")
            else
                Functions.RemoveESP(v)
            end
        end
    end
end
Groupboxes.SubfloorsRooms = t4.Rooms:AddLeftGroupbox("Rooms")
Groupboxes.SubfloorsRooms:AddToggle("RoomsAutoWalk", {
	Text = "Auto Rooms",
	Default = false,
	Tooltip = "Automatically moves and hides from entities in The Rooms (stops at A-1000)"
})
Groupboxes.SubfloorsRooms:AddSlider("RoomsAutoWalkPathfindTimeout", {
	Text = "Pathfind Timeout",
	Min = 0.5,
	Max = 3,
	Default = 1,
	Rounding = 1
})
Groupboxes.SubfloorsRooms:AddToggle("RoomsAutoWalkIgnoreA60", {
	Text = "Ignore A-60",
	Default = false,
	Tooltip = "Continues to walk if A-60 spawns, enables godmode"
})
Groupboxes.SubfloorsRooms:AddToggle("RoomsAutoWalkShowPathToggle", {
	Text = "Show Path",
	Default = false,
	Tooltip = "Shows the current path of rooms auto-walk"
})
Toggles2.RoomsAutoWalkShowPathToggle:AddColorPicker("RoomsAutoWalkShowPathColor", {
	Text = "Path",
	Default = Color3.fromRGB(0, 255, 0),
	Transparency = 0
})
Groupboxes.SubfloorsRooms:AddToggle("RoomsAutoWalkSpoofFootsteps", {
	Text = "Spoof Footsteps",
	Default = false,
	Tooltip = "Makes it appear as if your character is walking normally",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Groupboxes.SubfloorsRooms:AddDivider()
Groupboxes.SubfloorsRooms:AddToggle("RoomsPickupStardust", {
	Text = "Pickup Stardust",
	Default = false,
	Tooltip = "Automatically pathfinds to stardust in the current room"
})
Groupboxes.SubfloorsRooms:AddToggle("RoomsPickupGold", {
	Text = "Pickup Gold",
	Default = false,
	Tooltip = "Automatically pathfinds to gold piles in the current room"
})
local u124
local u125
Groupboxes.SubfloorsOutdoors = t4.Garden:AddLeftGroupbox("Outdoors")
Groupboxes.SubfloorsOutdoors:AddToggle("NoSurgeDamage", {
	Text = "No Surge Damage",
	Default = false,
	Tooltip = "Prevents Surge from hurting you"
})
Toggles2.NoSurgeDamage:OnChanged(function(p120)
    if not u36 then
        return
    end

    if p120 then
        if FakeEvents.Surge_Real and FakeEvents.Surge_Real.Parent then
            FakeEvents.Surge.Parent = u36
            FakeEvents.Surge_Real.Parent = nil

            return
        end
    elseif FakeEvents.Surge_Real then
        FakeEvents.Surge_Real.Parent = u36
        FakeEvents.Surge.Parent = nil
    end
end)
Groupboxes.SubfloorsOutdoors:AddToggle("RemoveSurge", {
	Text = "Remove Surge",
	Default = false,
	Tooltip = "Prevents Surge from spawning"
})
Toggles2.RemoveSurge:OnChanged(function(p121)
    if t12.SurgeFrame then
        t12.SurgeFrame.Name = not p121 and "SurgeVignette" or "SurgeVignette_Disabled"
    end
end)
Groupboxes.SubfloorsOutdoors:AddToggle("ShowEyestalkPathToggle", {
	Text = "Show Eyestalk Path",
	Default = false,
	Tooltip = "Shows you the correct path in the eyestalk chase"
})
Toggles2.ShowEyestalkPathToggle:AddColorPicker("ShowEyestalkPathColor", {
	Text = "Eyestalk Path",
	Default = Color3.fromRGB(0, 255, 0),
	Transparency = 0
})
Toggles2.ShowEyestalkPathToggle:OnChanged(function(p122)
    u124(t10.EyestalkHighlights, "ShowEyestalkPathColor", p122)
end)
Options2.ShowEyestalkPathColor:OnChanged(function(p123)
    u125(t10.EyestalkHighlights, p123)
end)
t4.Garden:SetVisible(false)
t12.RoomsNodesFolder = Instance.new("Folder")
t12.RoomsNodesFolder.Name = "RoomsPathNodes"
t12.RoomsNodesFolder.Parent = workspace
t12.SeekNodesFolder = Instance.new("Folder")
t12.SeekNodesFolder.Name = "SeekPathNodes"
t12.SeekNodesFolder.Parent = workspace
Toggles2.BypassKillbricks:OnChanged(function(p124)
    for _, descendant in workspace:GetDescendants() do
        if descendant.Name == "Lava" and descendant:IsA("BasePart") then
            descendant.CanTouch = not p124
        end
    end
end)
Toggles2.BypassSeekingWall:OnChanged(function(p125)
    for _, descendant in workspace:GetDescendants() do
        if descendant.Name == "ScaryWall" then
            for _, descendant2 in descendant:GetDescendants() do
                if descendant2:IsA("BasePart") then
                    descendant2.CanTouch = not p125
                    descendant2.CanCollide = not p125
                end
            end
        end
    end
end)
Toggles2.BypassBanana:OnChanged(function(p126)
    for _, descendant in workspace:GetDescendants() do
        if descendant.Name == "BananaPeel" and descendant:IsA("BasePart") then
            descendant.CanTouch = not p126
        end
    end
end)
Toggles2.BypassJeff:OnChanged(function(p127)
    for _, descendant in workspace:GetDescendants() do
        if descendant.Name == "JeffTheKiller" then
            for _, descendant3 in descendant:GetDescendants() do
                if descendant3:IsA("BasePart") then
                    descendant3.CanCollide = not p127
                    descendant3.CanTouch = not p127
                end
            end

            local Humanoid7 = descendant:FindFirstChildOfClass("Humanoid")

            if Humanoid7 then
                Humanoid7.Health = 0
            end
        end
    end
end)
Toggles2.RemoveSeekTrigger:OnChanged(function(p128)
    if p128 and Executor.firetouchinterest then
        for _, v in ipairs(t10.Entities) do
            if v.Name == "TriggerEventCollision" then
                for _, child in v:GetChildren() do
                    if child:IsA("BasePart") and HumanoidRootPart then
                        Executor.firetouchinterest(HumanoidRootPart, child, 0)
                        task.wait()
                        Executor.firetouchinterest(HumanoidRootPart, child, 1)
                    end
                end
            end
        end

        task.spawn(function()
            while Toggles2.RemoveSeekTrigger.Value do
                for _, v in ipairs(t10.Entities) do
                    if v.Name == "TriggerEventCollision" and HumanoidRootPart then
                        for _, child in v:GetChildren() do
                            if child:IsA("BasePart") then
                                Executor.firetouchinterest(HumanoidRootPart, child, 0)
                                task.wait()
                                Executor.firetouchinterest(HumanoidRootPart, child, 1)
                            end
                        end
                    end
                end

                task.wait(0.5)
            end
        end)
    end

    for _, descendant in workspace:GetDescendants() do
        if descendant.Name == "Seeking" or descendant.Name == "SeekTrigger" or descendant.Name == "ChaseStartTrigger" then
            for _, descendant4 in descendant:GetDescendants() do
                if descendant4:IsA("BasePart") then
                    descendant4.CanTouch = not p128
                end
            end
        end
    end
end)
Toggles2.AutoRoomSkip:OnChanged(function(p129)
    if p129 then
        task.spawn(function()
            pcall(function()
                local n8 = 0
                local n9 = 0
                local v1352 = false
                repeat
                    local v1353
                    local n10 = 0
                    for _, child in workspace.CurrentRooms:GetChildren() do
                        local num = tonumber(child.Name)

                        if num and n10 < num then
                            local Door = child:FindFirstChild("Door")

                            if Door then
                                v1353 = Door
                                n10 = num
                            end
                        end
                    end
                    if v1353 then
                        if n8 < n10 then
                            n8 = n10
                            if v1352 then
                                Toggles2.Noclip:SetValue(false)
                                v1352 = false
                            end
                        end

                        local v1359 = v1353:IsA("BasePart") and v1353 or v1353:FindFirstChildWhichIsA("BasePart")

                        if v1359 then
                            game.Players.LocalPlayer.Character:PivotTo(CFrame.new(v1359.Position))
                        end
                    else
                        n9 += 0.25

                        if n9 >= 5 then
                            if not Toggles2.Noclip.Value then
                                Toggles2.Noclip:SetValue(true)
                            end
                            v1352 = true
                            local Character7 = game.Players.LocalPlayer.Character
                            if Character7 and Character7:FindFirstChild("HumanoidRootPart") then
                                Character7.HumanoidRootPart.CFrame = Character7.HumanoidRootPart.CFrame * CFrame.new(0, 0, 10)
                            end
                        end
                    end
                    task.wait(0.25)
                until not Toggles2.AutoRoomSkip.Value
                if v1352 then
                    Toggles2.Noclip:SetValue(false)
                end
            end)
        end)
    end
end)
Toggles2.RemoveFigure:OnChanged(function(p130)
    if p130 and (Executor.isnetworkowner and workspace:FindFirstChild("Figure")) then
        local v697 = v39()

        for _, v in ipairs(t10.Entities) do
            if v.Name == "Figure" or v.Name == "FigureRig" or v.Name == "FigureRagdoll" then
                if v697 == "Mines" then
                    for _, descendant in v:GetDescendants() do
                        if descendant:IsA("BasePart") and pcall(function()
                            return Executor.isnetworkowner(descendant)
                        end) then
                            descendant.Position = Vector3.new(-49999, -49999, -49999)
                        end
                    end
                elseif v697 == "OldHotel" or v697 == "Fools" then
                    for _, descendant in v:GetDescendants() do
                        if descendant:IsA("BasePart") then
                            descendant.CanCollide = false

                            if pcall(function()
                                return Executor.isnetworkowner(descendant)
                            end) then
                                descendant.Position = Vector3.new(math.random(-29999, 29999), math.random(-29999, 29999), math.random(-29999, 29999))
                            end
                        end
                    end
                end
            end
        end
    end

    if p130 then
        local Figure = workspace:FindFirstChild("Figure")

        if Figure then
            for _, descendant in Figure:GetDescendants() do
                if descendant:IsA("BasePart") then
                    descendant.CanCollide = false
                end
            end
        end
    end
end)
Connections.AutoReviveHandler = LocalPlayer:GetAttributeChangedSignal("Alive"):Connect(function()
    if LocalPlayer:GetAttribute("Alive") == false and Toggles2.AutoRevive.Value then
        local v707 = v39()

        if v707 == "Fools" or v707 == "OldHotel" then
            while LocalPlayer:GetAttribute("Alive") ~= true do
                local RemotesFolder = ReplicatedStorage:FindFirstChild("RemotesFolder")

                if RemotesFolder and RemotesFolder:FindFirstChild("Revive") then
                    RemotesFolder.Revive:FireServer()
                end

                task.wait(0.5)
            end
        end
    end
end)
Toggles2.FigureGodmode:OnChanged(function(_)
end)
ObstructionNames = {
	ThingToOpen = "RemoveBasementGate",
	MovingDoor = "RemovePaintingsDoor",
	Wax_Door = "RemoveSkeletonDoor"
}
for k, v in pairs(ObstructionNames) do
    Toggles2[v]:OnChanged(function(p132)
        for _, descendant in workspace:GetDescendants() do
            if descendant.Name == k and descendant:IsA("Model") then
                if p132 then
                    descendant:PivotTo(CFrame.new(-10000, -10000, -10000))
                else
                    local OriginalPosition = descendant:GetAttribute("OriginalPosition")

                    if OriginalPosition then
                        descendant:PivotTo(CFrame.new(unpack(OriginalPosition)))
                    end
                end
            end
        end
    end)
end
Connections.DescendantAddedObstructions = workspace.DescendantAdded:Connect(function(descendant)
    if descendant:IsA("Model") and (descendant.Name == "ThingToOpen" or descendant.Name == "MovingDoor" or descendant.Name == "Wax_Door") then
        local t38 = {
			ThingToOpen = "RemoveBasementGate",
			MovingDoor = "RemovePaintingsDoor",
			Wax_Door = "RemoveSkeletonDoor"
		}

        descendant:SetAttribute("OriginalPosition", { descendant:GetPivot():GetComponents() })

        if Toggles2[t38[descendant.Name]] and Toggles2[t38[descendant.Name]].Value then
            descendant:PivotTo(CFrame.new(-10000, -10000, -10000))
        end
    end
end)
Toggles2.RoomsAutoWalkShowPathToggle:OnChanged(function(p133)
    for _, child in t12.RoomsNodesFolder:GetChildren() do
        if child.Name == "PathNode" then
            child.Transparency = not p133 and 1 or 0.5
        end
    end
end)
Options2.RoomsAutoWalkShowPathColor:OnChanged(function(p134)
    for _, child in t12.RoomsNodesFolder:GetChildren() do
        if child.Name == "PathNode" then
            child.Color = p134
        end
    end
end)
RoomsEntityList = {
	"RushMoving",
	"AmbushMoving",
	"BackdoorRush",
	"A60",
	"A120",
	"CustomEntity",
	"GlitchRush",
	"GlitchAmbush"
}
Functions.RoomsAutoWalk = {}
function Functions.RoomsAutoWalk.GetNearestHidingSpot()
    local t39 = {
		Distance = math.huge,
		Object = nil
	}

    for _, v in ipairs(t10.HidingSpots) do
        if v.PrimaryPart and v:FindFirstChild("HidePrompt") then
            local v725 = LocalPlayer:DistanceFromCharacter(v.PrimaryPart.Position)

            if v725 < t39.Distance and v.PrimaryPart.Position.Y > -10 then
                local HiddenPlayer = v:FindFirstChild("HiddenPlayer", true)

                if HiddenPlayer and not HiddenPlayer.Value then
                    t39.Distance = v725
                    t39.Object = v
                end
            end
        end
    end

    return t39.Object
end
local n11 = 0
local Value
function Functions.RoomsAutoWalk.GetPathfindTarget()
    if not u29 or not CurrentRooms then
        return
    end

    local v727 = CurrentRooms[tostring(u29.Value)]

    if not v727 then
        return
    end

    for _, child in workspace:GetChildren() do
        if not TableFind(RoomsEntityList, child.Name) or not child.PrimaryPart then
            continue
        end

        local PositionY = child.PrimaryPart.Position.Y

        if PositionY > -10 and PositionY < 150 and (child.Name == "A60" and not Toggles2.RoomsAutoWalkIgnoreA60.Value or child.Name ~= "A60") then
            return Functions.RoomsAutoWalk.GetNearestHidingSpot() or v727:FindFirstChild("RoomExit")
        end
    end

    if Toggles2.RoomsAutoWalkIgnoreA60.Value and next(t23.ActiveThreats) ~= nil then
        return v727:FindFirstChild("RoomExit")
    end

    if (Toggles2.RoomsPickupStardust.Value or Toggles2.RoomsPickupGold.Value) and Value ~= u29.Value then
        local t40 = {
			Distance = math.huge,
			Object = nil
		}

        for _, descendant in v727:GetDescendants() do
            if descendant.Name == "GoldPile" and descendant:GetAttribute("GoldValue") and Toggles2.RoomsPickupGold.Value then
                local Position = descendant:GetPivot().Position
                local v735 = LocalPlayer:DistanceFromCharacter(Position)

                if v735 < t40.Distance then
                    t40.Distance = v735
                    t40.Object = descendant
                end
            elseif descendant.Name == "StardustPickup" and Toggles2.RoomsPickupStardust.Value then
                local Position = descendant:GetPivot().Position
                local v737 = LocalPlayer:DistanceFromCharacter(Position)

                if v737 < t40.Distance then
                    t40.Distance = v737
                    t40.Object = descendant
                end
            end
        end

        if t40.Object then
            return t40.Object
        end
    end

    return v727:FindFirstChild("RoomExit")
end
Toggles2.RoomsAutoWalk:OnChanged(function(p135)
    for _, child in t12.RoomsNodesFolder:GetChildren() do
        if child.Name == "PathNode" then
            child:Destroy()
        end
    end

    if p135 and not Toggles2.NoA90Damage.Value then
        Toggles2.NoA90Damage:SetValue(true)
    end
end)
Connections.RoomsAutoWalkHandler = game:GetService("RunService").Heartbeat:Connect(function()
    if _Unloading then
        return
    end
    if not Toggles2.RoomsAutoWalk.Value or CurrentFloor ~= "Rooms" or t12.RoomsAutoWalkActive or not u28 then
        return
    end
    local ok9, result9 = pcall(function()
        return u29 and u29.Value
    end)
    if not ok9 or not result9 or result9 >= 1000 then
        return
    end
    t12.RoomsAutoWalkActive = true
    local v743 = game:GetService("PathfindingService"):CreatePath({
		AgentCanJump = true,
		AgentCanClimb = false,
		WaypointSpacing = 4,
		AgentRadius = 1.5,
		AgentHeight = 1.5,
		Costs = {
			StuckPart = 8
		}
	})
    if Toggles2.RoomsAutoWalkIgnoreA60.Value and not Toggles2.Godmode.Value then
        Toggles2.Godmode:SetValue(true)
    end
    local v744 = Functions.RoomsAutoWalk.GetPathfindTarget()
    if not v744 then
        t12.RoomsAutoWalkActive = false

        return
    end
    local Position
    if v744.Name == "RoomExit" then
        Position = v744.Position
    elseif v744:FindFirstChild("HidePrompt") then
        for _, descendant in v744:GetDescendants() do
            if descendant:IsA("BasePart") then
                descendant.CanCollide = false
            end
        end

        Position = v744.PrimaryPart.Position
    elseif v744.Name == "GoldPile" or v744.Name == "StardustPickup" then
        Position = v744:GetPivot().Position

        if n11 == 0 then
            n11 = tick()
        end
    end
    if v744.Name ~= "GoldPile" and v744.Name ~= "StardustPickup" then
        n11 = 0
    end
    if u28.Anchored and not v744:FindFirstChild("HidePrompt") then
        Character:SetAttribute("Hiding", true)
        u36.CamLock:FireServer()
        Character:SetAttribute("Hiding", false)
    end
    if not u29 or not CurrentRooms then
        t12.RoomsAutoWalkActive = false

        return
    end
    local v748 = CurrentRooms[tostring(u29.Value)]
    if not v748 then
        t12.RoomsAutoWalkActive = false

        return
    end
    if v748:FindFirstChild("Door") then
        v748.Door.Door.CanCollide = false
    end
    if not Position or LocalPlayer:DistanceFromCharacter(Position) >= 750 then
        t12.RoomsAutoWalkActive = false

        return
    end
    local _, result10 = pcall(v743.ComputeAsync, v743, u28.Position, Position)
    if result10 then
        t12.RoomsAutoWalkActive = false

        return
    end
    local Waypoints = v743:GetWaypoints()
    if #Waypoints == 0 then
        local RoomExit = v748:FindFirstChild("RoomExit")

        if RoomExit then
            Humanoid:MoveTo(RoomExit.Position)
        end

        t12.RoomsAutoWalkActive = false

        return
    end
    for _, child in t12.RoomsNodesFolder:GetChildren() do
        if child.Name == "PathNode" then
            child:Destroy()
        end
    end
    for _, v in ipairs(Waypoints) do
        local Part = Instance.new("Part", t12.RoomsNodesFolder)

        Part.Transparency = not Toggles2.RoomsAutoWalkShowPathToggle.Value and 1 or 0.5
        Part.Size = Vector3.one
        Part.Position = v.Position
        Part.Shape = Enum.PartType.Ball
        Part.CanCollide = false
        Part.Anchored = true
        Part.Name = "PathNode"
        Part.Color = Options2.RoomsAutoWalkShowPathColor.Value
        Part.Material = Enum.Material.Neon
    end
    local u758 = false
    for _, v in ipairs(Waypoints) do
        if u758 or not Toggles2.RoomsAutoWalk.Value then
            break
        end

        local u761 = false
        local timestamp = tick()
        local connection9 = game:GetService("RunService").RenderStepped:Connect(function()
            if _Unloading then
                return
            end

            if u758 or not Toggles2.RoomsAutoWalk.Value then
                u761 = true

                return
            end

            local v1223 = Functions.RoomsAutoWalk.GetPathfindTarget()

            if v1223 and v1223:FindFirstChild("HidePrompt") and not v744:FindFirstChild("HidePrompt") then
                u761 = true

                return
            end

            if v744.Name == "GoldPile" and (not v744.Parent or not v744:GetAttribute("GoldValue")) or v744.Name == "StardustPickup" and not v744.Parent then
                u761 = true

                return
            end

            if v744:FindFirstChild("HidePrompt") then
                local HidePrompt = v744:FindFirstChild("HidePrompt")

                if LocalPlayer:DistanceFromCharacter(Position) < HidePrompt.MaxActivationDistance and Character:GetAttribute("Hiding") ~= true then
                    Functions.ForceFirePrompt(HidePrompt)
                end
            elseif v744.Name == "GoldPile" or v744.Name == "StardustPickup" then
                local LootPrompt = v744:FindFirstChild("LootPrompt")

                if LootPrompt and LocalPlayer:DistanceFromCharacter(Position) < LootPrompt.MaxActivationDistance then
                    Functions.ForceFirePrompt(LootPrompt)
                end
            end

            local vector3 = Vector3.new(v.Position.X, HumanoidRootPart.Position.Y, v.Position.Z)

            if LocalPlayer:DistanceFromCharacter(vector3) < 5 then
                u761 = true
            end

            Humanoid:MoveTo(v.Position)
        end)

        while not u761 do
            if tick() - timestamp > Options2.RoomsAutoWalkPathfindTimeout.Value then
                local Part = Instance.new("Part", t12.RoomsNodesFolder)

                Part.Transparency = 1
                Part.Size = Vector3.one
                Part.CFrame = u27.CFrame
                Part.Shape = Enum.PartType.Ball
                Part.CanCollide = false
                Part.Anchored = true
                Part.Name = "StuckPart"
                Instance.new("PathfindingModifier", Part).Label = "StuckPart"
                u758 = true

                break
            end

            if n11 > 0 and tick() - n11 > 5 then
                Value = u29.Value
                n11 = 0
                u758 = true

                break
            end

            task.wait()
        end

        connection9:Disconnect()
        Humanoid:MoveTo(HumanoidRootPart.Position)
    end
    t12.RoomsAutoWalkActive = false
end)
if CurrentRooms then
    Connections.RoomsHandler = CurrentRooms.ChildAdded:Connect(function(child)
        for _, child3 in t12.RoomsNodesFolder:GetChildren() do
            if child3.Name == "StuckPart" then
                child3:Destroy()
            end
        end

        if child:GetAttribute("RawName") and string.find(child:GetAttribute("RawName"), "Eyestalk") then
            local u768
            child:WaitForChild("RoomEntrance", 9000000000)
            child:WaitForChild("RoomExit", 9000000000)
            while not child:GetAttribute("PathFound") do
                if rawget(Toggles2, "ShowEyestalkPathToggle") and Toggles2.ShowEyestalkPathToggle.Value then
                    local v769 = game:GetService("PathfindingService"):CreatePath({
						AgentCanJump = false,
						AgentCanClimb = false,
						WaypointSpacing = 2,
						AgentRadius = 1,
						AgentHeight = 1
					})
                    local _, result11 = pcall(v769.ComputeAsync, v769, HumanoidRootPart.Position, child.RoomExit.Position)

                    if result11 then
                        break
                    end

                    if v769.Status == Enum.PathStatus.Success then
                        child:SetAttribute("PathFound", true)

                        for _, v773 in v769:GetWaypoints() do
                            (function(p136)
                                local Part = Instance.new("Part")

                                Part.Size = Vector3.one
                                Part.Transparency = 1
                                Part.Parent = t12.SeekNodesFolder
                                Part.Anchored = true
                                Part.Position = p136
                                Part.CanCollide = false
                                Part.Name = "SeekLightNode"

                                local v1229 = u768 or Part

                                u768 = Part

                                local Beam = Instance.new("Beam")
                                local v1231 = rawget(Toggles2, "ShowEyestalkPathColor") and Options2.ShowEyestalkPathColor.Value or Color3.fromRGB(0, 255, 0)

                                Beam.Color = ColorSequence.new({
									ColorSequenceKeypoint.new(0, v1231),
									ColorSequenceKeypoint.new(1, v1231)
								})
                                Beam.FaceCamera = true
                                Beam.Width0 = 0.2
                                Beam.Width1 = 0.2
                                Beam.Brightness = 10
                                Beam.LightInfluence = 0
                                Beam.LightEmission = 0
                                Beam.Enabled = true

                                local v1232 = (not rawget(Toggles2, "ShowEyestalkPathToggle") or not Toggles2.ShowEyestalkPathToggle.Value) and 1 or 0

                                Beam.Transparency = NumberSequence.new({
									NumberSequenceKeypoint.new(0, v1232),
									NumberSequenceKeypoint.new(1, v1232)
								})
                                Beam.Parent = t12.SeekNodesFolder

                                local Attachment = Instance.new("Attachment", Part)
                                local Attachment3 = Instance.new("Attachment", v1229)

                                Beam.Attachment0 = Attachment
                                Beam.Attachment1 = Attachment3
                                table.insert(t10.EyestalkHighlights, Beam)
                            end)(v773.Position)
                            task.wait()
                        end

                        break
                    end
                end

                task.wait(0.25)
            end
        end
    end)
end
local function v130()
    local GameData3 = ReplicatedStorage:FindFirstChild("GameData")
    if GameData3 then
        local Floor = GameData3:FindFirstChild("Floor")
        if Floor then
            _FloorConnection = Floor:GetPropertyChangedSignal("Value"):Connect(v123)
            v123()

            return
        end
        local connection10
        connection10 = GameData3.ChildAdded:Connect(function(child)
            if child.Name == "Floor" then
                if connection10 then
                    connection10:Disconnect()
                end

                v130()
            end
        end)
        v123()

        return
    end
    local connection11
    connection11 = ReplicatedStorage.ChildAdded:Connect(function(child)
        if child.Name == "GameData" then
            if connection11 then
                connection11:Disconnect()
            end

            v130()
        end
    end)
    v123()
end
v130()
Groupboxes.MinesLadder = t4.Mines:AddLeftGroupbox("Ladders")
Groupboxes.MinesLadder:AddToggle("LaddersESP", {
	Text = "Ladders",
	Default = false
})
Toggles2.LaddersESP:AddColorPicker("LaddersESPColor", {
	Text = "Ladders",
	Default = Color3.fromRGB(255, 255, 0)
})
Groupboxes.MinesLadder:AddToggle("LaddersTracers", {
	Text = "Ladders Tracers",
	Default = false
})
Toggles2.LaddersTracers:OnChanged(function(p137)
    if p137 and not Toggles2.LaddersESP.Value then
        Toggles2.LaddersTracers:SetValue(false)

        return
    end

    for _, v in ipairs(ESPCategories.Ladders) do
        local v781 = GetESPTarget(v, "Ladders")

        if v781 then
            v15:SetTracersEnabled(v781, p137)
        end
    end
end)
Toggles2.LaddersESP:OnChanged(function(p138)
    if p138 then
        for _, v in ipairs(ESPCategories.Ladders) do
            v107(v, "Ladders", "Ladder")
        end

        return
    end

    v108("Ladders")
end)
Options2.LaddersESPColor:OnChanged(function(p139)
    for _, v in ipairs(ESPCategories.Ladders) do
        v15:UpdateObjectColor(GetESPTarget(v, "Ladders"), p139)
    end
end)
Groupboxes.MinesAnticheat = t4.Mines:AddLeftGroupbox("Anticheat")
Groupboxes.MinesAnticheat:AddToggle("DisableAnticheat", {
	Text = "Anticheat Bypass",
	Default = false,
	Tooltip = "Completely disables the anticheat, after interacting with a ladder."
})
Toggles2.DisableAnticheat:OnChanged(function(p140)
    if t12.AnticheatDisabled == true and not p140 then
        u36.ClimbLadder:FireServer()
        t12.AnticheatDisabled = false
    end
end)
Groupboxes.MinesAnticheat:AddDivider()
Groupboxes.MinesAnticheat:AddToggle("RemoveFigureMines", {
	Text = "Delete Figure",
	Default = false,
	Tooltip = "Removes Figure, however this is inconsistent",
	DisabledTooltip = "Your executor doesn't support this feature :("
})
Toggles2.RemoveFigureMines:OnChanged(function(p141)
    if p141 and (Executor.isnetworkowner and workspace:FindFirstChild("Figure")) then
        local v790 = v39()

        for _, v in ipairs(t10.Entities) do
            if v.Name == "Figure" or v.Name == "FigureRig" or v.Name == "FigureRagdoll" then
                if v790 == "Mines" then
                    for _, descendant in v:GetDescendants() do
                        if descendant:IsA("BasePart") and pcall(function()
                            return Executor.isnetworkowner(descendant)
                        end) then
                            descendant.Position = Vector3.new(-49999, -49999, -49999)
                        end
                    end
                elseif v790 == "OldHotel" or v790 == "Fools" then
                    for _, descendant in v:GetDescendants() do
                        if descendant:IsA("BasePart") then
                            descendant.CanCollide = false

                            if pcall(function()
                                return Executor.isnetworkowner(descendant)
                            end) then
                                descendant.Position = Vector3.new(math.random(-29999, 29999), math.random(-29999, 29999), math.random(-29999, 29999))
                            end
                        end
                    end
                end
            end
        end
    end

    if p141 then
        local Figure = workspace:FindFirstChild("Figure")

        if Figure then
            for _, descendant in Figure:GetDescendants() do
                if descendant:IsA("BasePart") then
                    descendant.CanCollide = false
                end
            end
        end
    end
end)
function u124(p142, _, p144)
    local v803 = not p144 and 1 or 0
    local numberSequence = NumberSequence.new({
		NumberSequenceKeypoint.new(0, v803),
		NumberSequenceKeypoint.new(1, v803)
	})

    for _, v in ipairs(p142) do
        v.Transparency = numberSequence
    end
end
function u125(p145, p146)
    local colorSequence = ColorSequence.new({
		ColorSequenceKeypoint.new(0, p146),
		ColorSequenceKeypoint.new(1, p146)
	})

    for _, v in ipairs(p145) do
        v.Color = colorSequence
    end
end
Groupboxes.MinesAutomation = t4.Mines:AddRightGroupbox("Automation")
Groupboxes.MinesAutomation:AddToggle("AutoSteerMinecart", {
	Text = "Auto Minecart",
	Default = false,
	Tooltip = "Automatically completes the minecart section of the seek chase"
})
Groupboxes.MinesAutomation:AddToggle("AutoSolveAnchors", {
	Text = "Auto Solve Anchors",
	Default = false,
	Tooltip = "Automatically enters the correct code into anchors when you are near them."
})
Groupboxes.MinesAutomation:AddButton({
	Text = "Complete Door 200",
	Tooltip = "Automatically teleports to and interacts with each water pump to complete the Dam Seek",
	Callback = function()
    if CurrentFloor ~= "Mines" then
        return
    end

    if not Character then
        u2:Notify({
				Title = "Error",
				Description = "Character not found",
				Time = 3
			})

        return
    end

    u2:Notify({
			Title = "Door 200",
			Description = "Starting water pump automation...",
			Time = 3
		})

    local u812 = false
    local v813 = u36 and (u36:FindFirstChild("Cutscene") and u36.Cutscene.OnClientEvent:Connect(function()
        u812 = true
        task.wait(7)
        u812 = false
    end))

    while task.wait(0.1) do
        local v814 = (function()
            local t41 = {
					Height = -69420,
					Object = nil
				}
            local CurrentRooms6 = workspace:FindFirstChild("CurrentRooms")

            if not CurrentRooms6 then
                return nil
            end

            for _, child in CurrentRooms6:GetChildren() do
                local WaterPump = child:FindFirstChild("WaterPump")

                if WaterPump and WaterPump:GetAttribute("CH_Completed") ~= true then
                    local BasePart = WaterPump:FindFirstChildWhichIsA("BasePart")

                    if BasePart and BasePart.Position.Y > t41.Height then
                        t41.Object = WaterPump
                        t41.Height = BasePart.Position.Y
                    end
                end
            end

            return t41.Object
        end)()

        if not v814 then
            break
        end;

        (function(p147)
            local n12 = 0

            while task.wait(0.1) do
                n12 += 1

                if n12 > 600 then
                    return
                end

                if u812 then
                    continue
                end

                if Character then
                    Character:PivotTo(p147:GetPivot())
                end

                local ValvePrompt = p147:FindFirstChild("ValvePrompt", true)

                if ValvePrompt then
                    Functions.ForceFirePrompt(ValvePrompt)
                end

                if p147:GetAttribute("CH_Completed") then
                    return
                end
            end
        end)(v814)
    end

    if v813 then
        v813:Disconnect()
    end

    u2:Notify({
			Title = "Door 200",
			Description = "Finished water pump automation",
			Time = 4
		})
end
})
MinesAnchorCache = {}
Toggles2.AutoSolveAnchors:OnChanged(function()
    if AutoSolveAnchorsCache then
        AutoSolveAnchorsCache:Disconnect()
        AutoSolveAnchorsCache = nil
    end

    if AutoSolveAnchorsConnection then
        AutoSolveAnchorsConnection:Disconnect()
        AutoSolveAnchorsConnection = nil
    end

    MinesAnchorCache = {}

    if not Toggles2.AutoSolveAnchors.Value then
        return
    end

    local function v815()
        local CurrentRooms7 = workspace:FindFirstChild("CurrentRooms")

        if CurrentRooms7 then
            for _, child in CurrentRooms7:GetChildren() do
                for _, descendant in child:GetDescendants() do
                    if descendant.Name == "MinesAnchor" and descendant:IsA("Model") then
                        MinesAnchorCache[descendant] = true
                    end
                end
            end
        end
    end

    v815()
    AutoSolveAnchorsCache = workspace.DescendantAdded:Connect(function(_)
        v815()
    end)

    local n13 = 0

    AutoSolveAnchorsConnection = RunService.Heartbeat:Connect(function()
        if _Unloading then
            return
        end

        if not Toggles2.AutoSolveAnchors.Value or CurrentFloor ~= "Mines" then
            return
        end

        local elapsed = os.clock()

        if elapsed - n13 < 0.1 then
            return
        end

        n13 = elapsed

        local PlayerGui6 = LocalPlayer:FindFirstChildOfClass("PlayerGui")

        if not PlayerGui6 then
            return
        end

        local AnchorHintFrame = PlayerGui6:FindFirstChild("AnchorHintFrame", true)

        if not AnchorHintFrame then
            return
        end

        local AnchorCode = AnchorHintFrame:FindFirstChild("AnchorCode")
        local Code = AnchorHintFrame:FindFirstChild("Code")

        if not AnchorCode or not Code then
            return
        end

        local str = tostring(AnchorCode.Text or "")

        if str == "" then
            return
        end

        for k, _ in pairs(MinesAnchorCache) do
            if k and k.Parent then
                if k:GetAttribute("Activated") then
                    MinesAnchorCache[k] = nil
                else
                    local PrimaryPart = k.PrimaryPart

                    if PrimaryPart and (HumanoidRootPart and (not not HumanoidRootPart.Position and (HumanoidRootPart.Position - PrimaryPart.Position).Magnitude) or math.huge) <= (k:FindFirstChild("ActivateEventPrompt") and k.ActivateEventPrompt.MaxActivationDistance or 20) then
                        local Sign = k:FindFirstChild("Sign")

                        if Sign then
                            local TextLabel = Sign:FindFirstChildOfClass("TextLabel")

                            if TextLabel and str == TextLabel.Text then
                                local AnchorRemote = k:FindFirstChild("AnchorRemote")

                                if AnchorRemote then
                                    AnchorRemote:InvokeServer(Code.Text)
                                end
                            end
                        end
                    end
                end
            else
                MinesAnchorCache[k] = nil
            end
        end
    end)
end)
Groupboxes.MinesVisuals = t4.Mines:AddRightGroupbox("Visuals")
Groupboxes.MinesVisuals:AddToggle("ShowSeekPathToggle", {
	Text = "Show Seek Path",
	Default = false,
	Tooltip = "Shows you the correct path in the seek chase"
})
Toggles2.ShowSeekPathToggle:AddColorPicker("ShowSeekPathColor", {
	Text = "Seek Path",
	Default = Color3.fromRGB(0, 255, 0),
	Transparency = 0
})
Toggles2.ShowSeekPathToggle:OnChanged(function(p149)
    u124(t10.SeekHighlights, "ShowSeekPathColor", p149)
end)
Options2.ShowSeekPathColor:OnChanged(function(p150)
    u125(t10.SeekHighlights, p150)
end)
Toggles2.AutoSteerMinecart:OnChanged(function()
    if AutoMinecartConnection then
        AutoMinecartConnection:Disconnect()
        AutoMinecartConnection = nil
    end

    if t12.OriginalGetMoveVector then
        pcall(function()
            require(LocalPlayer.PlayerScripts.PlayerModule):GetControls().GetMoveVector = t12.OriginalGetMoveVector
        end)
        t12.OriginalGetMoveVector = nil
    end

    if not Toggles2.AutoSteerMinecart.Value then
        return
    end

    local PlayerGui7 = LocalPlayer:FindFirstChildOfClass("PlayerGui")
    local v820 = PlayerGui7 and PlayerGui7:FindFirstChild("MainUI")
    local v821 = v820 and v820:FindFirstChild("Initiator")
    local v822 = v821 and v821:FindFirstChild("Main_Game")

    if not v822 then
        return
    end

    local ok10, result12 = pcall(require, v822)

    if not ok10 or not result12 then
        return
    end

    local ok11, result13 = pcall(function()
        return require(LocalPlayer.PlayerScripts.PlayerModule):GetControls()
    end)

    if ok11 and result13 then
        local GetMoveVector = result13.GetMoveVector

        t12.OriginalGetMoveVector = GetMoveVector

        function result13.GetMoveVector(...)
            if Toggles2.AutoSteerMinecart.Value and CurrentFloor == "Mines" then
                local NearestTurnNode = t12.NearestTurnNode

                if NearestTurnNode and Functions.GetMinecart() then
                    local Turn = NearestTurnNode:GetAttribute("Turn")

                    return Turn == "Left" and Vector3.new(-1, 0, 0) or (Turn == "Right" and Vector3.new(1, 0, 0) or Vector3.zero)
                end
            end

            return GetMoveVector(...)
        end

        t12.AutoMinecartDucked = false
        t12.LastDuck = tick()
        AutoMinecartConnection = RunService.Heartbeat:Connect(function()
            if _Unloading then
                return
            end

            if not Toggles2.AutoSteerMinecart.Value or not Functions.GetMinecart() or tick() - t12.LastDuck < 0.1 then
                return
            end

            t12.NearestTurnNode = Functions.GetNearestTurnNode()

            if not t12.AutoMinecartDucked and Functions.GetNearestDuckBoard() then
                result12.crouch(true)
                t12.AutoMinecartDucked = true
            elseif not Functions.GetNearestDuckBoard() and t12.AutoMinecartDucked then
                result12.crouch(false)
                t12.AutoMinecartDucked = false
            end

            t12.LastDuck = tick()
        end)

        return
    end
end)
Groupboxes.Visuals_Camera = t4.Visuals:AddRightGroupbox("Camera")
Groupboxes.Visuals_Camera:AddToggle("Fullbright", {
	Text = "Fullbright",
	Default = false,
	Tooltip = "Makes your game very bright, and removes shadows"
})
Groupboxes.Visuals_Camera:AddSlider("FullbrightBrightness", {
	Text = "Brightness Level",
	Default = 3,
	Min = 1,
	Max = 10,
	Rounding = 1,
	Compact = true
})
local function v131()
    local FullbrightBrightnessValue = Options2.FullbrightBrightness.Value

    Lighting.Ambient = Color3.new(1, 1, 1)
    Lighting.OutdoorAmbient = Color3.new(1, 1, 1)
    Lighting.GlobalShadows = false
    Lighting.Brightness = FullbrightBrightnessValue
    Lighting.ClockTime = 14
    Lighting.FogColor = Color3.new(1, 1, 1)
    Lighting.FogEnd = 1000000000
    Lighting.ColorShift_Top = Color3.new(1, 1, 1)
    Lighting.ColorShift_Bottom = Color3.new(1, 1, 1)
    Lighting.ExposureCompensation = 0

    local Atmosphere = Lighting:FindFirstChildOfClass("Atmosphere")

    if Atmosphere then
        Atmosphere.Haze = 0
        Atmosphere.Density = 0
        Atmosphere.Offset = 0
        Atmosphere.Color = Color3.new(1, 1, 1)
        Atmosphere.Decay = Color3.new(1, 1, 1)
        Atmosphere.Glare = 0
    end
end
local function v132()
    if not fbDefaults then
        return
    end

    Lighting.Ambient = fbDefaults.Ambient
    Lighting.OutdoorAmbient = fbDefaults.OutdoorAmbient
    Lighting.GlobalShadows = fbDefaults.GlobalShadows
    Lighting.Brightness = fbDefaults.Brightness
    Lighting.ClockTime = fbDefaults.ClockTime
    Lighting.FogColor = fbDefaults.FogColor
    Lighting.FogEnd = fbDefaults.FogEnd
    Lighting.ColorShift_Top = fbDefaults.ColorShift_Top
    Lighting.ColorShift_Bottom = fbDefaults.ColorShift_Bottom
    Lighting.ExposureCompensation = fbDefaults.ExposureCompensation

    if fbAtmos and fbAtmosDefaults then
        fbAtmos.Haze = fbAtmosDefaults.Haze
        fbAtmos.Density = fbAtmosDefaults.Density
        fbAtmos.Offset = fbAtmosDefaults.Offset
        fbAtmos.Color = fbAtmosDefaults.Color
        fbAtmos.Decay = fbAtmosDefaults.Decay
        fbAtmos.Glare = fbAtmosDefaults.Glare
    end
end
Toggles2.Fullbright:OnChanged(function(p151)
    if _FullbrightConnection then
        _FullbrightConnection:Disconnect()
        _FullbrightConnection = nil
    end

    if _FullbrightRoomConn then
        _FullbrightRoomConn:Disconnect()
        _FullbrightRoomConn = nil
    end

    if p151 then
        fbDefaults = {
			Ambient = Lighting.Ambient,
			OutdoorAmbient = Lighting.OutdoorAmbient,
			GlobalShadows = Lighting.GlobalShadows,
			Brightness = Lighting.Brightness,
			ClockTime = Lighting.ClockTime,
			FogColor = Lighting.FogColor,
			FogEnd = Lighting.FogEnd,
			ColorShift_Top = Lighting.ColorShift_Top,
			ColorShift_Bottom = Lighting.ColorShift_Bottom,
			ExposureCompensation = Lighting.ExposureCompensation
		}
        fbAtmos = Lighting:FindFirstChildOfClass("Atmosphere")

        if fbAtmos then
            fbAtmosDefaults = {
				Haze = fbAtmos.Haze,
				Density = fbAtmos.Density,
				Offset = fbAtmos.Offset,
				Color = fbAtmos.Color,
				Decay = fbAtmos.Decay,
				Glare = fbAtmos.Glare
			}
        end

        v131()

        local n14 = 0

        _FullbrightConnection = RunService.Heartbeat:Connect(function()
            if _Unloading then
                return
            end

            local timestamp = tick()

            if timestamp - n14 < 0.3 then
                return
            end

            n14 = timestamp
            v131()
        end)

        local CurrentRooms8 = workspace:FindFirstChild("CurrentRooms")

        if CurrentRooms8 then
            _FullbrightRoomConn = CurrentRooms8.ChildAdded:Connect(function()
                v131()
            end)
        end

        return
    end

    v132()
    fbDefaults = nil
    fbAtmosDefaults = nil
end)
Groupboxes.Visuals_Camera:AddToggle("AmbientToggle", {
	Text = "Ambient",
	Default = false,
	Tooltip = "Changes the lighting color to the specified value"
})
Toggles2.AmbientToggle:AddColorPicker("AmbientColor", {
	Text = "Ambient",
	Default = Color3.fromRGB(255, 255, 255),
	Transparency = 0
})
Toggles2.AmbientToggle:OnChanged(function(p152)
    local v834 = CurrentRooms and CurrentRooms:FindFirstChild((tostring(LocalPlayer:GetAttribute("CurrentRoom"))))
    local v835 = v834 and v834:GetAttribute("Ambient")

    TweenService:Create(Lighting, TweenInfo.new(0.2, Enum.EasingStyle.Exponential), {
		Ambient = p152 and Options2.AmbientColor.Value or (v835 or Color3.fromRGB(255, 255, 255))
	}):Play()
end)
Options2.AmbientColor:OnChanged(function()
    if Toggles2.AmbientToggle and Toggles2.AmbientToggle.Value then
        TweenService:Create(Lighting, TweenInfo.new(0.2, Enum.EasingStyle.Exponential), {
			Ambient = Options2.AmbientColor.Value
		}):Play()
    end
end)
Groupboxes.Visuals_Camera:AddDivider()
Groupboxes.Visuals_Camera:AddSlider("FieldOfView", {
	Text = "Field of View",
	Min = 70,
	Max = 120,
	Default = 70,
	Rounding = 0,
	Compact = true
})
Groupboxes.Visuals_Camera:AddToggle("ThirdPerson", {
	Text = "Third Person",
	Default = false,
	Tooltip = "Puts your camera in a third person view"
})
Toggles2.ThirdPerson:AddKeyPicker("ThirdPersonKeybind", {
	Text = "Third Person",
	Default = "V",
	Mode = "Toggle",
	SyncToggleState = true
})
Groupboxes.Visuals_Camera:AddSlider("ThirdPersonOffsetX", {
	Text = "X Offset",
	Min = -10,
	Max = 10,
	Default = 0,
	Rounding = 1,
	Compact = true
})
Groupboxes.Visuals_Camera:AddSlider("ThirdPersonOffsetY", {
	Text = "Y Offset",
	Min = -10,
	Max = 10,
	Default = 0.5,
	Rounding = 1,
	Compact = true
})
Groupboxes.Visuals_Camera:AddSlider("ThirdPersonOffsetZ", {
	Text = "Z Offset",
	Min = -10,
	Max = 10,
	Default = 7.5,
	Rounding = 1,
	Compact = true
})
Groupboxes.Visuals_Camera:AddToggle("ThirdPersonWallCheck", {
	Text = "Wall Check",
	Default = false,
	Tooltip = "Prevents third person camera from going through walls"
})
Toggles2.ThirdPerson:OnChanged(u46)
Groupboxes.Visuals_Camera:AddDivider()
Groupboxes.Visuals_Camera:AddToggle("ViewmodelOffsetToggle", {
	Text = "Viewmodel Offset",
	Default = false,
	Tooltip = "Changes the offset of your viewmodel while holding an item."
})
Groupboxes.Visuals_Camera:AddSlider("ViewmodelOffsetX", {
	Text = "X Offset",
	Min = -10,
	Max = 10,
	Default = 0,
	Rounding = 1,
	Compact = true
})
Groupboxes.Visuals_Camera:AddSlider("ViewmodelOffsetY", {
	Text = "Y Offset",
	Min = -10,
	Max = 10,
	Default = 0,
	Rounding = 1,
	Compact = true
})
Groupboxes.Visuals_Camera:AddSlider("ViewmodelOffsetZ", {
	Text = "Z Offset",
	Min = -10,
	Max = 10,
	Default = 0,
	Rounding = 1,
	Compact = true
})
Groupboxes.Visuals_Camera:AddToggle("FreecamToggle", {
	Text = "Freecam",
	Default = false,
	Tooltip = "Enables freecam with WASD movement and mouse look"
})
Toggles2.FreecamToggle:AddKeyPicker("FreecamKeybind", {
	Text = "Freecam",
	Default = "Q",
	Mode = "Toggle",
	SyncToggleState = true
})
Groupboxes.Visuals_Camera:AddSlider("FreecamSpeed", {
	Text = "Freecam Speed",
	Min = 10,
	Max = 300,
	Default = 75,
	Rounding = 0,
	Compact = true
})
Groupboxes.Visuals_Camera:AddDivider()
Groupboxes.Visuals_Camera:AddToggle("RemoveCameraBobbing", {
	Text = "Remove Camera Bobbing",
	Default = false,
	Tooltip = "Removes camera 'bobbing'"
})
Groupboxes.Visuals_Camera:AddToggle("RemoveCameraShake", {
	Text = "Remove Camera Shake",
	Default = false,
	Tooltip = "Removes camera shake"
})
Groupboxes.Visuals_Camera:AddToggle("RemoveCameraFog", {
	Text = "Remove Fog",
	Default = false,
	Tooltip = "Removes camera fog"
})
Groupboxes.Visuals_Camera:AddToggle("DisableFiredampEffect", {
	Text = "Remove Firedamp Effect",
	Default = false,
	Tooltip = "Removes the firedamp fog/effect when inside a room"
})
FreecamConnection = nil
local function v133()
    local FreecamPart = workspace:FindFirstChild("FreecamPart")

    if FreecamPart then
        FreecamPart:Destroy()
    end

    local v837 = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

    if v837 then
        v837.Anchored = false
    end

    if LocalPlayer:GetAttribute("fc_om") then
        LocalPlayer.CameraMinZoomDistance = LocalPlayer:GetAttribute("fc_om")
    end

    if LocalPlayer:GetAttribute("fc_ox") then
        LocalPlayer.CameraMaxZoomDistance = LocalPlayer:GetAttribute("fc_ox")
    end
end
Toggles2.FreecamToggle:OnChanged(function(p153)
    if FreecamConnection then
        FreecamConnection:Disconnect()
    end

    if not p153 then
        v133()

        return
    end

    FreecamConnection = RunService.RenderStepped:Connect(function(dt)
        if _Unloading then
            return
        end

        if not Toggles2.FreecamToggle.Value then
            return
        end

        local Character8 = LocalPlayer.Character
        local v1269 = Character8 and Character8:FindFirstChildOfClass("Humanoid")
        local v1270 = Character8 and Character8:FindFirstChild("HumanoidRootPart")

        if v1270 and not v1270.Anchored then
            v1270.Anchored = true
        end

        if not workspace:FindFirstChild("FreecamPart") then
            local Part = Instance.new("Part")

            Part.Name = "FreecamPart"
            Part.Size = Vector3.new(0.01, 0.01, 0.01)
            Part.Transparency = 1
            Part.CanCollide = false
            Part.Anchored = true
            Part.CFrame = CurrentCamera.CFrame
            Part.Parent = workspace
            LocalPlayer:SetAttribute("fc_om", LocalPlayer.CameraMinZoomDistance)
            LocalPlayer:SetAttribute("fc_ox", LocalPlayer.CameraMaxZoomDistance)
            LocalPlayer.CameraMinZoomDistance = 0
            LocalPlayer.CameraMaxZoomDistance = 0

            local v1272, v1273, _ = CurrentCamera.CFrame:ToOrientation()

            LocalPlayer:SetAttribute("fc_p", math.deg(v1272))
            LocalPlayer:SetAttribute("fc_y", math.deg(v1273))
        end

        local FreecamPart = workspace:FindFirstChild("FreecamPart")
        local v1276 = CurrentCamera

        if FreecamPart and v1276 then
            local MouseDelta = UserInputService:GetMouseDelta()
            local v1278 = not UserInputService.KeyboardEnabled and 0.6 or 0.3
            local v1279 = (LocalPlayer:GetAttribute("fc_p") or 0) - MouseDelta.Y * v1278
            local v1280 = (LocalPlayer:GetAttribute("fc_y") or 0) - MouseDelta.X * v1278
            local v1281 = math.max(-80, math.min(80, v1279))

            LocalPlayer:SetAttribute("fc_p", v1281)
            LocalPlayer:SetAttribute("fc_y", v1280)
            FreecamPart.CFrame = CFrame.new(FreecamPart.Position) * CFrame.fromOrientation(math.rad(v1281), math.rad(v1280), 0)
            v1276.CFrame = FreecamPart.CFrame
            v1276.Focus = v1276.CFrame * CFrame.new(0, 0, -10)

            if v1269 and v1269.MoveDirection.Magnitude > 0 then
                local v1282 = Options2.FreecamSpeed and Options2.FreecamSpeed.Value or 75
                local MoveDirection = v1269.MoveDirection
                local v1284 = v1276.CFrame:VectorToObjectSpace(MoveDirection)
                local v1285 = v1276.CFrame.RightVector * v1284.X + v1276.CFrame.LookVector * -v1284.Z

                FreecamPart.Position = FreecamPart.Position + v1285 * v1282 * dt
            end
        end
    end)
end)
Groupboxes.Visuals_Camera:AddDivider()
Groupboxes.Visuals_Camera:AddToggle("NotifyOxygen", {
	Text = "Oxygen Notifications",
	Default = false,
	Tooltip = "Shows a notification when your oxygen level changes"
})
Groupboxes.Visuals_Camera:AddToggle("NotifyHasteTime", {
	Text = "Haste/Time Notifications",
	Default = false,
	Tooltip = "Shows a notification of the remaining time before Haste spawns"
})
Groupboxes.Visuals_Camera:AddDivider()
Groupboxes.Visuals_Camera:AddToggle("TransparentHidingSpotsToggle", {
	Text = "Transparent Hiding Spots",
	Default = false,
	Tooltip = "Makes a hiding spot transparent when you enter it"
})
Groupboxes.Visuals_Camera:AddSlider("TransparentHidingSpotsSlider", {
	Text = "Transparency",
	Min = 0,
	Max = 1,
	Default = 0.5,
	Rounding = 2,
	Compact = true
})
Groupboxes.Visuals_Camera:AddDivider()
Groupboxes.Visuals_Camera:AddToggle("DisableJumpscares", {
	Text = "Disable Jumpscares",
	Default = false,
	Tooltip = "Removes jumpscares from the chosen entities"
})
Groupboxes.Visuals_Camera:AddDropdown("JumpscaresList", {
	Text = "Jumpscares To Disable",
	Values = {
		"Glitch",
		"Timothy",
		"Void",
		"Rush",
		"Ambush",
		"Screech",
		"Figure",
		"Seek"
	},
	Multi = true,
	AllowNull = true
})
Groupboxes.Visuals_Camera:AddDivider()
CutsceneNames = {
	"Figure",
	"FigureEnd",
	"FigureHotelEnd",
	"FigureHotelFire",
	"SeekIntroFools",
	"SeekIntroHotel",
	"SeekIntroMines",
	"SeekIntroMines2",
	"SerewSeekDrain",
	"SewerSeekLower",
	"GrumbleNestEnd",
	"EyestalkIntro"
}
Groupboxes.Visuals_Camera:AddToggle("RemoveCutscenes", {
	Text = "Remove Cutscenes",
	Default = false,
	Tooltip = "Removes cutscenes that aren't needed for progression"
})
Toggles2.RemoveCutscenes:OnChanged(function(p154)
    local PlayerGui8 = LocalPlayer:FindFirstChildOfClass("PlayerGui")
    local v841 = PlayerGui8 and PlayerGui8:FindFirstChild("MainUI")
    local v842 = v841 and v841:FindFirstChild("Initiator")
    local v843 = v842 and v842:FindFirstChild("Main_Game")
    local v844 = v843 and v843:FindFirstChild("RemoteListener")

    if not v844 then
        return
    end

    local Cutscenes = v844:FindFirstChild("Cutscenes")

    if Cutscenes then
        for _, child in pairs(Cutscenes:GetChildren()) do
            if TableFind(CutsceneNames, child.Name) and child:IsA("ModuleScript") or TableFind(CutsceneNames, child:GetAttribute("OriginalName")) and child:IsA("ModuleScript") then
                child.Name = p154 and child.Name .. "_Disabled" or (child:GetAttribute("OriginalName") or child.Name)
            end
        end
    end

    local FloorReplicated2 = ReplicatedStorage:FindFirstChild("FloorReplicated")

    if FloorReplicated2 then
        for _, child in pairs(FloorReplicated2:GetChildren()) do
            if TableFind(CutsceneNames, child.Name) and child:IsA("ModuleScript") or TableFind(CutsceneNames, child:GetAttribute("OriginalName")) and child:IsA("ModuleScript") then
                child.Name = p154 and child.Name .. "_Disabled" or (child:GetAttribute("OriginalName") or child.Name)
            end
        end
    end
end)
Groupboxes.Visuals_Settings:AddToggle("ESPRainbow", {
	Text = "Rainbow ESP",
	Default = false
})
Groupboxes.Visuals_Settings:AddDivider()
TracerCategories = {
	"Entities",
	"Doors",
	"Objectives",
	"Items",
	"Chests",
	"HidingSpots"
}
for _, v in ipairs(TracerCategories) do
    local v136 = v .. "Tracers"
    local v137 = ESPToggleKeys[v]
    local v138 = if v ~= "HidingSpots" then v else "Hiding Spots"

    Groupboxes.Visuals_Settings:AddToggle(v136, {
		Text = v138 .. " Tracers",
		Default = false
	})
    Toggles2[v136]:OnChanged(function(p155)
        if p155 and not Toggles2[v137].Value then
            Toggles2[v136]:SetValue(false)

            return
        end

        for _, v12 in ipairs(ESPCategories[v]) do
            local v854 = GetESPTarget(v12, v)

            if v854 then
                v15:SetTracersEnabled(v854, p155)
            end
        end
    end)
end
Groupboxes.Visuals_Settings:AddDropdown("ESPTracerOrigin", {
	Text = "Tracer Origin",
	Values = {
		"Top",
		"Middle",
		"Bottom"
	},
	Default = 3
})
Groupboxes.Visuals_Settings:AddToggle("ESPShowText", {
	Text = "Show ESP Text",
	Default = true
})
Groupboxes.Visuals_Settings:AddToggle("ESPShowDistance", {
	Text = "Show Distance",
	Default = true
})
Groupboxes.Visuals_Settings:AddSlider("ESPTextSize", {
	Text = "Text Size",
	Min = 8,
	Max = 24,
	Default = 15,
	Rounding = 0,
	Compact = true
})
Groupboxes.Visuals_Settings:AddDropdown("ESPTextFont", {
	Text = "Text Font",
	Values = {
		"Legacy",
		"Arial",
		"ArialBold",
		"SourceSans",
		"SourceSansBold",
		"SourceSansLight",
		"SourceSansItalic",
		"Bodoni",
		"Garamond",
		"Cartoon",
		"Code",
		"Highway",
		"SciFi",
		"Arcade",
		"Fantasy",
		"Antique",
		"SourceSansSemibold",
		"Gotham",
		"GothamMedium",
		"GothamBold",
		"GothamBlack",
		"AmaticSC",
		"Bangers",
		"Creepster",
		"DenkOne",
		"Fondamento",
		"FredokaOne",
		"GrenzeGotisch",
		"IndieFlower",
		"JosefinSans",
		"Jura",
		"Kalam",
		"LuckiestGuy",
		"Merriweather",
		"Michroma",
		"Nunito",
		"Oswald",
		"PatrickHand",
		"PermanentMarker",
		"Roboto",
		"RobotoCondensed",
		"RobotoMono",
		"Sarpanch",
		"SpecialElite",
		"TitilliumWeb",
		"Ubuntu"
	},
	Default = 11
})
Groupboxes.Visuals_Settings:AddSlider("ESPRenderLimit", {
	Text = "ESP Highlight Limit",
	Min = 30,
	Max = 240,
	Default = 240,
	Rounding = 0,
	Compact = true
})
Groupboxes.Visuals_Settings:AddSlider("ESPMaxDistance", {
	Text = "ESP Distance",
	Min = 50,
	Max = 500,
	Default = 250,
	Rounding = 0,
	Compact = true,
	Suffix = " studs"
})
Groupboxes.Visuals_Settings:AddDivider()
Groupboxes.Visuals_Settings:AddSlider("ESPFillTransparency", {
	Text = "Fill Transparency",
	Min = 0,
	Max = 1,
	Default = 0.75,
	Rounding = 2,
	Compact = true
})
Groupboxes.Visuals_Settings:AddSlider("ESPOutlineTransparency", {
	Text = "Outline Transparency",
	Min = 0,
	Max = 1,
	Default = 0,
	Rounding = 2,
	Compact = true
})
Groupboxes.Visuals_Settings:AddSlider("ESPTextTransparency", {
	Text = "Text Transparency",
	Min = 0,
	Max = 1,
	Default = 0,
	Rounding = 2,
	Compact = true
})
Groupboxes.Visuals_Settings:AddSlider("ESPTextOutlineTransparency", {
	Text = "Text Outline Transparency",
	Min = 0,
	Max = 1,
	Default = 0,
	Rounding = 2,
	Compact = true
})
Groupboxes.Visuals_Settings:AddSlider("ESPTracerThickness", {
	Text = "Tracer Thickness",
	Min = 0.5,
	Max = 2,
	Default = 0.75,
	Rounding = 2,
	Compact = true
})
Groupboxes.Visuals_Notifs = t4.Visuals:AddLeftGroupbox("Entity Notifications")
Groupboxes.Visuals_Notifs:AddToggle("NotifyEntities", {
	Text = "Entity Notifications",
	Default = false,
	Tooltip = "Receive notifications/popups when entities spawn"
})
Groupboxes.Visuals_Notifs:AddDropdown("EntityList", {
	Text = "Entity List",
	Values = {
		"Rush",
		"Ambush",
		"A-60",
		"A-120",
		"Blitz",
		"Eyes",
		"Lookman",
		"Monument",
		"Sally",
		"Gloombat Swarm",
		"Glitch Rush",
		"Glitch Ambush"
	},
	Multi = true,
	AllowNull = true,
	Tooltip = "Only notify for selected entities (leave empty to notify all)"
})
Groupboxes.Visuals_Notifs:AddDropdown("NotifyStyle", {
	Text = "Notification Style",
	Values = {
		"Obsidian",
		"Achievement"
	},
	Default = 1
})
Groupboxes.Visuals_Notifs:AddDropdown("NotifySound", {
	Text = "Notification Sound",
	Values = {
		"Achievement",
		"Tone",
		"Alert",
		"Windows XP",
		"GTA Cell"
	},
	Default = 5
})
Groupboxes.Visuals_Notifs:AddDivider()
Groupboxes.Visuals_Notifs:AddToggle("EntityChatToggle", {
	Text = "Notify Chat",
	Default = false,
	Tooltip = "Sends a message in the chat when an entity spawns"
})
Groupboxes.Visuals_Notifs:AddInput("EntityChatMessage", {
	Text = "Message",
	Default = "spawned!",
	Numeric = false,
	Placeholder = "Message"
})
Groupboxes.Visuals_ItemNotifs = t4.Visuals:AddLeftGroupbox("Item Notifications")
Groupboxes.Visuals_ItemNotifs:AddToggle("NotifyItems", {
	Text = "Item Notifications",
	Default = false,
	Tooltip = "Receive notifications/popups when items spawn (shows distance)"
})
Groupboxes.Visuals_ItemNotifs:AddDropdown("ItemList", {
	Text = "Item List",
	Values = {
		"Alarm Clock",
		"Aloe Vera",
		"Bandage",
		"Bandage Pack",
		"Battery",
		"Battery Pack",
		"Big Bomb",
		"Big Shield Potion",
		"Bomb",
		"Boxing Gloves",
		"Bread",
		"Candle",
		"Candy",
		"Cheese",
		"Compass",
		"Crucifix",
		"Donut",
		"Flashlight",
		"Glitch Fragment",
		"Glowstick",
		"Golden Gun",
		"Gummy Flashlight",
		"Gween Soda",
		"Hiding Box",
		"Holy Hand Grenade",
		"Iron Key",
		"Knockbomb",
		"Lantern",
		"Laser Pointer",
		"Lighter",
		"Lockpicks",
		"Lotus",
		"Lotus Petal",
		"Mini Shield Potion",
		"Moonlight Candle",
		"Moonlight Smoothie",
		"Multitool",
		"Nanner",
		"Rift Jar",
		"Shears",
		"Skeleton Key",
		"Smoothie",
		"Spotlight",
		"Starlight Barrel",
		"Starlight Bottle",
		"Starlight Vial",
		"Stop Sign",
		"Straplight",
		"Tablet",
		"Tip Jar",
		"Vitamins"
	},
	Multi = true,
	AllowNull = true,
	Tooltip = "Only notify for selected items (leave empty to notify all)"
})
Groupboxes.Visuals_ItemNotifs:AddDropdown("ItemNotifyStyle", {
	Text = "Notification Style",
	Values = {
		"Obsidian",
		"Achievement"
	},
	Default = 1
})
Groupboxes.Visuals_ItemNotifs:AddDropdown("ItemNotifySound", {
	Text = "Notification Sound",
	Values = {
		"Achievement",
		"Tone",
		"Alert",
		"Windows XP",
		"GTA Cell"
	},
	Default = 4
})
Groupboxes.Visuals_ItemNotifs:AddDivider()
Groupboxes.Visuals_ItemNotifs:AddToggle("ItemChatToggle", {
	Text = "Notify Chat",
	Default = false,
	Tooltip = "Sends a message in the chat when an item spawns"
})
Groupboxes.Visuals_ItemNotifs:AddInput("ItemChatMessage", {
	Text = "Message",
	Default = "spawned!",
	Numeric = false,
	Placeholder = "Message"
})
Toggles2.ESPRainbow:OnChanged(function(p156)
    v15:SetRainbow(p156)
end)
Options2.ESPTracerOrigin:OnChanged(function(p157)
    local t42 = {
		Top = "Top",
		Middle = "Center",
		Bottom = "Bottom"
	}

    v15:SetTracerOrigin(t42[p157] or "Bottom")
end)
Toggles2.ESPShowText:OnChanged(function(p158)
    v15.ShowESPText = p158
end)
Toggles2.ESPShowDistance:OnChanged(function(p159)
    v15:SetShowDistance(p159)
end)
Options2.ESPTextSize:OnChanged(function(p160)
    v15:SetTextSize(p160)
end)
Options2.ESPTextFont:OnChanged(function(p161)
    v15:SetFont(Enum.Font[p161])
end)
Options2.ESPRenderLimit:OnChanged(function(p162)
    v15:SetRenderLimit(p162)
end)
Options2.ESPMaxDistance:OnChanged(function(p163)
    v15.MaxDistance = p163
end)
Options2.ESPFillTransparency:OnChanged(function(p164)
    v15:SetFillTransparency(p164)
end)
Options2.ESPOutlineTransparency:OnChanged(function(p165)
    v15:SetOutlineTransparency(p165)
end)
Options2.ESPTextTransparency:OnChanged(function(p166)
    v15:SetTextTransparency(p166)
end)
Options2.ESPTextOutlineTransparency:OnChanged(function(p167)
    v15:SetTextOutlineTransparency(p167)
end)
Options2.ESPFadeTime:OnChanged(function(p168)
    if Toggles2.ESPFade.Value then
        v15:SetFadeTime(p168)
    end
end)
Toggles2.ESPFade:OnChanged(function(p169)
    v15:SetFadeTime(p169 and Options2.ESPFadeTime.Value or 0)
end)
Toggles2.ESPArrowsToggle:OnChanged(function(p170)
    v15:SetArrows(p170)
end)
Options2.ESPArrowsRadius:OnChanged(function(p171)
    v15:SetArrowRadius(p171)
end)
Options2.ESPTracerThickness:OnChanged(function(p172)
    v15:SetTracerSize(p172)
end)
v15:SetMatchColors(false)
v15:SetOutlineColor(Color3.fromRGB(255, 255, 255))
v15:SetShowDistance(true)
v15:SetFont(Enum.Font.Code)
v15:SetTextSize(15)
v15.ShowESPText = true
v15:SetRenderLimit(240)
v15.MaxDistance = 250
v15:SetFillTransparency(0.75)
v15:SetOutlineTransparency(0)
v15:SetTextTransparency(0)
v15:SetTextOutlineTransparency(0)
v15:SetFadeTime(Toggles2.ESPFade and (not not Toggles2.ESPFade.Value and Options2.ESPFadeTime.Value) or 0)
v15:SetArrows(false)
v15:SetArrowRadius(250)
v15:SetTracerSize(0.75)
local u139
local u140 = ReplicatedStorage:FindFirstChild("FloorReplicated") or Instance.new("Folder")
local function v141()
    if u139 then
        return u139
    end

    local PlayerGui9 = LocalPlayer:FindFirstChildOfClass("PlayerGui")
    local v874 = PlayerGui9 and PlayerGui9:FindFirstChild("MainUI")
    local v875 = v874 and v874:FindFirstChild("Initiator")
    local v876 = v875 and v875:FindFirstChild("Main_Game")

    if v876 then
        local ok12, result14 = pcall(require, v876)

        if ok12 and result14 then
            u139 = result14

            return u139
        end
    end

    return nil
end
Toggles2.RemoveCameraBobbing:OnChanged(function(p173)
    local v880 = v141()

    if v880 and v880.spring then
        v880.spring.Speed = not p173 and 8 or 9000000000
    end
end)
Toggles2.RemoveCameraFog:OnChanged(function(p174)
    Lighting.FogEnd = if not p174 then t12.OldFog or Lighting.FogEnd else 10000000

    for _, v in ipairs(t12.FogInstances) do
        v.Density = if not p174 then v:GetAttribute("Density_Old") else 0
    end
end)
Toggles2.DisableFiredampEffect:OnChanged(function(p175)
    local CurrentRooms9 = workspace:FindFirstChild("CurrentRooms")

    if CurrentRooms9 then
        for _, child in CurrentRooms9:GetChildren() do
            if p175 then
                child:SetAttribute("Firedamp", false)

                for _, child4 in CurrentCamera:GetChildren() do
                    if child4.Name == "LiveFiredamp" then
                        child4:Destroy()
                    end
                end
            else
                child:SetAttribute("Firedamp", child:GetAttribute("Firedamp_Old"))
            end
        end
    end

    local CurrentRoom = LocalPlayer:GetAttribute("CurrentRoom")

    LocalPlayer:SetAttribute("CurrentRoom", 0)
    task.wait()
    LocalPlayer:SetAttribute("CurrentRoom", CurrentRoom)
end)
local function v142(p176, p177)
    for _, v in ipairs(ESPCategories.HidingSpots) do
        local v895 = false

        for _, descendant in v:GetDescendants() do
            if descendant.Name == "HiddenPlayer" and descendant.Value == Character then
                v895 = true

                break
            end
        end

        for _, descendant in v:GetDescendants() do
            if descendant:IsA("BasePart") and descendant:GetAttribute("Transparency_Old") then
                TweenService:Create(descendant, TweenInfo.new(0.25, Enum.EasingStyle.Linear), {
					Transparency = p176 and (not not v895 and p177) or descendant:GetAttribute("Transparency_Old")
				}):Play()
            end
        end
    end
end
Toggles2.TransparentHidingSpotsToggle:OnChanged(function(p178)
    v142(p178, Options2.TransparentHidingSpotsSlider.Value)
end)
Options2.TransparentHidingSpotsSlider:OnChanged(function(p179)
    v142(Toggles2.TransparentHidingSpotsToggle.Value, p179)
end)
local function v143(p180)
    local PlayerGui10 = LocalPlayer:FindFirstChildOfClass("PlayerGui")
    local v904 = PlayerGui10 and PlayerGui10:FindFirstChild("MainUI")
    local v905 = v904 and v904:FindFirstChild("Initiator")
    local v906 = v905 and v905:FindFirstChild("Main_Game")
    local v907 = v906 and v906:FindFirstChild("RemoteListener")
    local v908 = Options2.JumpscaresList.Value or {}

    pcall(function()
        local v1286 = v907 and (v907:FindFirstChild("Jumpscares") or v907:FindFirstChild("Jumpscares_Disabled"))

        if v1286 then
            v1286.Name = not p180 and "Jumpscares" or "Jumpscares_Disabled"

            for _, descendant in v1286:GetDescendants() do
                if descendant:IsA("ModuleScript") then
                    local v1289 = descendant:GetAttribute("CHOriginalName") or descendant.Name:gsub("_Disabled", "")

                    descendant:SetAttribute("CHOriginalName", v1289)

                    for k, _ in pairs(v908) do
                        if v1289:lower():find(tostring(k):lower(), 1, true) then
                            descendant.Name = p180 and v1289 .. "_Disabled" or v1289

                            break
                        end
                    end
                end
            end
        end
    end)
end
Toggles2.DisableJumpscares:OnChanged(v143)
Options2.JumpscaresList:OnChanged(function()
    if Toggles2.DisableJumpscares.Value then
        v143(true)
    end
end)
local connection12
connection12 = RunService.RenderStepped:Connect(function()
    if _Unloading then
        if connection12 then
            connection12:Disconnect()
        end

        return
    end

    local v909 = v141()

    if v909 then
        if Toggles2.RemoveCameraShake.Value then
            v909.csgo = CFrame.new()
        end

        if Toggles2.ViewmodelOffsetToggle.Value then
            v909.tooloffset = Vector3.new(Options2.ViewmodelOffsetX.Value, Options2.ViewmodelOffsetY.Value, Options2.ViewmodelOffsetZ.Value)
        else
            v909.tooloffset = Vector3.zero
        end
    end

    if Toggles2.AmbientToggle and Toggles2.AmbientToggle.Value then
        TweenService:Create(Lighting, TweenInfo.new(0.2, Enum.EasingStyle.Exponential), {
			Ambient = Options2.AmbientColor.Value
		}):Play()
    end
end)
EntityNotifyData = {
	RushMoving = {
		Alias = "Rush",
		Danger = true
	},
	AmbushMoving = {
		Alias = "Ambush",
		Danger = true
	},
	A60 = {
		Alias = "A-60",
		Danger = true
	},
	A120 = {
		Alias = "A-120",
		Danger = true,
		GodmodeText = "A-120 is coming, godmode does not work, hide!"
	},
	BackdoorRush = {
		Alias = "Blitz",
		Danger = true
	},
	Eyes = {
		Alias = "Eyes",
		Text = "Eyes has spawned, look away!"
	},
	Lookman = {
		Alias = "Lookman",
		Text = "Lookman has spawned, look away!"
	},
	BackdoorLookman = {
		Alias = "Lookman",
		Text = "Lookman has spawned, look away!"
	},
	MonumentEntity = {
		Alias = "Monument",
		Text = "Monument has spawned, look at it!"
	},
	SallyMoving = {
		Alias = "Sally",
		Text = "Sally has spawned, drop an item for her!"
	},
	GloombatSwarm = {
		Alias = "Gloombat Swarm",
		Text = "Gloombats have spawned, turn off lights!"
	},
	["RNIUSHCG=="] = {
		Alias = "Glitch Rush",
		Danger = true
	},
	AR0xMBUSH = {
		Alias = "Glitch Ambush",
		Danger = true
	},
	GlitchRush = {
		Alias = "Glitch Rush",
		Danger = true
	},
	GlitchAmbush = {
		Alias = "Glitch Ambush",
		Danger = true
	},
	GiggleCeiling = {
		Alias = "Giggle"
	},
	GrumbleRig = {
		Alias = "Grumble"
	}
}
local function v145(p181)
    if p181.Text then
        return p181.Text
    end

    if p181.GodmodeText then
        return p181.GodmodeText
    end

    if p181.Danger then
        return p181.Alias .. " is coming, hide!"
    end

    return "Entity detected!"
end
t28.SoundAssets = {
	Achievement = "rbxassetid://10469938989",
	Tone = nil,
	Alert = nil,
	["Windows XP"] = nil,
	["GTA Cell"] = nil
}
pcall(function()
    if isfolder then
        if not isfolder("cheesy/Notification Sounds") then
            makefolder("cheesy/Notification Sounds")
        end

        local function v911(p182, p183)
            local v1294 = "cheesy/Notification Sounds" .. "/" .. p182 .. "." .. (p183:match("%.([a-zA-Z0-9]+)$") or "mp3")

            if not isfile(v1294) then
                writefile(v1294, game:HttpGet(p183))
            end

            return getcustomasset(v1294)
        end

        t28.SoundAssets.Tone = v911("tone", "https://raw.githubusercontent.com/doram44/cheesy/main/notif%20sounds/tone%20notification.mp3")
        t28.SoundAssets.Alert = v911("alert", "https://raw.githubusercontent.com/doram44/cheesy/main/notif%20sounds/alert%20notification.mp3")
        t28.SoundAssets["Windows XP"] = v911("xp", "https://raw.githubusercontent.com/doram44/cheesy/main/notif%20sounds/windows%20xp%20exclamation.ogg")
        t28.SoundAssets["GTA Cell"] = v911("gta", "https://raw.githubusercontent.com/doram44/cheesy/main/notif%20sounds/gta%20notification.ogg")
    end
end)
t28.SoundInstance = Instance.new("Sound")
t28.SoundInstance.Name = "cheesehubnotifsound"
t28.SoundInstance.Volume = 0.65
pcall(function()
    t28.SoundInstance.Parent = SoundService
end)
function t28.PlaySound(p184)
    local u913 = p184 or "NotifySound"

    pcall(function()
        local Value2 = Options2[u913].Value
        local v1296 = t28.SoundAssets[Value2] or t28.SoundAssets.Achievement

        t28.SoundInstance.SoundId = v1296
        t28.SoundInstance.TimePosition = 0
        t28.SoundInstance:Play()
    end)
end
t28.ActiveAchievement = {}
local function v146(p185)
    for i = p185, #t28.ActiveAchievement do
        local v916 = t28.ActiveAchievement[i]
        local v917 = (i - 1) * 150 + 72

        TweenService:Create(v916, TweenInfo.new(0.3, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Position = UDim2.new(1, -22, 0, v917)
		}):Play()
    end
end
function t28.ShowAchievement(p186, p187, p188)
    local ScreenGui = Instance.new("ScreenGui")

    ScreenGui.Name = "cheesehubentitynotif"
    ScreenGui.ResetOnSpawn = false
    ScreenGui.IgnoreGuiInset = true
    ScreenGui.Parent = CoreGui

    local v922 = not p188 and 90 or 110
    local Frame = Instance.new("Frame")

    Frame.Size = UDim2.new(0, 350, 0, v922)
    Frame.AnchorPoint = Vector2.new(1, 0)
    Frame.BackgroundTransparency = 1
    Frame.Parent = ScreenGui

    local v924 = (#t28.ActiveAchievement + 1 - 1) * 150 + 72

    Frame.Position = UDim2.new(1, 370, 0, v924)
    table.insert(t28.ActiveAchievement, Frame)

    local TextLabel = Instance.new("TextLabel")

    TextLabel.BackgroundTransparency = 1
    TextLabel.Position = UDim2.new(0, 0, 0, -30)
    TextLabel.Size = UDim2.new(1, 0, 0, 28)
    TextLabel.Font = Enum.Font.GothamBlack
    TextLabel.Text = "Entity detected"
    TextLabel.TextColor3 = Color3.fromRGB(255, 238, 216)
    TextLabel.TextStrokeTransparency = 0.75
    TextLabel.TextSize = 20
    TextLabel.TextXAlignment = Enum.TextXAlignment.Left
    TextLabel.Parent = Frame

    local Frame4 = Instance.new("Frame")

    Frame4.Name = "Frame"
    Frame4.Size = UDim2.new(1, 0, 1, 0)
    Frame4.BackgroundColor3 = Color3.fromRGB(35, 22, 18)
    Frame4.BorderSizePixel = 0
    Frame4.Parent = Frame
    Instance.new("UICorner", Frame4).CornerRadius = UDim.new(0, 9)

    local UIStroke = Instance.new("UIStroke", Frame4)

    UIStroke.Color = Color3.fromRGB(255, 236, 214)
    UIStroke.Thickness = 3

    local ImageLabel = Instance.new("ImageLabel")

    ImageLabel.Name = "ImageLabel"
    ImageLabel.BackgroundTransparency = 1
    ImageLabel.Image = "rbxassetid://106990021902592"
    ImageLabel.Size = UDim2.new(0, 76, 0, 76)
    ImageLabel.Position = UDim2.new(0, 10, 0, 10)
    ImageLabel.ScaleType = Enum.ScaleType.Fit
    ImageLabel.Parent = Frame4

    local Frame5 = Instance.new("Frame")

    Frame5.Name = "Details"
    Frame5.BackgroundTransparency = 1
    Frame5.Position = UDim2.new(0, 96, 0, 10)
    Frame5.Size = UDim2.new(1, -106, 1, -20)
    Frame5.Parent = Frame4

    local function v930(p189, p190, p191, p192, p193)
        local TextLabel2 = Instance.new("TextLabel")

        TextLabel2.Name = p189
        TextLabel2.BackgroundTransparency = 1
        TextLabel2.Position = UDim2.new(0, 0, 0, p191)
        TextLabel2.Size = UDim2.new(1, 0, 0, p192 + 12)
        TextLabel2.Font = p193
        TextLabel2.Text = p190
        TextLabel2.TextScaled = true
        TextLabel2.TextSize = p192
        TextLabel2.TextWrapped = true
        TextLabel2.TextXAlignment = Enum.TextXAlignment.Left
        TextLabel2.TextYAlignment = Enum.TextYAlignment.Top
        TextLabel2.TextColor3 = Color3.fromRGB(255, 238, 216)
        TextLabel2.TextStrokeTransparency = 0.8
        TextLabel2.Parent = Frame5
    end

    v930("Title", p186, 0, 18, Enum.Font.GothamBold)
    v930("Desc", p187, 34, 14, Enum.Font.Gotham)

    if p188 then
        v930("Reason", p188, 64, 14, Enum.Font.Gotham)
    end

    task.spawn(function()
        TweenService:Create(Frame, TweenInfo.new(0.55, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Position = UDim2.new(1, -22, 0, v924)
		}):Play()
        task.wait(5.5)
        TweenService:Create(Frame, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
			Position = UDim2.new(1, 370, 0, v924)
		}):Play()
        task.wait(0.5)

        for i, v in ipairs(t28.ActiveAchievement) do
            if v == Frame then
                table.remove(t28.ActiveAchievement, i)
                v146(i)

                break
            end
        end

        ScreenGui:Destroy()
    end)
end
t28.ActiveEntity = 0
function u105(p194)
    if not t26[p194.Name] then
        return
    end

    local v932 = EntityNotifyData[p194.Name]

    if not v932 then
        return
    end

    if not Toggles2.NotifyEntities.Value then
        return
    end

    local EntityListValue = Options2.EntityList.Value

    if EntityListValue and #EntityListValue > 0 and not EntityListValue[v932.Alias] then
        return
    end

    if t28.ActiveEntity >= 6 then
        return
    end

    t28.ActiveEntity = t28.ActiveEntity + 1
    t28.PlaySound()

    local u934 = v145(v932)
    local t43 = {
		RushMoving = true,
		AmbushMoving = true,
		A60 = true,
		BackdoorRush = true,
		["RNIUSHCG=="] = true,
		AR0xMBUSH = true
	}

    if Toggles2.Godmode.Value and t43[p194.Name] then
        u934 = v932.Alias .. " is coming, but you have godmode enabled so don't worry!"
    end

    if Options2.NotifyStyle.Value == "Obsidian" then
        u2:Notify({
			Title = "Entity '" .. v932.Alias .. "' has spawned",
			Description = u934,
			Time = 5
		})
    else
        task.spawn(function()
            t28.ShowAchievement("Entity '" .. v932.Alias .. "' has spawned", u934)
        end)
    end

    if Toggles2.EntityChatToggle.Value then
        Functions.SendChat(v932.Alias .. " " .. Options2.EntityChatMessage.Value)
    end

    task.delay(6, function()
        t28.ActiveEntity = math.max(0, t28.ActiveEntity - 1)
    end)
end
for _, descendant in workspace:GetDescendants() do
    task.spawn(v110, descendant)
    task.spawn(v69, descendant)
    task.spawn(v77, descendant)
end
t12.OldFog = Lighting.FogEnd
for _, child in Lighting:GetChildren() do
    if child:IsA("Atmosphere") then
        child:SetAttribute("Density_Old", child.Density)

        local connection13 = child:GetPropertyChangedSignal("Density"):Connect(function()
            if child.Density ~= 0 then
                child:SetAttribute("Density_Old", child.Density)
            end

            if Toggles2.RemoveCameraFog.Value then
                child.Density = 0
            end
        end)

        child.Destroying:Once(function()
            connection13:Disconnect()
        end)
        table.insert(Connections, connection13)
        table.insert(t12.FogInstances, child)
    end
end
workspace.DescendantAdded:Connect(v110)
workspace.DescendantAdded:Connect(v69)
workspace.DescendantAdded:Connect(v77)
for _, descendant in workspace:GetDescendants() do
    if descendant:IsA("ProximityPrompt") and not descendant:GetAttribute("FakePrompt") then
        task.spawn(v69, descendant)
    end
end
local timestamp = tick()
RunService.Heartbeat:Connect(function()
    if _Unloading then
        return
    end

    if tick() - timestamp <= 0.5 then
        return
    end

    timestamp = tick()

    for _, v in ipairs(_TrackedPrompts) do
        if v and v.Parent then
            if v:HasTag("DisableWhenEnabledOnClient") then
                v:RemoveTag("DisableWhenEnabledOnClient")
            end

            if Toggles2 and Toggles2.InstantInteract and Toggles2.InstantInteract.Value and v.HoldDuration ~= 0 then
                v.HoldDuration = 0
            end

            if Toggles2 and Toggles2.PromptClip and Toggles2.PromptClip.Value and v.RequiresLineOfSight ~= false then
                v.RequiresLineOfSight = false
            end

            if Toggles2 and Toggles2.PromptReach and Toggles2.PromptReach.Value then
                local MaxActivationDistance_Old = v:GetAttribute("MaxActivationDistance_Old")

                if MaxActivationDistance_Old then
                    local v939 = Options2 and (Options2.PromptRange and MaxActivationDistance_Old * Options2.PromptRange.Value)

                    if v939 and v939 ~= v.MaxActivationDistance then
                        v.MaxActivationDistance = v939
                    end
                end
            end
        end
    end

    if Toggles2 and Toggles2.NotifyVulnerable and Toggles2.NotifyVulnerable.Value and HumanoidRootPart then
        local PositionY = HumanoidRootPart.Position.Y

        if VulnerableLastY and PositionY - VulnerableLastY >= 3 and tick() - VulnerableNotifCooldown >= 3 then
            u2:Notify({
				Title = "Vulnerable",
				Description = "You've moved 3+ studs upward!",
				Time = 3
			})
            VulnerableNotifCooldown = tick()
        end

        VulnerableLastY = PositionY
    end
end)
AutoHide = {
	Last = tick(),
	ThreatConns = {}
}
KnobFarmConn = nil
Toggles2.AutoClosetToggle:OnChanged(function(p195)
    if _AutoHideConnection then
        _AutoHideConnection:Disconnect()
        _AutoHideConnection = nil
    end

    if p195 then
        _AutoHideConnection = RunService.Heartbeat:Connect(function()
            if _Unloading then
                return
            end

            if not Character or not HumanoidRootPart or not Humanoid then
                return
            end

            if tick() - AutoHide.Last <= 0.1 then
                return
            end

            if Functions.GetNearestEntity(true, Options2.AutoClosetEntityList.Value) then
                local v1305 = Functions.GetNearestHidingSpot()

                if Character:GetAttribute("Hiding") ~= true and v1305 then
                    Functions.ForceFirePrompt(v1305.HidePrompt)
                end
            elseif Character:GetAttribute("Hiding") == true then
                u36.CamLock:FireServer()
            end

            if Toggles2.RemoveClosetDelay.Value and Humanoid.MoveDirection ~= Vector3.zero and ((u28.Anchored or HumanoidRootPart.Anchored) and Character:GetAttribute("AnimatingClient") ~= true and Character:GetAttribute("Hiding") == true) then
                u36.CamLock:FireServer()
            end

            AutoHide.Last = tick()
        end)
    end
end)
local function v155()
    if Character then
        local n15 = -1
        local t44 = {
			80,
			60,
			40,
			20
		}

        Character:GetAttributeChangedSignal("Oxygen"):Connect(function()
            local Oxygen = Character:GetAttribute("Oxygen")

            if Toggles2.NotifyOxygen and Toggles2.NotifyOxygen.Value then
                local v1307 = math.floor(Oxygen + 0.5)

                if v1307 ~= n15 and TableFind(t44, v1307) then
                    u2:Notify({
						Title = "Oxygen decreasing",
						Description = "Your oxygen is at " .. tostring(v1307) .. "%",
						Time = 3
					})
                    n15 = v1307
                end
            end
        end)
    end

    if u140:FindFirstChild("DigitalTimer") then
        local n16 = -1

        u140.DigitalTimer:GetPropertyChangedSignal("Value"):Connect(function()
            if Toggles2.NotifyHasteTime and Toggles2.NotifyHasteTime.Value then
                local DigitalTimerValue = u140.DigitalTimer.Value

                if TableFind({
					90,
					60,
					30,
					15
				}, DigitalTimerValue) and DigitalTimerValue ~= n16 then
                    n16 = DigitalTimerValue
                    u2:Notify({
						Title = "Haste notification",
						Description = "Haste is coming in " .. DigitalTimerValue .. " seconds!",
						Time = 3
					})
                end
            end
        end)
    end
end
v155()
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    v155()
end)
Connections.FogHandler = Lighting:GetPropertyChangedSignal("FogEnd"):Connect(function()
    if Lighting.FogEnd ~= 10000000 then
        t12.OldFog = Lighting.FogEnd
    end

    if Toggles2.RemoveCameraFog.Value then
        Lighting.FogEnd = 10000000
    end
end)
Connections.FogHandler2 = Lighting.DescendantAdded:Connect(function(descendant)
    if not descendant:IsA("Atmosphere") then
        return
    end

    descendant:SetAttribute("Density_Old", descendant.Density)

    if Toggles2.RemoveCameraFog.Value then
        descendant.Density = 0
    end

    local connection14 = descendant:GetPropertyChangedSignal("Density"):Connect(function()
        if descendant.Density ~= 0 then
            descendant:SetAttribute("Density_Old", descendant.Density)
        end

        if Toggles2.RemoveCameraFog.Value then
            descendant.Density = 0
        end
    end)

    descendant.Destroying:Once(function()
        connection14:Disconnect()
    end)
    table.insert(Connections, connection14)
    table.insert(t12.FogInstances, descendant)
end)
LocalPlayer.CharacterAppearanceLoaded:Connect(function()
    task.wait(1)

    local v947 = v141()

    if Toggles2.RemoveCameraBobbing.Value and (v947 and v947.spring) then
        v947.spring.Speed = 9000000000
    end
end)
LocalPlayer.CharacterAdded:Connect(function()
    task.wait(1)
    u139 = nil

    local v948 = v141()

    if Toggles2.RemoveCameraBobbing.Value and (v948 and v948.spring) then
        v948.spring.Speed = 9000000000
    end

    if Toggles2.DisableJumpscares.Value then
        v143(true)
    end
end)
u4:SetFolder("cheesy/Data")
u4:SetLibrary(u2)
u4.BuiltInThemes.cheesy = {
	#u4.BuiltInThemes + 1,
	{
		FontColor = "ebebeb",
		MainColor = "0c0c0c",
		AccentColor = "ffc33f",
		BackgroundColor = "151518",
		OutlineColor = "28282a",
		BackgroundImage = ""
	}
}
pcall(function()
    writefile(u4.Folder .. "/themes/default.txt", "cheesy")
end)
u3:SetFolder("cheesy/Configs")
u3:SetLibrary(u2)
local v156 = v54:AddTab("Settings", "palette")
local v157 = v156:AddLeftGroupbox("Menu")
v157:AddToggle("ShowCustomCursor", {
	Text = "Custom Cursor",
	Default = false,
	Callback = function(p196)
    u2.ShowCustomCursor = p196
end
})
v157:AddButton({
	Text = "Test Notification",
	Tooltip = "Sends a test notification with the selected style and sound settings",
	Func = function()
    t28.PlaySound()
    u2:Notify({
			Title = "Test",
			Time = 5
		})
end
})
v157:AddLabel("Menu Keybind"):AddKeyPicker("MenuKeybind", {
	Default = "RightShift",
	NoUI = true,
	Text = "Menu Keybind"
})
u2.ToggleKeybind = Options2.MenuKeybind
v157:AddDivider()
v157:AddDropdown("UILibrary", {
	Text = "UI Library",
	Values = {
		"Obsidian",
		"Linoria"
	},
	Default = u1 ~= "Linoria" and 1 or 2,
	Callback = function(p197)
    u1 = p197
    v11()
    u2:Notify({
			Title = "UI Library",
			Description = "Re-execute the script to apply",
			Time = 5
		})
end
})
v157:AddDivider()
v157:AddButton({
	Text = "Unload Script",
	Tooltip = "Completely unloads the script and cleans up everything",
	Func = function()
    u2:Unload()
end
})
u4:ApplyToGroupbox(v157)
u3:SetIgnoreIndexes({
	"MenuKeybind",
	"UILibrary"
})
u3:BuildConfigSection(v156)
u3:LoadAutoloadConfig()
if CurrentFloor == "Lobby" then
    for _, v in pairs(Toggles2) do
        if v.Value then
            v:SetValue(false)
        end
    end
end
local function v160()
    _Unloading = true
    pcall(v95)
    pcall(v86)
    pcall(v82)
    pcall(v81)

    if FreecamConnection then
        FreecamConnection:Disconnect()
        FreecamConnection = nil
    end

    local FreecamPart = workspace:FindFirstChild("FreecamPart")

    if FreecamPart then
        FreecamPart:Destroy()

        local v952 = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")

        if v952 then
            v952.Anchored = false
        end

        LocalPlayer.CameraMinZoomDistance = LocalPlayer:GetAttribute("fc_om") or 0.5
        LocalPlayer.CameraMaxZoomDistance = LocalPlayer:GetAttribute("fc_ox") or 128
    end

    if CameraConnection then
        CameraConnection:Disconnect()
        CameraConnection = nil
    end

    if _TPCharConn then
        _TPCharConn:Disconnect()
        _TPCharConn = nil
    end

    if CurrentCamera then
        CurrentCamera.CameraType = Enum.CameraType.Custom
    end

    if NoclipConnection then
        NoclipConnection:Disconnect()
        NoclipConnection = nil
        pcall(v88, true)
    end

    if connection then
        connection:Disconnect()
        connection = nil
    end

    if _AutoHideConnection then
        _AutoHideConnection:Disconnect()
        _AutoHideConnection = nil
    end

    if AnticheatConnection then
        AnticheatConnection:Disconnect()
        AnticheatConnection = nil
    end

    if InfiniteJumpConnection then
        InfiniteJumpConnection:Disconnect()
        InfiniteJumpConnection = nil
    end

    if JumpAttributeConnection then
        JumpAttributeConnection:Disconnect()
        JumpAttributeConnection = nil
    end

    if SlideAttributeConnection then
        SlideAttributeConnection:Disconnect()
        SlideAttributeConnection = nil
    end

    if DisableIdleKickConn then
        DisableIdleKickConn:Disconnect()
        DisableIdleKickConn = nil
    end

    if DisplayInfoConn then
        DisplayInfoConn:Disconnect()
        DisplayInfoConn = nil
    end

    if UhhhhConn then
        UhhhhConn:Disconnect()
        UhhhhConn = nil
    end

    if u80 then
        u80:Disconnect()
        u80 = nil
    end

    if _FloorConnection then
        _FloorConnection:Disconnect()
        _FloorConnection = nil
    end

    if KnobFarmConn then
        KnobFarmConn:Disconnect()
        KnobFarmConn = nil
    end

    if t12 then
        t12.KnobFarmActive = false
        t12.KnobFarmStarted = false
    end

    if t12 and t12.LobbyGuardConnection then
        t12.LobbyGuardConnection:Disconnect()
        t12.LobbyGuardConnection = nil
    end

    if Phase then
        if Phase.Connection then
            Phase.Connection:Disconnect()
            Phase.Connection = nil
        end

        if Phase.Body then
            Phase.Body:Destroy()
            Phase.Body = nil
        end

        if Phase.CharConn then
            Phase.CharConn:Disconnect()
            Phase.CharConn = nil
        end
    end

    v120()

    if t35 and t35.NoclipConn then
        t35.NoclipConn:Disconnect()
        t35.NoclipConn = nil
    end

    if Connections then
        for _, v in pairs(Connections) do
            pcall(v.Disconnect, v)
        end

        if Connections.InfiniteItemsHandler then
            Connections.InfiniteItemsHandler:Disconnect()
            Connections.InfiniteItemsHandler = nil
        end

        if Connections.PadlockConnection then
            Connections.PadlockConnection:Disconnect()
            Connections.PadlockConnection = nil
        end
    end

    for _, v in pairs(ESPConnections or {}) do
        pcall(v.Disconnect, v)
    end

    if _FullbrightConnection then
        _FullbrightConnection:Disconnect()
        _FullbrightConnection = nil
    end

    if _FullbrightRoomConn then
        _FullbrightRoomConn:Disconnect()
        _FullbrightRoomConn = nil
    end

    pcall(v132)

    if Folder then
        Folder:Destroy()
    end

    pcall(function()
        for k, v in pairs(FakePrompts or {}) do
            pcall(function()
                v.Parent = k.Parent
                k:Destroy()

                if v.Parent then
                    v.HoldDuration = v:GetAttribute("HoldDuration_Old") or v.HoldDuration
                    v.RequiresLineOfSight = v:GetAttribute("RequiresLineOfSight_Old") or v.RequiresLineOfSight
                    v.MaxActivationDistance = v:GetAttribute("MaxActivationDistance_Old") or v.MaxActivationDistance
                end
            end)
        end
    end)

    if _TrackedPrompts then
        for _, v in ipairs(_TrackedPrompts) do
            pcall(function()
                if v.Parent then
                    v.HoldDuration = v:GetAttribute("HoldDuration_Old") or v.HoldDuration
                    v.RequiresLineOfSight = v:GetAttribute("RequiresLineOfSight_Old") or v.RequiresLineOfSight
                    v.MaxActivationDistance = v:GetAttribute("MaxActivationDistance_Old") or v.MaxActivationDistance
                end
            end)
        end
    end

    for _, descendant in workspace:GetDescendants() do
        task.spawn(function()
            pcall(Functions.RemoveESP, descendant)
        end)
    end

    pcall(v15.Unload, v15)
    pcall(function()
        if Executor.hookmetamethod and MainHook then
            Executor.hookmetamethod(game, "__namecall", MainHook)
        end
    end)
    pcall(function()
        if Executor.hookmetamethod and OtherHook then
            Executor.hookmetamethod(game, "__index", OtherHook)
        end
    end)
    pcall(function()
        local v1311 = v141()

        if v1311 then
            v1311.tooloffset = Vector3.zero
            v1311.spring.Speed = 8
            v1311.fovtarget = 70
            v1311.csgo = CFrame.new()
        end
    end)
    pcall(function()
        workspace.CurrentCamera.FieldOfView = 70
    end)
    pcall(function()
        workspace.Gravity = 196.2
    end)
    pcall(function()
        Lighting.FogEnd = t12 and t12.OldFog or Lighting.FogEnd
    end)
    pcall(function()
        for _, v in ipairs(t12.FogInstances or {}) do
            local Density_Old = v:GetAttribute("Density_Old")

            if Density_Old then
                v.Density = Density_Old
            end
        end
    end)
    pcall(function()
        local v1315 = ReplicatedStorage:FindFirstChild("FloorReplicated") and (ReplicatedStorage.FloorReplicated:FindFirstChild("ClientRemote") and ReplicatedStorage.FloorReplicated.ClientRemote:FindFirstChild("Haste"))

        if v1315 then
            local Ambience = v1315:FindFirstChild("Ambience")

            if Ambience then
                Ambience.Playing = true
            end
        end
    end)
    pcall(function()
        local PlayerGui11 = LocalPlayer:FindFirstChildOfClass("PlayerGui")

        if PlayerGui11 then
            local DisplayInfoLabel2 = PlayerGui11:FindFirstChild("DisplayInfoLabel", true)

            if DisplayInfoLabel2 then
                DisplayInfoLabel2:Destroy()
            end
        end
    end)

    if t12 then
        if t12.SeekNodesFolder then
            t12.SeekNodesFolder:Destroy()
        end

        if t12.RoomsNodesFolder then
            t12.RoomsNodesFolder:Destroy()
        end

        if t12.OriginalGetMoveVector then
            pcall(function()
                require(LocalPlayer.PlayerScripts.PlayerModule):GetControls().GetMoveVector = t12.OriginalGetMoveVector
            end)
            t12.OriginalGetMoveVector = nil
        end

        t12.AnticheatDisabled = false
        t12.RoomsAutoWalkActive = false
    end

    if AutoMinecartConnection then
        AutoMinecartConnection:Disconnect()
        AutoMinecartConnection = nil
    end

    if Character then
        pcall(function()
            Character:SetAttribute("CanJump", OldJump or false)
        end)
        pcall(function()
            Character:SetAttribute("CanSlide", OldSlide or false)
        end)
        pcall(function()
            Character:SetAttribute("SpeedBoost", 0)
        end)
        pcall(function()
            Character:SetAttribute("SpeedBoostBehind", 0)
        end)
        pcall(function()
            Character:SetAttribute("SpeedBoostExtra", 0)
        end)
        pcall(function()
            Character:SetAttribute("SpeedBoostMod", 0)
        end)
        pcall(function()
            Character:SetAttribute("AntiSpeed", nil)
        end)
        pcall(function()
            Character:SetAttribute("Antispeed", nil)
        end)
        pcall(function()
            Character:SetAttribute("SpeedBypass", nil)
        end)
        pcall(function()
            Character:SetAttribute("Climbing", nil)
        end)
    end

    if Humanoid then
        pcall(function()
            Humanoid.WalkSpeed = 16
        end)
        pcall(function()
            Humanoid.JumpPower = 5
        end)
        pcall(function()
            Humanoid.HipHeight = 2.367
        end)
        pcall(function()
            Humanoid.PlatformStand = false
        end)
        pcall(function()
            Humanoid:SetAttribute("AntiSpeed", nil)
        end)
        pcall(function()
            Humanoid:SetAttribute("Antispeed", nil)
        end)
    end

    if HumanoidRootPart then
        HumanoidRootPart.AssemblyLinearVelocity = Vector3.zero
    end

    for _, v in pairs(t10 or {}) do
        if type(v) == "table" then
            for _, v16 in ipairs(v) do
                pcall(v16.Destroy, v16)
            end

            TableClear(v)
        end
    end

    for _, v in pairs(PartProperties or {}) do
        pcall(function()
            v.CustomPhysicalProperties = PartProperties[v]
        end)
    end

    pcall(function()
        local v1319 = v53()

        if v1319 then
            for _, v in ipairs({
				"Screech",
				"GlitchScreech",
				"A90",
				"Dread",
				"SurgeVignette"
			}) do
                local v1322 = v1319:FindFirstChild(v .. "_Disabled")

                if v1322 then
                    v1322.Name = v
                end
            end
        end
    end)

    if FakeEvents then
        if FakeEvents.Screech_Real then
            FakeEvents.Screech_Real.Parent = u36
        end

        if FakeEvents.Shade_Real then
            FakeEvents.Shade_Real.Parent = u36
        end

        if FakeEvents.A90_Real then
            FakeEvents.A90_Real.Parent = u36
        end

        if FakeEvents.Surge_Real then
            FakeEvents.Surge_Real.Parent = u36
        end

        for _, v in pairs(FakeEvents) do
            if type(v) == "Instance" and v:IsA("RemoteEvent") then
                pcall(v.Destroy, v)
            end
        end
    end

    if Character then
        local CollisionClone = Character:FindFirstChild("CollisionClone")

        if CollisionClone then
            CollisionClone:Destroy()
        end

        local CollisionPartClone = Character:FindFirstChild("CollisionPartClone")

        if CollisionPartClone then
            CollisionPartClone:Destroy()
        end
    end

    if LocalPlayer then
        LocalPlayer.CameraMinZoomDistance = 0.5
        LocalPlayer.CameraMaxZoomDistance = 128
        LocalPlayer:SetAttribute("fc_om", nil)
        LocalPlayer:SetAttribute("fc_ox", nil)
        LocalPlayer:SetAttribute("fc_p", nil)
        LocalPlayer:SetAttribute("fc_y", nil)
    end
end
getgenv().CheesyUnload = v160
u2:OnUnload(function()
    _Unloading = true
    v160()
    getgenv().CheesyUnload = nil
end)
u2.ScreenGui.Destroying:Once(function()
    _Unloading = true
    v160()
end)
local LogService = game:GetService("LogService")
local connection15
connection15 = LogService.MessageOut:Connect(function(p198)
    if p198 == "client teleporting" then
        if t12.AnticheatDisabled then
            t12.AnticheatDisabled = false
            u2:Notify({
				Title = "Anticheat re-enabled",
				Description = "Interact with a ladder for re-disabling",
				Time = 5
			})
        end

        connection15:Disconnect()
    end
end)
t4.FoolsHotel:SetVisible(false)
u2:Toggle(true)
u2:Notify({
	Title = "Loaded cheesy in " .. math.floor((tick() - LoadStart) * 1000) / 1000 .. " seconds",
	Description = "discord.gg/CuZJQaCquK",
	Time = 5
})
if t12.UILibrary == "Linoria" then
    task.spawn(function()
        task.wait(2)
        u2:Notify({
			Title = "Linoria limitations",
			Description = "Linoria is very unstable, and all labels do not work. I recommend using obsidian if you're new, as everything is guaranteed to work",
			Time = 8
		})
    end)
end
if Toggles2.AutoInteract then
    pcall(function()
        Toggles2.AutoInteract:SetDisabled(not hasFirePrompt)
    end)
end
if Toggles2.InfiniteItemsToggle then
    pcall(function()
        Toggles2.InfiniteItemsToggle:SetDisabled(not hasFirePrompt)
    end)
end
if Toggles2.InfiniteCrucifix then
    pcall(function()
        Toggles2.InfiniteCrucifix:SetDisabled(not hasFirePrompt)
    end)
end
if Toggles2.RemoveSeekTrigger then
    pcall(function()
        Toggles2.RemoveSeekTrigger:SetDisabled(not hasFireTouch)
    end)
end
if Toggles2.RemoveFigure then
    pcall(function()
        Toggles2.RemoveFigure:SetDisabled(not hasNetworkOwner)
    end)
end
if Toggles2.RemoveFigureMines then
    pcall(function()
        Toggles2.RemoveFigureMines:SetDisabled(not hasNetworkOwner)
    end)
end
if Toggles2.AutoHeartbeatMinigame then
    pcall(function()
        Toggles2.AutoHeartbeatMinigame:SetDisabled(not hasHookMeta)
    end)
end
if Toggles2.BypassEyes then
    pcall(function()
        Toggles2.BypassEyes:SetDisabled(not hasHookMeta)
    end)
end
if Toggles2.BypassLookman then
    pcall(function()
        Toggles2.BypassLookman:SetDisabled(not hasHookMeta)
    end)
end
if Toggles2.RoomsAutoWalkSpoofFootsteps then
    pcall(function()
        Toggles2.RoomsAutoWalkSpoofFootsteps:SetDisabled(not hasHookMeta)
    end)
end
pcall(function()
    execName = RootEnv.identifyexecutor()
end)
if hasFirePrompt and (hasFireTouch and (hasReplicateSignal and (hasNetworkOwner and hasHookMeta))) then
    u2:Notify({
		Title = "Yay",
		Description = "All features should work as all used functions are supported :D (" .. tostring(execName) .. ")",
		Time = 5
	})
else
    u2:Notify({
		Title = "Uh oh",
		Description = "Some features may not work as not all used functions are supported :( (" .. tostring(execName) .. ")",
		Time = 5
	})
end
